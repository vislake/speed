{
  "name": "@speed/test-utils",
  "version": "0.0.0",
  "private": true,
  "description": "The workspace's shared frontend test-support package: the single home of the DOM-test scaffolding the @speed packages and the reference-app web host would otherwise each carry a copy of -- the axe-core assertion harness (two rule tiers), the render-primitive layer, the vitest setup, the MediaQueryList stub, the emotion CSS probe, the scripted session harness and the real-client network rig",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@tanstack/react-query": "^5.62.0",
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/react": "^16.3.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5",
    "axe-core": "^4.13.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "^5.9.3"
  }
}
