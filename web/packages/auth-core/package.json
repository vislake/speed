{
  "name": "@speed/auth-core",
  "version": "0.0.0",
  "description": "Speed auth core: the browser session lifecycle (password and SMS login, logout, tenant switch, step-up and silent refresh) as a memory-only state machine over the generated authn surface of @speed/api-sdk, with the in-memory access-token store as the bridge into @speed/api-client, plus React hooks (useAuthState, useCurrentTenant, usePermission) reading one host-attached session",
  "type": "module",
  "sideEffects": false,
  "files": ["dist", "README.md", "AGENTS.md"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "build": "pnpm --filter @speed/api-sdk build && tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@speed/api-client": "workspace:*",
    "@speed/api-sdk": "workspace:*"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5",
    "jsdom": "^30.0.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
