name: e2e
# STUB -- not implemented. Triggered runs fail loudly at the guard step
# below; nothing in this pipeline can pass silently.
#
# PURPOSE: reference-app end-to-end tests with Playwright -- run on
# merge to main and daily, target under 20 minutes, with recorded and
# replayed AI responses, so real provider calls happen only during
# milestone acceptance and demos.
#
# WHAT EXISTS: no e2e suite -- no Playwright config, no e2e/ directory,
# no test doubles for provider calls, and no browser-driven frontend
# (the web/ workspace ships libraries only, with no application shell
# and no Playwright or e2e tooling anywhere). The reference-app server
# itself runs and its Go module is unit-tested, but nothing exercises it
# end to end.
#
# TRIGGER: workflow_dispatch only. A scheduled stub would only produce
# daily noise, so the push-to-main and daily triggers are not enabled.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  e2e:
    name: reference-app e2e (stub guard -- not implemented yet)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Stub guard
        run: |
          echo "error: the e2e pipeline is not implemented. This failing run"
          echo "is its stub guard, so the pipeline can never pass silently."
          echo "Wire the reference-app Playwright suite when the roadmap M4"
          echo "e2e item lands."
          exit 1
