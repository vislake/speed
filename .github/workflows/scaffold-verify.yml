name: scaffold-verify

# STUB -- not implemented. Triggered runs fail loudly at the guard step
# below; nothing in this pipeline can pass silently.
#
# PIPELINE (docs/internal/18-cicd.md pipeline table, scaffold-verify row):
# generate a fresh project with the CLI, build it, boot it once in each
# deployment mode, and smoke-test it. Target under 15 minutes. Runs daily
# and after every release; a post-release failure marks the released
# version unusable (18-cicd release section, step 6).
#
# DESIGN-TABLE TRIGGER: daily plus post-release; both are enabled by the
# implementing round.
#
# WHAT EXISTS TODAY (updated by the saasctl round): go/saasctl v0.1 ships
# the project-generation side of this pipeline -- `saasctl new` materializes
# a bootable consumer project from go:embed-ed templates (selection over
# {authn, rbac, org}, golden output pinned byte-identical), and the round's
# end-to-end consumer proof generated, tidied, built, booted and smoke-tested
# one (recorded answer by answer in go/saasctl/AGENTS.md's Testing section).
# Still absent: create-saas-app and the web-side template (frontend-scaffold
# round), and -- the point of this stub -- no generated project has yet been
# booted once in each deployment mode by a pipeline. The GATE below is the
# earliest partial wiring's full form; nothing in this file changes until
# then except this header.
#
# GATE: the roadmap M4 acceptance "saasctl new + create-saas-app generate a
# fresh project that boots in both deployment modes"; earliest partial
# wiring happens when go/saasctl grows a real new-project command.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  scaffold-verify:
    name: scaffold verify (stub guard -- not implemented yet)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Stub guard
        run: |
          echo "error: the scaffold-verify pipeline is not implemented. This"
          echo "failing run is its stub guard, so the pipeline can never pass"
          echo "silently. Wire the generate/build/boot/smoke legs when the"
          echo "saasctl project scaffold exists (roadmap M4 acceptance)."
          exit 1
