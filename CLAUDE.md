# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status


**Milestone M0 is complete** (see `docs/internal/15-roadmap.md`): the M0 deliverables below — the Go modules, the web packages, the tooling and the CI pipelines — are all landed on main with tests passing, and, measured against the milestone's own five exit conditions (`docs/internal/15-roadmap.md`'s M0 row), the live CI this section's closing paragraphs describe enforces some fully, some partly, and one not at all — not the blanket pass a "complete" milestone would suggest. Fully enforced: the spec-change-fails-compilation condition (`api-contract.yml` rebuilds the reference app after regenerating both halves from the spec, so a spec change with no matching implementation change cannot compile). Partly enforced: the third condition bundles three sub-checks — i18n lint and contract-generated-artifact consistency are both live in CI (`docs-check.yml`'s key-parity check plus the compiled `pkgcore/i18n` and `@speed/i18n` catalogs; `api-contract.yml`'s seven `git diff --exit-code` gates) — but the documentation-example compile check only covers godoc `Example` functions inside each module's own unit suite; examples embedded in markdown prose (`AGENTS.md`, READMEs, ADRs) have no compile harness at all, a gap `docs-check.yml`'s own header records. The dual-dialect database condition is likewise partial: SQLite runs on every PR but PostgreSQL only on `full-ci`-labeled PRs. Not enforced at all: the dual-deployment-mode condition — no `Kernel.Bootstrap` has ever run under the distributed mode itself. The reference app's refusal is gone (its `cmd/server/server.go` now parses `SPEED_DEPLOYMENT_MODE`, defaulting to standalone and refusing nothing), and its Docker-backed integration tier boots the real server so `Kernel.Bootstrap` composes a real Redis Streams `EventBus` — genuine distributed infrastructure, but in a standalone topology, which leaves the distributed mode's own end-to-end path unproven; `pr-full.yml`'s header records exactly that in its NOT YET list. The fifth condition, one-command lockstep release, is proven offline only: one command verifies the one-version plan and exits 0, but nothing publishes, and real publishing lands at M4. `go.work` lists the planned Go modules; the ones awaiting their M1-and-later implementing rounds are still placeholder stubs (`go.mod` + a one-line `doc.go` + an `AGENTS.md` pointing at the relevant design doc — nothing to build against yet). The following modules have real, tested implementation: `go/pkgcore`, the dependency floor every other module sits on (the `Module`/`Registry`/`Kernel` wiring contract — one `Register(reg *Registry)` call per module, with the `Registry` built by the three-argument `NewRegistry(bus, kv, mailer)` — tenant-context primitives, `apperr`, the bootstrap `config` loader, the M1 audit-infrastructure round's `Actor{Type, ID, DisplayName}` context carrier (`WithActor`/`ActorFromContext`, and `WithOnBehalfOf`/`OnBehalfOfFromContext` layered independently so an impersonated user's `Actor` and the real administrator's `OnBehalfOf` can both be set without one overwriting the other — doc 10's dual-identity rule) that `go/dbkit/audit`'s collection mechanisms read at capture time, and the four infrastructure seams, resolved by the deployment-composition retrofit through `database/sql`-style seam registries — `SeamRegistry[T]`/`Registration[T]` in `go/pkgcore/seam_registry.go`, the eight built-in implementations pre-registered on `EventBusRegistry`/`KVStoreRegistry`/`MailerRegistry`/`ObjectStoreRegistry` in `builtin_implementations.go`, and the `PresetStandalone`/`PresetDistributed` maps of `preset.go` (`Preset` = seam-name → implementation-name): `KVStore` (in-memory, Redis), `EventBus` (in-process channel, Redis Streams with consumer-group readers — both Redis halves living in pkgcore itself), `Mailer` (`NewConsoleMailer`, `NewSMTPMailer`), `ObjectStore` (`NewLocalObjectStore` — a throwaway temp directory by default, a persistent one when the host injects its own via `WithObjectStore` — and `NewS3ObjectStore`). `NewKernel(opts...)` takes no deployment mode: `WithDeploymentMode` declares the topology, `WithPreset` swaps the whole map, and `WithEventBus`/`WithKVStore`/`WithMailer`/`WithObjectStore` inject one implementation plus its declared capability bits (`capability.go`'s `MultiReplicaSafe`/`SurvivesRestart`/`Stateless` — the third added right after so stateless implementations, the console mailer first among them, skip a `SurvivesRestart` banner that has nothing to warn about for them); `Kernel.Bootstrap` resolves every seam and fails startup with `ErrCapabilityUnsatisfied` — naming the seam, implementation, missing capability and mode — when a resolved implementation cannot satisfy the declared mode's `RequiredCapabilities()` (the four `ErrMissingDistributed*` sentinels are gone), printing a startup banner rather than failing when only `SurvivesRestart` is missing; `Registry.Mailer()`/`Registry.ObjectStore()` expose the resolved transports, and a `Registry` built directly with `NewRegistry(bus, kv, mailer)` bypasses registries and presets alike, exactly as it always did; plus the merged message catalog: `pkgcore/i18n` aggregates the locale files every module ships (each message-shipping module's `Locales()` embed.FS, one `zh-CN.toml` and one `en-US.toml` in M0 — `go/pkgcore/locales` carrying pkgcore's own seed bundle) through `Builder.AddModule` while `Kernel.Bootstrap` walks the module graph, `Build` freezing the merge into an immutable `*Catalog` whose `Lookup`/`LookupPlural` render a message in the locale asked for, never silently falling back to another language; the catalog is installed on the `Registry` only after every module has registered (`Registry.Locales()` is nil inside `Register`), and a `Registry` built directly with `NewRegistry` carries neither catalog nor ObjectStore — both arrive through `Bootstrap`); `go/dbkit` (the dual-dialect `*gorm.DB` wrapper, the mandatory generic `Repository[T]`, `MigrationRegistry`, field-level encryption, and the blind-index machinery that keeps an encrypted field queryable by exact match — `BlindIndex` computing HMAC-SHA256 over normalized input, with `NewBlindIndexer` binding a validated 32-byte key and a canonical normalizer (`NormalizeEmail`, `NormalizePhoneE164`) to each index column; plus the M1 audit-infrastructure round's two collection mechanisms and their persistence: `audit_capture.go`'s `Auditable` marker interface and `Options.AuditBus` install a GORM write-capture plugin publishing `EventWriteCaptured` after a successful Create/Update/Delete against an opted-in model — nil by default, so every pre-existing `Open` call site is unaffected — and the `go/dbkit/audit` subpackage carries the append-only `AuditEvent` model and its dual-dialect migrations, `Repository` (`Insert`/`Get`/`ListByTenant`, deliberately no Update or Delete method), the declarative `Emit` collection path validated against `Registry.AuditActions`, and the `Module` persister subscribing to both mechanisms plus `tenancy`'s already-shipped system-context-entered event; `AuditEvent` is platform data with a real, non-enforced `tenant_id` column, the same treatment `go/jobs`'s `jobRecord` and `go/config`'s `row` already get, never `dbkit.TenantScoped` — and `go/dbkit/AGENTS.md`'s "Audit trail collection" section records a known limitation the reference app's own wiring surfaced: `AuditBus` must not point a persister at the same SQLite file a `dbkit.Repository[T]` write's own open transaction is still writing to, or the write-capture plugin's synchronous, same-goroutine publish deadlocks into `SQLITE_BUSY`); `go/dbkit` additionally carries the mark-delete (soft-delete) round's `dbkit.SoftDeletable` capability and `Repository[T].Restore` (`soft_delete.go`): a model opting in by carrying `DeletedAt`/`DeletedBy` fields gets a `Delete` that marks instead of physically removing the row (an UPDATE hidden from ordinary reads by a query-only GORM auto-scope plugin `Open` installs unconditionally), with every non-opted-in model's `Delete` byte-for-byte unchanged; `HardDelete`, the irreversible compliance-erasure counterpart docs/internal/04-data-and-tenancy.md's delete-semantics section frames as mark-delete's sequential-next phase, is landed in the round after (`hard_delete.go`): a real physical `DELETE`, never a mark-delete, a soft-deleted row as removable as a live one (the auto-scope is query-only), gated on a system context of presence only -- an ordinary tenant-scoped context is refused with `ErrHardDeleteRequiresSystemContext` (`dbkit.hard_delete_requires_system_context`, an `apperr.Invalid` deliberately not a `Forbidden`, since dbkit does no authorization; who may hold the grant at all is the caller-side whitelist of admin/compliance/jobs/authn, enforced outside the package, with production grants entering through tenancy's audited `WithSystemContext` wrapper) before the database is touched; even with the gate passed the tenant stays mandatory and binding -- a system context never substitutes for a tenant (`pkgcore.ErrNoTenant` unmodified) and never widens the delete beyond the ctx tenant's rows, the cross-tenant non-eraser property re-proven against a real restricted role and RLS policy in `go/dbkit/integration_test/postgres_hard_delete_rls_test.go`; the physical DELETE involves no migration or DDL (the schema always permitted it) and is captured by the `Options.AuditBus` write-capture plugin as Operation "delete" with `After` nil and no duplicate, pinned by `TestAuditCapturePlugin_HardDelete_ClassifiesAsDelete`; the orchestration around the mechanism -- retention-window configuration, the cleanup schedule, right-to-erasure intake -- remains `compliance` module (M4) work, until which the method exists for hosts that need a row actually gone behind the gate; the reference app's notes module (`Note` implementing `SoftDeletable`) is the first consumer of both halves, proved service-level (no HTTP delete/restore endpoint yet) by `internal/notes/repository_test.go`'s full create/delete/restore/delete lifecycle test plus `TestRepository_HardDelete_SoftDeletedNote_PhysicallyRemoved` and `TestRepository_HardDelete_PlainTenantContext_Refused`; `go/tenancy` (the tenant-resolution `Middleware`, the audited `WithSystemContext` escape hatch, and the `tenancytest.AssertIsolated` / `AssertNotTenantScoped` suites every other module's repositories are required to run); `go/config` (dynamic configuration, the roadmap's M1 config cell: every module declares its configuration items and feature flags on the registry during `Register`, and `Attach` — called exactly once, after `Bootstrap` returns — freezes that schema and refuses a cipher-less startup while any Sensitive item exists (`ErrCipherRequired`); values live in the `configs` table under the `system`→`tenant` scope tiers, served through a process-local cache invalidated by the `config.item.changed` events every write publishes — Sensitive items sit encrypted at rest under the host-injected `dbkit.NewCipher` key, and their events carry `[redacted]` markers in both value slots, never the plaintext — with an anti-loss poller behind the events and `Service.Close` stopping both; its two pre-auth GET/HEAD endpoints, `/api/config/public` and `/api/system/features` (exported `PathPublic`/`PathSystemFeatures` constants, so a host's middleware allowlist names them without stringly drift), serve the effective Public values and dependency-resolved enabled flags for the tenant the host-injected `tenancy.Resolver` maps the request to, falling back to platform defaults — never an error — when nothing matches, per the login-page rule; the reference app wires it as the module's first consumer, registering the brand/support/AI keys from the notes module); `go/dbkit/audit`'s mandatory-first-consumer proof is the same reference app's notes module: `internal/notes/handler.go`'s `NotesCreateNote` calls `audit.Emit` explicitly after a note is created (under the already-declared `notes.note.create` audit action), and `cmd/server/server.go` wires `audit.New(db)` into the app's own `Kernel.Bootstrap` module set, sharing the notes module's database connection; `cmd/server/server_test.go`'s `TestBuildServer_NoteCreate_PersistsAuditEvent` proves the row lands and reads back through a real `audit.Repository.ListByTenant` call via a second connection to the same SQLite file, not a mock — the app deliberately does not wire `dbkit.Options.AuditBus` itself, for the same-file deadlock reason `go/dbkit`'s own census entry above records; and `go/observability` (OpenTelemetry initialization via `Init` for both deployment modes, the context-aware `FromContext` structured logger — wrapped by an on-by-default PII/secret redaction layer that masks sensitive attribute keys and secret-shaped values before any sink, with no per-call opt-out — and an HTTP `Middleware` recording request-count/duration metrics behind a cardinality-bounded route label — `tenant_id` never becomes a metric label, per this file's own Security rule); `go/jobs` (the `Queue`/`Task`/`Job`/`Handler` contract shared by both deployment modes, `StandaloneQueue` — the standalone mode's SQLite-backed in-process worker pool — and `AsynqQueue`, the distributed mode's Redis-backed implementation); `go/storage` (the roadmap's M2 storage cell, landed ahead of its planned window as this round's module — the first module whose payload bytes live outside the database: metadata in two tenant-scoped (`TenantScoped`, `tenancytest.AssertIsolated`) tables with dual-dialect migrations, `objects` and `object_derivatives`, while the bytes travel through the `ObjectStore` seam the kernel resolves (`Registry.ObjectStore()`) under keys the module itself derives — `ObjectKey(tenant, objectID)`, `DerivativeKey(tenant, objectID, kind)` — never keys a caller supplies. `ObjectService` runs the transfer lifecycle as the three-step protocol `uploading` → `completed`: `Create` opens the upload window against the caller's declared size/type/checksum and optional requested retention (capped by the host's `WithMaxObjectLifetime` ceiling), `Upload` streams the request body into the store, and `Complete` revalidates what actually arrived — the probe of the stored bytes is the authority over the uploader's claims: real size and MIME, byte and pixel limits, and a structural metadata strip (JPEG APP segments and PNG eXIf; a file the walker cannot verify structurally is refused, never passed through) — before finalizing the row with checksum and dimensions and enqueueing the thumbnail-derive task onto the `jobs.Queue` the module's wiring must supply, `DeriveService` fulfilling it into `object_derivatives` (thumbnail the single shipped kind). `LifecycleService` ends object life with a crash-convergent delete protocol — mark the row `deleting`, remove the object's bytes from the store, remove each derivative row's bytes, then remove every row in one transaction, an interruption at any step converged by the next run rather than duplicated — plus the per-tenant expiry sweep a host schedules through `EnqueueExpirySweep` (task `storage.expiry_sweep`, deterministic per-tenant idempotency key), which resumes interrupted deletions, reclaims `uploading` rows whose upload window closed and removes `completed` objects whose requested retention passed; only an `uploading` row is refused deletion, it belonging to the transfer runtime until its window closes. The HTTP surface is the module's own OpenAPI fragment, the fourth after notes, org and authn — `go/storage/api/openapi.yaml`, seven operations under `/api/v1/storage` (create, upload content, complete, list, get, open content, delete), generated by the same pinned oapi-codegen v2.8.0 into a committed `storage-server.gen.go`, implemented by `Handler` behind a `var _ api.ServerInterface` compile-time assertion with the tenant read from the request context, never from the request itself. The reference app is the mandatory first consumer end to end: `cmd/server/server.go` wires `storage.NewModule(db, storage.WithQueue(standaloneQueue))`, and `cmd/server/storage_flow_test.go` drives the composed HTTP stack through one object's full create/upload/complete/read/delete life. What is not shipped is recorded rather than half-built: uploads stream through the server, with presigned direct-to-store uploads and short-lived read URLs deferred to the distributed-mode round (`go/storage/AGENTS.md`'s Deferred list and docs/internal/07's implementation-status note)); `go/notification` (the roadmap's M2 notifications cell, landed ahead of its planned window as this round's module — the platform's messaging surface, built on a live type registry rather than a template store: every notification type is declared by its owning business module during `Register` through `reg.Notifications.Add`, the registry seat this round's `NotificationRegistrar` opened, each `NotificationType` carrying its key, preference-matrix group, default channels and whether recipients may unsubscribe — transactional types like verification codes are not unsubscribable — while the copy itself is never stored here: a type's templates live in its declaring module's own bilingual locale bundles under the `<type_key>.<channel>.<part>` id convention (in-app title/body, email subject/body_text, SMS text) and are rendered by delivery in the recipient's locale from the host's merged catalog at send time — a missing template is a coded `ErrInternal` failure, never a fallback-language message — the type directory's description a second id space of the same shape (`<type_key>.description`). Its own tables span the data-domain table's three scopes: `notification_preferences` and `in_app_messages` are tenant data (`TenantScoped`, `tenancytest.AssertIsolated`); the preference matrix stores per-type × per-channel choices whose absence resolves to the type's declared defaults and whose `Set` refuses an undeclared type or channel outright, and the inbox is this module's first-class channel — every in-app delivery lands one `in_app_messages` row with read state and per-user unread counts, `notification.inbox.created` is published only after the row commits so a consumer reads the row back instead of racing the writer, and an SSE stream endpoint (`GET /api/v1/notifications/stream`, deliberately absent from the fragment because server-sent events are not an OpenAPI 3.0 media type, hand-mounted on the handler's inner mux and recorded in the fragment's header) announces committed rows to the requesting browser with row-then-event ordering. `verified_contacts` is the consent ledger for external recipients — tenant-scoped rows, channel-addressed, address encrypted at rest under a blind-indexed column — consent a state machine over the two paths the design names: `double_opt_in` codes hashed on the row, whose `VerifyCode` checks the hash and flips state in one compare-and-swap, and `business_attested` rows carrying a `ConsentRef`; `unsubscribed` and `bounced` are terminal states every delivery refuses, and a never-verified address can only receive the verification message itself. `send_records` and `platform_blacklist` are platform data, deliberately never `dbkit.TenantScoped` (compile-time `AssertNotTenantScoped`), with `tenant_id` a real, unenforced column: `send_records` keeps one upserted row per delivery under a `UNIQUE (tenant_id, idempotency_key)` index — `IdempotencyKey` the derived delivery key (tenant, type, recipient class and id, channel, params, canonical JSON hashed; never caller-supplied), status a succeeded/failed/skipped vocabulary with the skip's short reason recorded, error text truncated at the write site, `ProviderReceiptID` reserved and empty. Delivery is asynchronous by construction: `Dispatch` enqueues one `jobs` task per recipient per channel (job type `notification.deliver`); the worker rebuilds tenant context, re-checks preferences, consent and addresses at send time — a user's addresses through the host's `UserAddressResolver` seam, since the module's own tables hold no identity data and notification never imports authn — renders the channel's parts, sends through the resolved transport (email the pkgcore `Mailer` seam under the `WithMailFrom` address, SMS the module's own console-first `SMSSender` seam, in-app the inbox row itself) and settles the record; replay convergence is best-effort at-most-once (windows documented in delivery.go), and a transport's `ErrTransportPermanent` marks the contact bounced. The HTTP surface is the module's own OpenAPI fragment, the fifth after notes, org, authn and storage — `go/notification/api/openapi.yaml`, eleven operations under `/api/v1/notifications` (inbox list, unread count, mark-read, type directory, preference list and single-key update, contact roster list/create/verify/resend), generated by the same pinned oapi-codegen v2.8.0 into a committed `notification-server.gen.go`, implemented by `Handler` behind a `var _ api.ServerInterface` compile-time assertion with the tenant read from the request context, never from the request. The reference app is the mandatory first consumer end to end: `cmd/server/server.go` wires `notification.NewModule` with its console SMS sender, mail-from address, both contact blind indexers over the shared cipher, the standalone queue and the demo user-address and subject resolvers, and `cmd/server/notification_flow_test.go` drives the composed stack through the consent-ledger and inbox surfaces. What is not shipped is recorded rather than half-built — platform-blacklist writers and recovery, type-scoped opt-out, per-contact locale negotiation, a pkgcore-level SMS seam, the platform-staff push consumer, tenant-enforced preference tiers, same-type delivery aggregation and rate limiting, admin template editing and the frontend `@speed/notification-ui`, each in `go/notification/AGENTS.md`'s Deferred list with the item-by-item comparison against the target design in docs/internal/07's implementation-status note); `go/ratelimit` (a `KVStore`-backed rate limiter shared by `authn`, `integration`, and other modules that need to limit how often something happens — one dimension per call, with multi-dimension composition left to the caller); `go/authn` (authentication, the roadmap's M1 auth cell — who a caller is, never what they may do, and the module `rbac`'s own entry below never imports; the fixed middleware chain names it first. It owns eight identity-domain tables — `users`, `sessions`, `refresh_tokens`, `login_attempts`, `user_identities`, the verification-code table, and the MFA-factor and recovery-code tables — plus one tenant-domain table, `tenant_sso_configs`, and every one of them is platform or identity data, deliberately never `TenantScoped`, its isolation suite running `AssertNotTenantScoped` over all nine. Passwords are argon2id with the cost floor set at bootstrap config while policy raises it dynamically, and the PHC parameter string travels inside each stored hash rather than in a column the codebase must keep in step with the verifier. Access tokens are Ed25519 EdDSA-signed and pinned by `kid` against a rotating `KeySet` of signing keys (the reference app injects a dev seed via `WithSigningKeys`), refresh tokens are single-use values stored hashed whose replay rotates the whole token family and revokes the session — two concurrent refreshes are treated as theft, so clients must serialise them — and `sessions.current_tenant_id` is re-verified against the host-injected `MembershipReader` on every refresh, an absent reader refusing rather than defaulting. Phone numbers are encrypted at rest and blind-indexed through `dbkit.NewBlindIndexer` for exact-match lookup, with delivery over `NewConsoleSMSSender` for the standalone mode and `NewHTTPSMSSender` for the distributed one (injected via `WithSMSSender`, whose absence fails a distributed bootstrap with `ErrMissingDistributedSMSSender` — the SMS twin of the mailer seam's `ErrMissingDistributedMailer`). Social sign-in spans Google, GitHub, WeChat, DingTalk and Feishu plus enterprise OIDC — WeChat keyed on `unionid`, never `openid`, which is app-scoped and would let two consumers of one WeChat app conflate two accounts — and account auto-linking is gated on a verified provider email *and* a trusted provider, enterprise SSO adding a third condition, an active membership in the tenant configuring the connection, so the no-merge-on-email-alone rule is enforced in code rather than prose; OIDC is tested offline against the module's own `testutil.OIDCServer`, no testcontainers. An account whose last login method is social or SSO cannot shed it (`ErrLastLoginMethod`), and every existence-disclosing answer (user exists, email taken, provider bound) is suppressed so enumeration learns nothing. TOTP lives in an internal, stdlib-only `totp` implementation pinned to the official RFC 4226/6238 test vectors, and step-up verification is never persisted to the session — it outlives exactly one access token. Login, registration, code send/verify and step-up all sit behind sliding-window rate limiting on `go/ratelimit` with progressive lockout, fail closed. The HTTP surface is `go/authn/api/openapi.yaml`, twenty operations under `/api/v1/authn`, generated by the same pinned oapi-codegen v2.8.0 into a committed `authn-server.gen.go` implemented by `Handler` behind a `var _ api.ServerInterface` compile-time assertion; authn's own routes never sit downstream of `tenancy.Middleware` — the tenant comes from the `Principal`'s own claim, resolved per operation by `requirePrincipal`, never from a Host header or a tenant a middleware guessed. That is the middleware-order correction this round ships, pinned by the reference app's tests: `authn.Middleware` verifies optionally (a bad token is a 401, an absent one stays anonymous) and never injects tenant context, while `tenancy.Middleware(authn.NewPrincipalResolver())` is the layer that turns a verified principal into `pkgcore` tenant context; a global `RequireAuthenticated` middleware was tried and rejected because config's two pre-auth endpoints must render a login page before anyone has signed in. The reference app is the mandatory first consumer end to end: `cmd/server/server.go` wires `authn.NewModule` with a dev signing-key seed, a dev blind-index key, a demo `MembershipReader` granting each demo user membership in the demo tenants, and the console SMS sender, composing the chain over every route the pre-auth allowlist does not name (config's host→tenant `DomainResolver` keeps serving its two endpoints; Host plays no part anywhere else); `cmd/server/authn_e2e_test.go` drives all three sign-in channels plus refresh rotation and session revoke through the real composed HTTP stack, and the org round's `org_flow_test.go` was ported onto that same chain — registering real accounts, granting them membership and signing them in where it used to fake a Host header, so org's flow test doubles as a second consumer proof of the authn stack; and `go/rbac` (role-based access control, implemented natively over three `TenantScoped` tables rather than with Casbin — the deviation and its reasons are recorded as an implementation correction in `docs/internal/05-identity-and-access.md`: `casbin_rule` has no `tenant_id` column, so the product's most security-critical table would opt out of all three isolation layers at once. The module's defining property is what it does **not** depend on: it never imports `authn`, because authorization knows only `Subject{TenantID, UserID}` and the authenticating side assembles it. Two interfaces declared here and implemented by the host are the whole of its knowledge of its neighbours — `SubtreeResolver`, which maps an org node id to a materialized path so a subtree-scoped grant can be evaluated without importing `org`, and the middleware's `WithSubjectResolver`, which lets any authenticating layer supply the `Subject`. `Module.Attach` — exactly once, after `Bootstrap` — freezes the snapshot of every permission every module declared, and a grant of anything outside it is refused; evaluation is deny by default with exact `resource:action` matching and no wildcards, `Can` answering the coarse gate and `DataScope` answering which slice of the org tree a grant covers, with node paths resolved at decision time and never denormalized onto a binding row. Decisions are cached per subject and invalidated by the module's own events, so a revoke on one replica converges the others through the bus rather than through a TTL. It mounts no HTTP routes — role management is an admin-console surface and `/me` belongs to `authn` — and contributes `RequirePermission`/`RequirePermissionFunc`, the permission gate the fixed middleware chain names after authentication; the reference app is its first consumer, really gating the notes route on `notes:read`/`notes:write`); and `go/org` (a tenant's organization tree, the memberships bound to it, and the invitations that create them — the roadmap's M1 org cell, and the module this file's own module-boundary rule uses as its canonical example: `org` never imports an `authn` struct anywhere, only the `authn.user.created` event name and a JSON-shaped payload probe, resilient to that event never being published at all. The tree is `OrgNode{ParentID, Path, Depth}` — adjacency list plus a materialized path, no closure table and no recursive CTE — with a dialect-identity proof test-pinned on both dialects (an all-lowercase-hex-and-hyphen id alphabet containing no `LIKE` metacharacter, so a `path LIKE prefix||'%'` prefix scan returns identical rows on SQLite's case-insensitive and PostgreSQL's case-sensitive `LIKE`), including in the module's own PostgreSQL `integration_test/`. `memberships` is link data (`TenantScoped`, `tenancytest.AssertIsolated`), using an empty-string `parent_id` sentinel rather than `NULL` on `org_nodes` — precedented by `go/config`'s own tenant-scope sentinel — because `NULL` defeats a `UNIQUE(tenant_id, parent_id, name)` index on both engines; roles are deliberately not stored here (`rbac`'s Casbin policy store owns them, keyed by tenant, user and node path). Two structurally-typed, no-import seams carry org's data across the module boundary the same way `config.WithResolver` does: `Scope` (`Path`/`DescendantIDs`/`MemberNodeIDs`, every signature built from stdlib types only, so `rbac` can declare the identical interface and accept `*ScopeService` without either module importing the other) and `FeatureGate` (`IsEnabled`, satisfied structurally by `*config.Service` without an `org`→`config` import edge — wired for real in the reference app's `orgFeatureGate`). Invitations are the messaging module's standing verification-message exception (this file's own Security rules): the raw token is never stored (only its SHA-256 hash), the address is encrypted at rest and blind-indexed under a key that must never double as the encryption key, delivery is rate-limited on two `go/ratelimit` dimensions — per tenant and per recipient, the latter keyed by the blind index, never the plaintext address — and the mail renders in the recipient's negotiated locale, read from `reg.Locales()` at send time rather than captured (nil) inside `Register`. The HTTP surface is `go/org/api/openapi.yaml`, the second module fragment after notes (11 operations: node CRUD plus move, subtree-scoped member listing and removal, invitation create/list/accept), generated by the same pinned oapi-codegen v2.8.0 into a committed `org-server.gen.go`, implemented by `Handler` behind a `var _ api.ServerInterface` compile-time assertion, with the caller identity resolved through a third structurally-typed seam, `SubjectResolver` — unwired or failing, it returns 401 rather than inventing a default user. The reference app is the mandatory first consumer end to end: `cmd/server/server.go` wires `org.NewModule` alongside notes and config, and `cmd/server/org_flow_test.go` drives the real composed HTTP stack through the DSO shape docs/internal/14 requires — a multi-level tree, an invitation accepted, and the roster read back correctly scoped to a subtree, invisible from a sibling one); and `go/saasctl` (the consumer-facing CLI, the roadmap's M1 CLI cell — the tool that shapes the application a consumer actually runs, with all four commands wired: `new` materializes a bootable consumer project from the module's own `go:embed`ed template tree (the reference app's `cmd/server` shape with every demo-specific piece removed — no notes module, no demo tenants, grants or membership store, its host seams left unwired and fail-closed, each named by a doc comment as the owner's first task), over the five legal selections of the switchable `{authn, rbac, org}` universe — positive-selection `--with` with downward-closure validation (selecting `rbac` or `org` without `authn` is an error naming `authn` as implied, `--with=""` a bare config-only app), no `--without`, the full set the default — its two tokens (`__APP_NAME__`, `__SPEED_ROOT__`) substituted at materialization, with each selection's golden go.mod and server.go pinned byte-identical against the embedded assets in the module's own tests (and re-verified byte-identical against real materialized, tidied, built projects in the round's end-to-end proof); `upgrade --version vX.Y.Z [go.mod]` rewrites a consumer go.mod's speed requires to one lockstep version through `golang.org/x/mod/modfile`'s own parser, edited in place and printed back — replace directives, `// indirect` markers, comments and formatting survive byte for byte, the rewrite derived from the file's own require lines, idempotence pinned by tests, the result structurally self-checked from bytes before anything is written back; `db migrate [go.mod]` applies the SQL migrations of exactly the migration-shipping modules a project requires (authn, config, org and rbac, in that order — 14 files in the full selection) to the project's SQLite database through the same `dbkit.MigrationRegistry` the generated app's own startup `Apply` uses, over the same bootstrap environment the app boots from, one transaction per module with every file recorded in `schema_migrations` as it lands — refusing a deployment mode other than standalone before the database is touched, and an existing database file that carries no `schema_migrations` ledger — so CLI-then-boot and boot-only agree; and `config print [go.mod]` renders how a generated project's bootstrap configuration resolves — the five `SPEED_*`/`PORT` variables through the appconfig twin of the generated project's own embedded `cmd/server/config.go` (the twin relationship itself test-pinned), each of the five lines showing its value and provenance, the two key rows `[redacted]` whatever the environment holds, malformed input refused with the very errors the generated app's own bootstrap would refuse with. The round's end-to-end consumer proof, recorded answer by answer in `go/saasctl/AGENTS.md`'s Testing section, ran the full lifecycle against real materialized projects — `new` twice, genuine `go mod tidy` (network) and `go build` in each, boot and smoke of the composed HTTP chain (healthz and `/api/config/public` 200, register 201, wrong password 401 `authn.invalid_credentials`, bad token 401 `authn.token_invalid`, anonymous route 403 `tenancy.tenant_unresolved`, and — the one plan item a skeleton cannot satisfy, recorded as such — a correct-password login answers 403 `authn.tenant_membership_unavailable`, with or without a named `tenant_id`, because the skeleton ships no membership store and authn's contract re-verifies tenant membership through the host-injected `MembershipReader` on every sign-in, a nil reader failing closed; a generated project whose logins must succeed wires its own), then a fresh-file `db migrate`, an idempotent re-run, a boot against the migrated file that no-ops startup `Apply`, an `upgrade` to a newer version and a `config print` under both an env-driven and an env-empty environment. saasctl deliberately never joins the reference-app wiring: its consumers are the generated projects, which are the mandatory-first-consumer proof the reference app's role usually plays — and the scaffold-verify pipeline owns the repeatable CI form of that proof at M4, staying a gated stub until then (its header updated by this round to say so)).762f2e0 (docs(authn): record the M1 status and update the CLAUDE.md census) (docs(repo): reflect the deployment-composition retrofit in the status markers)



On the web side, the `web/` pnpm workspace (its own workspace root — the repository root is the `go.work` workspace, and the two roots deliberately never overlap; layout and rationale in `web/README.md`) currently ships eleven implemented, tested packages: `@speed/tokens`, the design-token tree as dependency-free pure data (the typed `defaultTokens` assembly and `deepMerge`, the copy-on-write override mechanism that rebuilds only touched branches, so hostile `__proto__` keys become inert own properties), whose MUI-parity rows (`breakpoints.values`, `zIndex.values`) are pinned by tests — an MUI major that changes those defaults fails here, in the token package, before any theme adapter ships silently-wrong chrome; `@speed/i18n`, the react-i18next wrapper that is the frontend counterpart of `pkgcore/i18n`'s catalog: `createI18n` negotiates the start language from URL parameter, stored choice, the profile-language extension slot (fed by the M1 user-profile step), navigator languages, then the `zh-CN` default, and `registerNamespace` validates — before any mutation — that every supported language is covered by bundles with identical leaf key sets. The missing-key discipline matches the Go catalog: `fallbackLng: false` plus `load: 'currentOnly'` make cross-language fallback impossible, so a key missing in the loaded language renders as the key itself and fires a visible handler — never another language's text. MUI localization is bridged through the isolated `@speed/i18n/mui-locale` subpath, keeping MUI out of the main entry's import graph; and `@speed/ui-kit`, the first DOM-rendering package: the `createAppTheme` theme factory maps the merged token tree (defaults ← project ← tenant, each a `deepMerge` diff layer) onto an MUI v9 theme — spacing from the token unit, the elevation shadows floored onto MUI's 25-slot shadow ramp, the typography role table from the token scale, the adapter decisions documented and test-pinned in the package — `AppThemeProvider` composes that theme with the MUI locale of the active language and the CssBaseline, and seven controlled components (`PageHeader`, `EmptyState`, `ConfirmDialog`, `FormField`, `FormLayout`, `DataTable`, `FileUploader`) render only state given through props — sorting, selection, pagination and filtering report up through callbacks; a component never fetches or stores data. `FileUploader` keeps that contract too: the queue renders from the host's own `rows` state, each row's status and progress rendered exactly as given, and every pick, cancel, retry and remove reports up through a callback (`onSelectFiles` / `onCancel` / `onRetry` / `onRemove`) — the upload transport, validation and concurrency included, is the host's own code, never package code, and the widget holds no `File` past the event handler that reported it and renders no row that is not in the current `rows` prop. Every built-in string renders from the bilingual `ui-kit` namespace (one `zh-CN.json` plus one `en-US.json` under `src/locales/`, identical key sets, registered once by the host), and the workspace's own `speed/no-literal-text` ESLint rule (implementation and rule tests in `web/eslint-rules/`) refuses user-facing text written inline in package `src`; and `@speed/api-client`, the web side's single home of hand-written HTTP: `createClient` wires an injectable `fetch` (never an implicit environment global — captured at construction, and construction without one fails), a memory-only `AccessTokenStore` (no storage API exists in the package — the access token is a credential an XSS would walk away with from `localStorage`, and the refresh token is the session layer's business, not the store's: the authn API returns it in the token-issuing response bodies and sets no refresh cookie, so `@speed/auth-core` holds it in the session closure; the token is re-read before every attempt, so a retry after a refresh carries the fresh token), a per-request timeout through an internal `AbortController` (caller cancellation rejects the raw `AbortError`, never wrapped, never retried), a silent single-flight 401 refresh that retries the refused request exactly once — any method, outside the transient-retry budget — with the fresh token (`refreshAccessToken?: () => Promise<boolean>` is the seam `@speed/auth-core` fills — `() => session.refresh()`; failure reports `access token refresh failed` and surfaces the 401 as an `auth: true` `ApiError`), conservative transient retries (idempotent methods only — GET/HEAD/OPTIONS — on 429 honouring `Retry-After` plus 502/503/504/network/timeout, exponential full-jitter backoff under the frozen `DEFAULT_RETRY_POLICY` of 3 attempts / 200ms / 4s ceiling), and every failure normalized into one `ApiError` carrying the API envelope's `code`/`traceId`/`params`/`details` — or a reserved `client.*` code (`client.network`, `client.timeout`, `client.protocol`, `client.http.<status>`) when the API layer itself failed — with an `isApiError` type guard and a structured `Reporter` seam (constant message, snake_case attributes; the console sink is a stopgap until the M1 diagnostics round). No tenant header exists anywhere — tenant context travels inside the access token; no i18n resources ship — codes map to bilingual text in consuming packages' catalogs; typed `fetchPublicConfig`/`fetchSystemFeatures` wrappers around `go/config`'s pre-auth `PathPublic`/`PathSystemFeatures` endpoints (hand-kept in sync — no OpenAPI fragment exists for either, per `go/config/AGENTS.md`'s Known limitations) live in the main entry, while the isolated `./react` subpath (mirroring `@speed/i18n`'s `./mui-locale` — the main entry stays dependency-free, `react` a required peer of the subpath only) exports `usePublicConfig`/`useFeature`: one fetch shared per `RequestFn` identity via `useSyncExternalStore`, with `useFeature` composing on that same cache instead of a second round-trip and defaulting to `false` while loading or on error, never throwing; uploads/SSE remain deferred with their reasons in the package README; and `@speed/api-sdk`, the generated typed surface of the merged API document (the notes, authn and notification fragments joined into `build/openapi/speed.yaml` by pinned redocly — notification's joining the merged document in the round its HTTP surface shipped, its operations shipping into the generated package with no in-workspace web consumer yet, its UI surface a later round's work —; the merged document drives the orval frontend leg): `src/index.ts` is orval output — pinned at 8.17.0 and invoked via `pnpm dlx`, so orval is never a workspace dependency; regenerated by the Taskfile `api:gen` task's frontend leg and stamped with a DO-NOT-EDIT header carrying that pinned version — that never touches HTTP itself: every generated call routes through the package's single hand-written seam (`src/runtime.ts` — the host binds `bindRequestFn(createClient(...))` once at bootstrap, last bind wins, and `speedRequest` adapts orval's axios-shaped calls onto the `RequestFn` contract), with orval's extensionless mutator import rewritten to an explicit `.js` by the deterministic post-generation `web/scripts/orval-nodenext-fixup.mjs` (nodenext builds reject extensionless relative imports — TS2835 — so tooling gaps are fixed in tooling, never with a hand-written bridge file in `src/`). Peers are react and @tanstack/react-query v5, the shared-QueryClient contract (docs/internal/21-api-contract.md); generated code has no tenant concept — no tenant header (tenant context travels inside the access token) and bare spec-path query keys, tenant query-key namespacing being an M1 consumer-shell discipline; no i18n resources ship (errors are the spec's typed `{code, params}` envelopes); and the package is compile-consumed in-workspace by `@speed/auth-core` (see below — the first consumer of the authn surface, type-checking and exercising the generated operations through the scripted `bindRequestFn` harness), with runtime end-to-end consumption discharged in form since the auth-ui round — its usage example drives the composed sign-in family over a real `@speed/api-client` bound through this same seam, its scripted fetch answering genuine `Response` objects (the auth-ui census entry below) — the runtime leg the consumer shell (entry below) now carries as host composition: its bootstrap binds this same seam, and its surfaces drive the real client's calls through the composed tree, with the browser-page leg still landing at M4. The workspace's `speed/no-direct-http` ESLint rule (implementation and rule tests in `web/eslint-rules/`) enforces the single-home claim: a direct `fetch` call, a `window.fetch`/`globalThis.fetch` call, a `new XMLHttpRequest`, or an `axios`/`node-fetch` import in any other package's `src` is an error, with this package as the rule's one config-level whitelist.; and `@speed/layout-kit`, the shared app-chrome layer sitting at the same auth-agnostic tier as `@speed/ui-kit`: `AppShell` (a fixed `AppBar`, a responsive nav `Drawer` — permanent at the `md` breakpoint and temporary/overlaid below it, driven off the ambient MUI theme's own breakpoints with no new one introduced — and a `main` content landmark, with host-computed `navItems` carrying their own `selected` state so the package never does path-matching, and a controlled/uncontrolled mobile-drawer-open duality matching `ui-kit`'s own interaction-local carve-out) and `RouteGuard` (gates children on a host-injected `status: 'allowed' | 'denied' | 'pending'` value — never a callback the package calls — firing an `onDenied` side effect exactly once per transition into `denied`, its default denied fallback reusing `ui-kit`'s own `EmptyState variant="noPermission"`, the package's one concrete dependency and never anything auth- or routing-shaped). Depends on `@speed/i18n` and `@speed/ui-kit` only — no `auth-core`, `api-client`, `api-sdk`, or any concrete authentication/routing package — so the same package can serve both the tenant-facing and platform-staff shells the roadmap plans — the tenant-facing half now real (`product-shell` composes `AppShell` as the frame's first package consumer, auth-agnostic opinion intact: `RouteGuard`'s `status` stays host-injected, and the shell's gated-journey suite proves the gate's value arrives by host composition, never from package code), the platform-staff `admin-shell` still a later round; and `@speed/auth-core`, the headless session layer over the generated authn surface, sitting directly above `api-client` and `api-sdk` (its first in-workspace compile consumer): `createAuthSession(store)` in `src/session.ts` turns the generated authn operations — password and SMS login, logout, tenant switch, step-up, refresh — into one observable memory-only state machine, with the access token in the caller-supplied api-client store (re-read on every send) and the refresh token only in the session closure (the authn API returns it in the token-issuing response bodies and sets no refresh cookie — the only HttpOnly cookie authn ever sets is the social-binding pre-auth one — so nothing here writes storage, and a reload starts anonymous). User operations reject with the raw `ApiError` and change nothing on failure; a token-issuing 2xx violating the contract rejects with `client.protocol` before any state change; `refresh()` is the silent path (resolving `false` signs the session out locally, transport failures rethrow raw with the held tokens untouched), single-flight per held refresh token (the flight keys on the token, so a call after a switch or step-up, which keep the held token, still shares it), and a generation guard makes a completed logout win over a refresh that resolves after it — a losing refresh applies nothing over the winner's state but the rotated refresh token itself, adopted only when the winner kept the held token. The refresh request is credential-less by declaration (the generated mutator's `omitAccessToken`) and never touches the token store. The hooks in `src/hooks.ts` — `useAuthState`, `useCurrentTenant`, `usePermission(domain, permission)` — read one session the host attaches with `attachSession` (last bind wins, the previous session's transitions stop reaching the hooks) and never drive it: login and logout are called from event handlers, and every hook fails closed (anonymous snapshot, null tenant, `false`) before attach and after logout. Permission checks are set lookup only over the host-attached per-domain lists — `setPermissionSet('tenant' | 'system', string[] | null)`, with survival rules applied when a principal change commits (silent refresh and step-up keep both lists, a tenant switch drops the tenant list and keeps the system one, a login — even by the same user in the same tenant — or a logout clears both) — never fetched or evaluated here: the shipped `/api/v1/authn/me` returns identity only (`AuthnPrincipal` carries no permissions) and rbac mounts no HTTP routes, so the /me-derived lists are the host's to attach (see `docs/internal/12-frontend.md`'s mechanism notes). Peers are react only — session hooks are subscription-based, and the shared-QueryClient contract stays with api-sdk's generated hooks; the package's suite drives every operation through the scripted `bindRequestFn` harness (the same seam a host's real client binds), while `src/usage-example.test.tsx` compiles and runs the README's documented flow so it cannot drift from the API; and `@speed/auth-ui`, the sign-in component family over an auth-core session, sitting at the same tier as `ui-kit` and `layout-kit` but deliberately auth-aware: `SignInScreen` assembles the channels behind a channel tab strip (password first by default, and switching unmounts the previous form, so its half-typed state and whole-attempt error reset with it), and each channel form drives one slice of the session contract as a controlled react-hook-form flow over `ui-kit`'s `FormLayout`/`FormField`: `PasswordSignInForm`'s single identifier field (email or phone — the backend decides which), `SMSSignInForm`'s two-step phone→code flow (whose request step renders the codes the server answers — `authn.invalid_phone` when the number has no E.164 form, `authn.rate_limited` when the send policy trips), and `RegisterForm`, which never signs in — `register` is not a session operation, so the created user goes to the host's `onRegistered` callback (typed with the generated `AuthnUser`, which is why `@speed/api-sdk` is a dependency here, type-only) or, without one, a success panel — splitting its identifier on the `'@'` heuristic into the spec's separated email/phone shape, trimming the optional display name, and reading the locale at submit time. Social sign-in renders one outlined button per configured provider (`SocialSignInSection`) whose click asks the session for that channel's authorization URL — a pure request reported upward through `onAuthorizeUrl`, never a navigation the package performs — and completes at the host's callback route through `SocialCallbackHandler`, keyed on the returned `(code, state)` pair so StrictMode's double effect invocation starts exactly one exchange, its in-flight state a `role="status"` notice and a failed exchange retryable for the same pair. `SignOutButton` drives `session.logout()` from a click — a failed logout renders the answer's code text (session-lifecycle answers included) and stays retryable; a successful one is deliberately quiet, the host observing the snapshot flip through its own hooks — and `SessionEndedScreen` is the pure placeholder for a view whose authenticated snapshot just turned anonymous: `ui-kit`'s `EmptyState` in the `noPermission` variant with every text slot overridden from the bundle, no session prop, no hooks, no network. Every component is controlled — the session arrives as a prop, nothing consumes the auth-core hooks, attaches or reads session state, navigates, or touches the network (every request is a session operation through the shared seam), and a successful sign-in fires the host's `onSignedIn` exactly once; the host gate — `useAuthState` plus the host's own transition memory, the SessionGate pattern this package's suite journeys drive — decides between the sign-in surface, the app and the session-ended screen — the very decision the product-shell round packaged as the shipped `ProductShell` three-branch view machine (entry below) — while the router-level `RouteGuard` wired behind real permission fetches has now landed with the consumer shell (entry below), where the notes surface's gate status derives from the list query the server answers — a refused read failing closed to the denied state — and the browser page rendering it still waits on M4. Every built-in string renders from the bilingual `auth-ui` namespace (59 keys per language under `src/locales/`, registered by the host alongside `ui-kit`'s), and every submit-path failure resolves through the reachable-error whitelist — 27 authn/session-lifecycle/client codes with an `unknown` fallback that never shows a raw key. The package's suite drives component behaviour through the scripted `bindRequestFn` harness and drives journeys through a real `@speed/api-client` over a scripted fetch answering genuine `Response` objects — `src/usage-example.test.tsx` compiles and executes the README quick start (a password sign-in, a refused and silently refreshed protected call, a server-side session death whose refused refresh converges to the session-ended screen, a sign-in-again turn and a `switchLanguage` leg, six requests pinned in order) — discharging in form the runtime end-to-end consumption of the authn surface the api-sdk entry above deferred to the shells, the browser-and-real-server leg still theirs. `@speed/account-ui`, the account-management family that completes that same surface (the signed-in account page the auth-ui round's deferral pointed at): `SessionsSection` renders the server's session list — the current session marked from the server's own `is_current`, one row revoked in place (icon-only button, no second confirmation), the rest behind a double-confirmed danger dialog whose `revoked_count` answer surfaces in a success notice, revoked rows staying greyed and the current session never revocable from the list — `LoginHistorySection` renders the newest twenty attempts (its method and failure-reason tokens pass through `t()` only when on the component's known-token lists; session `amr` values render as opaque references, deliberately untranslated), `SocialBindingsSection` lists the bound identities with a row-end unbind behind the danger `ConfirmDialog` — an unbind the server refuses with `authn.last_login_method` stays on the page with its code text — and an add area asking the session for each configured channel's authorization URL, reported upward through `onAuthorizeUrl`, never a navigation, and `BindingCallbackHandler` completes a binding at the host's callback route, dispatching on the answer shape: binding-shaped answers invalidate the identities list through the exported query-key builder and fire `onBound` exactly once; login-shaped answers (the exchange turned into a sign-in) render the signed-elsewhere panel and fire nothing. `MfaSection` discovers an active factor through the 403 `authn.step_up_required` the server answers — the spec ships no factor-status and no disable operation, so the section never declares enabled or disabled — drives the challenge through `session.verifyStepUp` (the elevation settling in a fresh access token whose `amr` carries the factor, never outliving that token), replaces an authenticator or regenerates recovery codes behind that gate, and shows recovery codes exactly once, never cached or re-fetchable. Its reads go through the generated react-query hooks of `@speed/api-sdk` over the host's QueryClient — the first package to render generated hooks into a component tree, the deliberate counterpart of auth-ui's zero-hook forms (account lists are cacheable state that invalidates after their own mutations; a sign-in answer is one-shot, not a cache) — invalidating through the exported query-key builders and driving the binding exchange through a plain generated call, which is why `@speed/api-sdk` is a runtime dependency here (auth-ui's is type-only) and why the host tree gains a `QueryClientProvider` beyond the auth-ui tree. The session arrives as a prop exactly where a session operation exists that the generated surface cannot express — `session.socialAuthorizeUrl` in the add area, `session.verifyStepUp` in the challenge dialog — while the two read-only sections take no props at all. The provider vocabulary is its own definition copied to match auth-ui's (same-layer packages never import each other), the authn spec the shared source of truth that keeps both copies in step. Every built-in string renders from the bilingual `account-ui` namespace (106 keys per language under `src/locales/`), registered by the host alongside `ui-kit`'s (the confirm-again label and the `EmptyState` texts are ui-kit keys), with a 21-code reachable whitelist — the 8 codes whose failure context matches the sign-in surface reusing the auth-ui bundle's text verbatim — and an `errors.unknown` fallback that never shows a raw key; MFA enrollment is manual-entry (no QR or clipboard dependency), by recorded decision. Every surface drives through the same real-client rig, and `src/usage-example.test.tsx` compiles and executes the README quick start — eighteen requests pinned in order with authorization headers asserted per request (credential-less turn zero, `access-1` until the step-up challenge rotates it to `access-2`): a social-exchange sign-in, an iPad session revoked alone, the other two behind the double-confirmed revoke-others, the refused GitHub unbind, a step-up-gated authenticator replacement with its show-once recovery codes, a WeChat binding walked to the host's callback route and back, and a `switchLanguage` leg — the in-form runtime consumer proof of the generated half of the authn frontend surface, which the consumer shell (entry below) composes through its own bootstrap over the shared seam, the browser-page leg still landing at M4. `@speed/tenancy-ui` and `@speed/product-shell` complete the roadmap's M1 web-package list, both landing in the product-shell round. tenancy-ui is the tenant-switch affordance: one controlled `TenantSwitcher` — the session, the tenant list and the current tenant all host-supplied props, every commit reported through `onSwitched`, nothing here consuming the auth-core hooks, reading storage, navigating or touching the network directly — whose switch answers resolve through a nine-code reachable-error whitelist (the one application-level code `authn.tenant_membership_required`, five session-lifecycle codes and the three `client.*` transport codes) with an `errors.unknown` fallback and twelve strings per language under its own `tenancy-ui` namespace, the error texts verbatim copies of auth-ui's for the codes the two packages share; its usage example is the `authn_switchTenant` operation's in-form consumer proof — one login and three switch attempts over a real api-client, the third pinned for its bearer token and `{tenant_id}` body. product-shell is the tenant-facing assembly shell the roadmap's M1 row names: `ProductShell` and its props type — the package's only two exports — compose the `AppShell` frame, the auth-ui sign-in family and the auth-core session hooks into one three-branch view machine (signed out showing the sign-in surface, signed in with a tenant showing the frame with the tenant switcher, a dead session converging to the session-ended screen), with zero text of its own — no namespace, no locale files, no reachable-error whitelist — and never consuming `RouteGuard`, `usePermission` or `setPermissionSet`; its gated-journey suite proves the route-gate shape as host composition — a stand-in host loading role lists and gating children on them, the same shape the consumer shell's notes surface (entry below) realizes behind a real permission fetch, and its usage-example suite runs the whole journey — sign-in, frame, tenant switch, sign-out, session-ended, sign-in again — through the four-namespace bootstrap. `web/.nvmrc` (Node 24) and the `packageManager` field of `web/package.json` (pnpm 11.1.2) are the single version sources the shared CI action reads.

Alongside the eleven packages sits the consumer shell: `examples/reference-app/web`, the reference app's web host living outside `web/` exactly where a delivered consumer project lives — an external member of the `web/` pnpm workspace (`web/pnpm-workspace.yaml` lists the `../examples/reference-app/web` path; one shared frozen lockfile), `private: true` and deliberately never versioned, so it never joins the changesets fixed group (which holds the versioned `@speed` packages only), and the app directory's own `pnpm-workspace.yaml` carries the pnpm-11 settings key and no packages list — which is why the npm-package-ci leg that runs inside the app directory sees a one-project workspace. It is the browser-leg consumer proof of the whole frontend surface at once, on this round's two-layer standard: above each package's in-form usage-example journeys (real `@speed/api-client` over scripted fetch answering genuine `Response` objects) the shell's own suites pin the composed answers — `src/codes-alignment.test.ts` aligns the four surfaces' reachable-error whitelists (the auth-ui sign-in/session family, the account-ui signed-in family, the tenancy-ui switch and the notes create surface, each deep-imported from where it lives) against a hand-maintained, source-cited enumeration of 34 server codes those surfaces can be answered with, in both directions, so a server code the whitelists forgot and a whitelist entry no server emits here both fail with the code and the surface named. `src/main.tsx`'s `bootstrapReferenceApp` is the first real composition of every host contract at once: six namespaces registered (the five package families plus the app's own `reference-app` bundle), a memory-only auth-core session attached, the app's single client built over the environment's own fetch with the session's silent refresh as the 401-refresh leg and bound once into the api-sdk seam, and the product-shell view machine rendered over hash-routed surfaces in the `AppShell` frame — the router-level gate the auth-ui census defers to this shell is real here: the notes list query is the permission fetch itself (the server's rbac layer answers 403 `rbac.permission_denied` for a caller without `notes:read`), so the notes view's `RouteGuard` derives its status from that query, fails closed on the refusal, and reads through a tenant-namespaced query key over the generated bare key. Server side, the demo identity layer stands in for the pieces a real consumer wires itself: the demo subject resolver reads `X-Demo-User` first (a verified authn `Principal` second, otherwise denial), `SPEED_DEMO_USERS_PASSWORD` seeds three real accounts — `demo-owner@example.com`, `demo-reader@example.com`, `demo-acme-only@example.com` — through the real composed register route, with memberships and grants attached under each tenant's own context (`demo-owner` everywhere, `demo-reader` everywhere behind the custom `note-reader` role, `demo-acme-only` in `tenant-acme` alone), and Go tests pin the composed behaviours. The browser page mounting all of it, served by the app itself, is the M4 html-runner/e2e work — until then this package compiles, typechecks and renders under test harnesses, which is the whole of the shipped browser story.

This matters for how you work here: **`go build github.com/vislake/speed/go/...`, `go vet`, `golangci-lint run ./...`, and `go test ./... -race` genuinely run and pass today for `go/pkgcore`, `go/dbkit`, `go/tenancy`, `go/observability`, `go/config`, `go/jobs`, `go/storage`, `go/notification`, `go/ratelimit`, `go/authn`, `go/rbac`, `go/org`, and `go/saasctl`** (run them from inside each module's own directory for the vet/lint/test forms — the repo root is a `go.work` workspace, not a module, so a bare `./...` from the root only works with the full import-path form). The web-side mirror holds for the eleven packages above plus the consumer shell riding the same workspace as its external member: **`pnpm -r lint`, `pnpm -r typecheck`, `pnpm -r test`, and `pnpm -r build` genuinely run and pass today for `web/packages/tokens`, `web/packages/i18n`, `web/packages/ui-kit`, `web/packages/api-client`, `web/packages/api-sdk`, `web/packages/layout-kit`, `web/packages/auth-core`, `web/packages/auth-ui`, `web/packages/tenancy-ui`, `web/packages/product-shell`, `web/packages/account-ui`, and `examples/reference-app/web`** (run from inside `web/`, the pnpm workspace root, whose workspace-member list reaches the app directory). `go/dbkit` and `go/tenancy` have a real integration tier (`go test -tags=integration ./...`) that starts real PostgreSQL via testcontainers, `go/jobs` and `go/pkgcore` have the same tier backed by a real Redis (pkgcore additionally exercising its MinIO/S3-backed `ObjectStore` implementation), and `go/config` and `go/rbac` each carry both legs — a PostgreSQL tier and a Redis tier in their own integration_test/ (the Redis leg standing in for a second distributed replica converging through the bus; rbac's PostgreSQL leg additionally discharges the dual-dialect migration rule its unit tier cannot, and its Redis leg runs both replicas on a one-hour cache lifetime so no convergence it proves can be explained by the anti-loss TTL) — and `go/org` carries a PostgreSQL tier that re-runs its materialized-path dialect-identity proof and its three tenant-scoped repositories' `AssertIsolated` suite against a real server; `go/authn` carries a PostgreSQL tier of its own (its `integration_test/` directory, run as `go test -tags=integration ./integration_test/...` from the module dir) that applies its dual-dialect migrations from zero, re-runs the `AssertNotTenantScoped` isolation suite over its eight identity-domain tables and its one tenant-domain table, proves the blind-index unique constraint is enforced identically on both dialects where their collation genuinely differs, and re-proves the refresh-rotation single-winner property against real PostgreSQL — a property its unit-tier SQLite proof could pass even with a broken compare-and-swap, which is exactly why it earns a second real-database proof — `go/storage` carries a two-leg tier of its own — a PostgreSQL leg applying its postgres migration set from zero against a real server and re-running both its repositories' `AssertIsolated` suite there, and a MinIO/S3 leg driving the module's full lifecycle through the S3-backed `ObjectStore` implementation against a real MinIO container, the store's physical contents asserted through a raw minio client so nothing the module believes about its writes goes unchecked; `go/notification` carries a two-leg tier of its own — a PostgreSQL leg applying its migration set from zero against a real server and re-running its tenant-scoped repositories' `AssertIsolated` suite there, plus the consent-ledger single-winner verify (a compare-and-swap SQLite's serialization could never put at risk), and a Redis leg proving the cross-replica inbox fan-out — two booted modules over one shared Redis client and database, a delivery written on one replica announcing its inbox row on the bus the other replica reads — Docker must be running for any of these forms, though not for the plain unit-test form; `go/observability` has no such tier by design, not by gap — its OTLP path for the distributed deployment mode is proven in-process against a real generated gRPC collector server instead (see `go/observability/AGENTS.md`'s Testing section), needing no Docker. Do not assume this claim is stale without checking; conversely, do not assume any *other* module has real code behind it just because its directory exists — check for more than a `doc.go` stub before relying on one. `Taskfile.yml` exists and its underlying commands work, but the `task` CLI binary itself is not installed in this environment as of this writing — run the commands it wraps directly (`go test ./...`, `go vet ./...`, `golangci-lint run ./...`) rather than assuming `task test` works until you've confirmed `task` is on PATH. CI mirrors this status with the same honesty: `.github/workflows/pr-check.yml` runs on every pull request — the same thirteen-module matrix as the paragraph above (per-module `golangci-lint`, `go vet`, unit tests under `-race`, a workspace-context build and a `GOWORK=off` standalone build, via the reusable `go-module-ci` workflow), a per-package npm matrix for the eleven web packages above plus the consumer shell (`examples/reference-app/web`) — twelve rows of the same legs (a `pnpm install --frozen-lockfile` from `web/`, then lint / strict typecheck / unit tests / build per package or app, via the reusable `npm-package-ci` workflow, with Node and pnpm from `setup-node-env`'s single version sources and the pnpm store cached on the lockfile hash) — plus a `repo-checks` job that runs `tools/scan_cjk.py` over the whole tree, the six architecture-discipline semgrep rules under `tools/semgrep_rules/` over `go/` `examples/` `tools/` (each rule's planted-violation fixtures excluded at the CLI level; rule catalog and honest limitations in `tools/README.md`), a workspace-wide `go build github.com/vislake/speed/go/...`, a `go work sync` drift gate, `tools/check_toolchain.py` (the `.mise.toml` tool-mirror drift gate), and the workspace eslint rules' own vitest unit tests (`web/eslint-rules/` — `no-literal-text` and `no-direct-http` — once per PR from the `web/` root: the rules live outside every package, so no per-package suite would exercise them). Three further pipelines are live on demand: `pr-full.yml` on PRs labeled `full-ci` (the same thirteen-module matrix through the same reusable, the Docker-backed integration tiers of dbkit/tenancy/jobs/pkgcore/config/rbac/org/authn/storage/notification, and the reference-app lint leg and unit suite, the latter now including authn's composed-HTTP e2e (`authn_e2e_test.go`) alongside org's (`org_flow_test.go`), storage's (`storage_flow_test.go`) and notification's (`notification_flow_test.go`)), `docs-check.yml` on PRs touching docs or i18n resources, via path filtering (runs `tools/check_i18n_keys.py` and the docs-site structural check `tools/check_docs_site.py`), and `api-contract.yml` on PRs touching the API-contract toolchain side of any of the five module fragments — notes' spec fragment and generator config under `examples/reference-app/internal/notes/api/`, org's under `go/org/api/`, authn's under `go/authn/api/`, storage's under `go/storage/api/`, notification's under `go/notification/api/`, the Taskfile `api:gen`/`api:merge` tasks, `redocly.yaml`, `build/openapi/`, or api-contract.yml itself (regenerates all five fragments' backend interfaces with the same pinned oapi-codegen v2.8.0 the Taskfile uses — all five fragments' compile enforcement arriving transitively through the reference-app build, which imports every one of the five modules — authn included, so its handler-plus-generated-interface pair compiles there too — with authn additionally keeping its own regeneration-and-build leg in the workflow per the header's rationale (its compile enforcement must not wait on full-ci the way the app's suite does) — plus the frontend SDK regenerated by pinned orval 8.17.0 from the merged document into `@speed/api-sdk` (the merge: pinned redocly joining the notes, authn and notification fragments into the committed `build/openapi/speed.yaml`, linted against `redocly.yaml`'s naming rules — org's and storage's fragments are regenerated and compile-checked but not part of the merged document, org's because it predates the merge machinery and storage's because it ships its backend leg only — plus the deterministic `web/scripts/orval-nodenext-fixup.mjs` rewrite), failing via seven `git diff --exit-code` gates when any committed artifact is not what its spec generates, then `go build`ing the reference app so a spec whose generated interface outgrew its handler — any fragment's — cannot compile). `security.yml` is live from the same implementing round that ships the semgrep ruleset, the license scanner and the gitleaks config: it runs on every PR plus a daily 05:37 UTC schedule — pnpm audit against the web lockfile, a checksum-pinned gitleaks secret scan (`.gitleaks.toml` extends the default rules with exactly one allowlist, for observability's redaction-test fixtures), a CodeQL Go + TypeScript/JavaScript analysis (the Go extractor covers the go.work workspace natively), and the license scan; its header's TOOL POSTURE / DEFERRED sections record what it does not yet run and why — govulncheck cannot pass on the current tree (clearing its stdlib findings needs a go-directive raise off the 1.25.0 toolchain line and its module findings need test-support dependency bumps by the owning modules, both still open), and trivy has no images to scan since nothing ships a Dockerfile yet. The remaining three files in `.github/workflows/` are gated stubs as of this writing (e2e, nightly, scaffold-verify): their jobs fail at a stub-guard step until their implementing rounds land, and none of them triggers on pull requests. A fourth gated stub sits one level down — `.github/workflows/reusable/docker-build.yml`, a wiring-ready reusable that nothing calls and whose guard step fails any invocation, waiting on the first image-producing app. `release.yml` is live on demand too — the lockstep-release pipeline, dispatched manually with a version number (the workflow_dispatch `version` input, release-version form required): it validates the version against `^v[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z-]+(\.[0-9A-Za-z-]+)*)?$`, runs the release coordinator (`tools/release/lockstep-release.py`) in its default offline-verification mode — the full one-version plan derived at runtime: every go.work `go/` module with its `go/<module>/<version>` tag, every `web/packages/*` package with the version the changesets fixed group bumps it to (`web/.changeset/config.json`), exit 0 only when the plan is consistent — and then the coordinator's own `--self-test` suite. No publish credential is wired anywhere (`permissions: contents: read`), so nothing in `release.yml` can publish; real publishing is scheduled for the v1.0 release at M4. So a rule in this file that claims CI enforces it is only as real as the workflow behind it, which today means pr-check on every PR, pr-full on `full-ci`-labeled PRs, docs-check on doc/i18n-touching PRs, api-contract on spec-toolchain-touching PRs, security on every PR plus a daily schedule, and release on a manual dispatch.


## Language Rule (read this first)

- **`docs/internal/**` is written in Chinese.** It holds internal design discussion and decision rationale.
- **Everything else is English**: code comments, godoc/TSDoc, module docs, per-module `AGENTS.md` files, package READMEs, the root `README.md`, `.claude/skills/**`, and commit messages.
- User-facing product text is bilingual (zh-CN + en-US) and lives in i18n resources, never in code.

CI fails on CJK characters found outside `docs/internal/` (i18n resources and `docs/site/` localization directories excepted).

## Planned Commands

Defined in `docs/internal/19-dev-workflow.md`. Task runner is Taskfile; toolchain versions are pinned in the root `.mise.toml`, mirrored from each tool's authoritative source (`go.work`, `web/.nvmrc`, `web/package.json`, the Taskfile header, setup-go-env's `GOLANGCI_VERSION`) with a CI drift gate (`tools/check_toolchain.py`) proving the mirrors cannot drift.

```
task setup        # install toolchain, fetch deps, initialize the database
task dev          # run backend + frontend in standalone deployment mode with hot reload
task test         # test the affected modules
task test:full    # full matrix (dual deployment mode x dual dialect)
task lint         # lint everything
task api:gen      # merge specs, generate backend interfaces and frontend sdk
task docs:serve   # preview the docs site locally
task new:module   # scaffold a new module (prints the registration checklist: go.work use entry, CI matrix, roadmap rows)
task release:plan # verify the lockstep release plan for one version, offline
```

`task dev` must work in **standalone deployment mode** — single process, SQLite, zero external dependencies. Local development does not require `docker compose`.

## Architecture

### Shape

A **modular monolith distributed as libraries**. This is the single most important thing to internalize: speed is not an application, it is independently released Go modules and npm packages that business projects pull in via `go get` / `npm install`. They compile into one binary and call each other in-process — no service discovery, no Kubernetes-shaped infrastructure. Only a minimal starter skeleton is generated by CLI and freely editable by consumers.

Consequences that drive most design decisions:

- Every exported signature change propagates to every delivered project. Treat public API as frozen unless you are intentionally shipping a breaking change.
- Every dependency added here lands in someone else's `go.sum` or bundle. Adding one needs justification in the pull request.
- Implementation details belong under `internal/` so consumers cannot import them.

### Module dependency direction

Dependencies flow strictly bottom-up (full graph in `docs/internal/01-architecture.md`):

```
pkgcore -> dbkit / observability / ratelimit -> tenancy -> config / jobs -> storage / notification
        -> authn / rbac / org / metering -> billing / ai-gateway / sharing / integration
        -> compliance -> admin
```

### Module wiring

Every module implements `pkgcore.Module` and registers everything through a single `Register(reg *Registry)` call — routes, config schema, feature flags, permissions, job handlers, notification types, events, audit actions. The `Registry` struct exists so that adding a new cross-cutting mechanism does not change the `Module` interface, which under lockstep versioning would break every module at once.

### Deployment mode and implementation composition

These are **two orthogonal axes**, and conflating them is a design error the design docs used to make (`docs/internal/03-deployment-modes.md` is the authority):

- **Deployment mode** — how many replicas this runs as, and therefore which implementations are *permissible*.
- **Implementation composition** — which implementation each infrastructure seam actually uses.

Every infrastructure dependency is an interface in `pkgcore` with **N implementations** (N ≥ 1, not fixed at two): `EventBus` has an in-process channel and a Redis Streams implementation, with NATS a candidate; `Mailer` has console and SMTP; and so on. **The deployment mode does not select an implementation — it only constrains one.** Each implementation declares its capabilities (`MultiReplicaSafe`, `SurvivesRestart`, `Stateless` — the third added later so the restart-banner warning skips stateless implementations like the console mailer), each deployment mode declares what it requires, and assembly fails at startup when the composition cannot run in the declared mode, naming the seam and the implementation.

The constraint is one-directional: a multi-replica deployment excludes in-process implementations, while a single-process deployment excludes nothing — a single binary talking to real PostgreSQL, real Stripe and real SMTP is the ordinary shape of a small-customer production install, not a misuse. Whether an implementation is a fake or the real thing is an environment-and-credentials question the *application assembler* answers; the framework ships no "production" or "test" preset and enforces no such policy.

**Status:** shipped as of the deployment-composition retrofit round (2026-09) — the registry, presets, capability declarations and assembly validation described above are landed code in `go/pkgcore` (`seam_registry.go`'s `SeamRegistry[T]`/`Registration[T]` mirroring `database/sql`'s driver registration, `capability.go`'s `MultiReplicaSafe`/`SurvivesRestart`/`Stateless` bits (`Stateless` added right after the round, exempting stateless implementations such as the console mailer from a `SurvivesRestart` banner with nothing to warn about for them), `preset.go`'s `PresetStandalone`/`PresetDistributed` maps, and `builtin_implementations.go`'s eight pre-registered built-ins over `EventBusRegistry`/`KVStoreRegistry`/`MailerRegistry`/`ObjectStoreRegistry`) and in `go/observability`: `NewKernel` takes options, not a mode (`WithDeploymentMode` declares the topology, `WithPreset` swaps the whole map, and `WithEventBus`/`WithKVStore`/`WithMailer`/`WithObjectStore` inject one implementation plus its capability bits); `Bootstrap` resolves every seam and fails a composition that cannot run in the declared mode with `ErrCapabilityUnsatisfied`, naming the seam, implementation, missing capability and mode (a startup banner covers the `SurvivesRestart`-only miss); `observability.Init(ctx, opts...)` likewise takes no mode — the `WithOTLPEndpoint` option alone decides the exporter. Mode values and chosen implementations stay out of business code — the discipline table's semgrep rule enforces that as a residual-risk detector. `docs/internal/03-deployment-modes.md`'s implementation-status section records the landed shape and what remains (the preset layer's per-implementation parameter channel and its config-file override tier, unshipped; Queue-seam registry-ization deferred to go/jobs' own round; no distributed-mode or second-dialect run yet).

A side benefit worth knowing: the in-process implementations double as test doubles, so most unit tests need no testcontainers.

### Multi-tenancy

Shared database with `tenant_id` isolation, guarded three ways: a GORM plugin that auto-injects the filter, a mandatory generic `dbkit.Repository[T]` base, and PostgreSQL RLS in distributed deployment mode. Tables fall into four data domains (tenant / identity / platform / link) — `users` is deliberately **not** tenant-scoped, since a person can belong to several tenants; `memberships` bridges them.

### API contract

**Spec-first, non-negotiable order**: edit `api/openapi.yaml` → `task api:gen` → compilation failures reveal every handler to fix → implement → update frontend → commit together. The generated Go server interface participates in compilation, so drift between spec and implementation cannot compile. Both halves of that loop are implemented today for the reference app's notes module: its handler implements the generated `api.ServerInterface` (compile-time assertion at the bottom of `internal/notes/handler.go`), and one fragment — `examples/reference-app/internal/notes/api/openapi.yaml` — drives both regenerations of the Taskfile `api:gen` task: the backend interface via pinned oapi-codegen, and the frontend `@speed/api-sdk` package via pinned orval, whose generated hooks call the `@speed/api-client` runtime through a single hand-written binding seam (`src/runtime.ts`'s `bindRequestFn`, bound by the host at bootstrap), never HTTP of their own. Artifact consistency on both halves, plus handler compilation, is enforced by the `api-contract.yml` pipeline (see Repository Status for exactly what it runs).

### Versioning

**Lockstep**: all Go modules and npm packages share one version number and release together; only same-version combinations are supported. This removes the compatibility matrix entirely, at the cost of consumers upgrading everything at once (`saasctl upgrade` handles the rewrite).

## Architecture Discipline

Every rule below is enforced by code review, and by CI where the tooling for it exists (Repository Status above says what CI genuinely runs today) — **these are not style suggestions**. Code that violates any of them should not be merged. The reasoning behind each lives in `docs/internal/`; the detailed how-to lives in `.claude/skills/`.

### Dependencies and module boundaries

- **Do not let `rbac` depend on `authn`.** Authorization only knows `Subject{TenantID, UserID}`; the authenticating side assembles the Subject and calls authorization.
- **Do not import another business module's structs for database relations.** Use ID references plus domain events — `authn` publishes `UserCreated`, `org` subscribes to create the default workspace; `org` never imports `authn.User`.
- **Do not import concrete infrastructure implementations in business code.** Depend on the `pkgcore` interfaces (`KVStore`, `EventBus`, `ObjectStore`, `Mailer`), never on `go-redis`, an S3 SDK, and so on.
- **Do not expose a capability on an interface that only one implementation can satisfy.** Interfaces are designed against the weakest of that seam's registered implementations — an anchor that moves as implementations are added, not a fixed "the standalone one".
- **Do not put a backend implementation in the same package as the interface it implements.** The rule above governs source code; this one governs packaging, and neither substitutes for the other — Go resolves dependencies per *package*, not per symbol, so a module can obey "business code depends only on interfaces" and still hand every consumer the backend SDK, purely because the implementation sits in the package they import. Which backends a binary contains is the assembling application's decision: an application certain it only wants SQLite imports one dialect package and carries one dialect's dependencies; an application that wants to switch at run time imports both and carries both. Bundling everything is not a trade-off in exchange for "one binary runs any composition" — that property survives splitting, since whoever wants it imports every implementation and pays exactly the same. `database/sql` is the model: import two drivers and switch by DSN, import one and support one. Rationale, the measurement method, and the accepted cost (a missing registration becomes a startup error rather than a compile error, so the message must name the import that fixes it) are in `docs/internal/03-deployment-modes.md`.
- **Prefer a subpackage over a new module when splitting an implementation out.** A subpackage already isolates completely — the effect reaches `go.mod`, `go.sum` and minimal version selection, not just the import graph (measured: a consumer importing only the `pkgcore` root package has no `koanf` entry anywhere, `koanf` being used solely by `pkgcore/config`). Modules are release units, divided by domain cohesion, and must not be bent to serve a packaging need: under lockstep versioning each one costs a `go.work` entry, a CI matrix row, an `AGENTS.md`, a changesets fixed-group entry and a version tag, none of which a subpackage requires. A module is warranted only for an implementation needing its own release cadence, or one consumers would use without the parent — lockstep rules out both.
- **Adding a built-in implementation requires measuring what it costs consumers.** Create a throwaway module, `require` the target, `go mod tidy` under `GOWORK=off`, count `// indirect` entries, and put the number in the PR. The cost accumulates upward through the dependency graph, so the bar rises the closer a module sits to `pkgcore`. Existing violations are tracked in issue #1; `go/ratelimit` is the illustration — zero third-party imports in its own non-test code, 24 indirect dependencies for its consumers, S3 SDK among them.

### API contract

- **Do not hand-write backend API calls.** The frontend may only use the generated hooks from `@speed/api-sdk`; `fetch` / `axios` are permitted only inside `@speed/api-client`.
- **Do not change the implementation before the spec.** Spec first, always.
- **Do not edit any file in `@speed/api-sdk`.** It is generated and overwritten wholesale on the next release.

### Multi-tenant isolation

- **Do not hold a `*gorm.DB` and write queries yourself.** Business repositories for tenant-owned data must embed `dbkit.Repository[T]`. Identity and platform data (see the data-domain table in `docs/internal/04-data-and-tenancy.md`) can't use it — the generic constraint requires `TenantScoped`, which those domains must *not* implement — so they use `dbkit.Open()`'s plain `*gorm.DB` directly; see `go/dbkit/AGENTS.md`'s "Known limitations" for why that's safe rather than a loophole.
- **Do not use `db.Table` / `db.Model` / `db.Raw` to work around the Repository.** The three bypass entry points are checked in CI today by the semgrep rule `tools/semgrep_rules/raw-gorm-bypass.yml` (repo-checks, every PR), whose header names the allowlisted sites (dbkit's own internals, `go/jobs/store.go`'s platform-data queries) and the residual gap it deliberately leaves (a workaround through another `*gorm.DB` method, e.g. `Exec` with hand-written SQL) — code review owns the residue.
- **Do not hand-write `WHERE tenant_id = ?`.** Tenant filtering is injected by the GORM plugin and the Repository; writing it by hand means you are bypassing the guard.
- **Do not accept a caller-supplied `tenant_id` at the API layer.** The tenant comes from the access token claims, never from request parameters, headers or bodies.
- Every new repository **must** run `tenancytest.AssertIsolated` (tenant data) or `AssertNotTenantScoped` (identity and platform data).
- **Do not reach for raw SQL to escape tenant filtering.** The only legitimate cross-tenant path is `pkgcore.WithSystemContext` (the raw primitive, implemented today) — the `tenancy` module, once built, wraps it with audit publishing for business-module use. Either way, it is restricted to `admin`, `compliance`, `jobs` and `authn`, and audited on every use.
- **Do not create cross-module foreign keys.** Store IDs only — cross-module FKs make independently released migrations and cascading deletes unmanageable.

### Asynchronous work

- **Do not assume a worker has tenant context.** Rebuild `tenantctx` explicitly inside the job, or the Repository will fail closed.
- **Do not put business compensation in the queue layer.** The queue offers an `OnFailure` hook; refunding credits and similar compensation belongs to the business module.
- Long-running operations **must** go through the `jobs` queue and report progress; never run them synchronously inside an HTTP request.

### Deployment modes

- **Do not branch on `if mode == "standalone"` in business logic.** Deployment-mode differences belong exclusively to kernel wiring.
- Any new infrastructure dependency **must** ship at least one implementation with zero external dependencies (so it stays usable in a single-process composition and as a test double), and every implementation **must** declare its capabilities and pass that seam's contract test suite.

### Logging

- **Use structured logging only.** Take the logger from the context (`obs.FromContext(ctx)`) so trace and tenant correlation survives.
- **Do not build log messages by concatenation or `fmt.Sprintf`.** The message is a constant string; everything variable goes into key-value attributes.
- **Do not use `fmt.Println`, `log.Printf`, or `console.log`.** Frontend diagnostics go through the reporter in `@speed/api-client`.
- Attribute keys are `snake_case` and shared across the stack: `tenant_id`, `user_id`, `job_id`, `trace_id`, `duration_ms`.

### Internationalization

- **Do not hardcode user-facing text**, in any language. UI packages must contain no bare text nodes; Go returns structured error codes.
- New text **must** ship with both `zh-CN` and `en-US` resources. The backend half of that rule is enforced in code rather than CI: `pkgcore/i18n`'s `Builder.AddModule` fails a module whose language files' id sets differ (`ErrParityMismatch`) while `Kernel.Bootstrap` merges its catalog. `tools/check_i18n_keys.py` checks the same key-set parity over the raw files; the docs-check pipeline runs it on every PR touching documentation or i18n resources (Repository Status).
- Backend-generated content (emails, invoices, notifications) renders in the **recipient's** locale, not the operator's UI language.

### Database and migrations

- **Do not use `AutoMigrate`.** Migrations are versioned SQL generated by Atlas from the GORM models, one set per dialect.
- **Do not use PostgreSQL-only features**: `gen_random_uuid()`, native arrays, JSONB operator filtering, `NOW()`. Generate IDs in the application and use `datatypes.JSON`.

### Security

- **Do not merge social login accounts on matching email alone.** Auto-link only when the provider reports a verified email and is on the trusted list; otherwise require sign-in first, then binding.
- **Do not use `tenant_id` as a Prometheus metric label.** High cardinality will take Prometheus down; tenant dimensions belong in span attributes and log fields.
- **Do not let outbound webhooks reach internal addresses.** SSRF protection is mandatory, including DNS-rebinding protection.
- **Do not write plaintext PII, secrets or tokens into logs, traces or API responses.** Redaction is on by default.
- **Do not send messages to unverified phone numbers or email addresses.** External contacts must complete consent verification first; the verification message itself is the only exception and is rate limited.
- **Do not forward internal domain events straight to outbound webhooks.** Map them to a versioned public event schema.
- Audit records produced during impersonation **must** carry both the impersonated user (`Actor`) and the real administrator (`OnBehalfOf`).

### Testing

- **Unit test files are named after the file they test** (`registry.go` → `registry_test.go`; `PlanCard.tsx` → `PlanCard.test.tsx`). A test file that doesn't map 1:1 to one source file is named for the behaviour it verifies, never a generic word like `misc` or `extra`.
- **Do not scatter shared test helpers across test files.** Put them in a dedicated `internal/testutil` package (Go) or `test-utils/` directory (frontend) — never duplicated, never inline in a file another package's tests need to import.
- **Do not mix integration tests into the unit test set.** They live in a physically separate directory (`integration_test/` per Go package, `e2e/` for Playwright), so a plain `go test ./...` or unit test run never touches them.
- Full detail and examples: `.claude/skills/backend-coding-standards/SKILL.md` §13, `.claude/skills/frontend-coding-standards/SKILL.md` §12.

### Documentation

- A new public API **must** ship, in the same pull request, with usage docs, a compilable example, and an entry in the module's `AGENTS.md`.
- Godoc `Example` functions are compiled and run by CI inside each module's unit suite, so a failing example fails the build — every implemented module ships at least one. Examples embedded in markdown prose (`AGENTS.md`, READMEs, ADRs) have **no** compile harness; `docs-check.yml`'s DELIBERATELY NOT WIRED list records that gap.
- The configuration reference is generated from the config schema — **do not hand-write it**.

### Commits and merging

- **Rebase onto the target branch before merging; fast-forward merges only.** History stays linear.
- Commit messages are English, in Conventional Commits form, scoped by module: `fix(billing): prevent credit over-deduction under concurrent debits`.
- **Every bug fix ships with a test that reproduces the bug** (failing before the fix, passing after). If one genuinely cannot be added, explain why and what the follow-up is.
- **Do not ignore warnings.** Compiler, lint, deprecation, console, a11y and race-detector warnings are all first-class issues; anything deferred must be called out explicitly.

## Traps Specific to This Codebase

Each of these has bitten real SaaS products:

- **Workers do not inherit tenant context.** Rebuild it explicitly (`pkgcore.WithTenant(ctx, job.TenantID)`) or the Repository fails closed.
- **Encrypted fields cannot be queried.** Phone numbers are encrypted at rest yet used as a login identifier; the blind-index column such a lookup needs is a `go/dbkit` facility implemented today (`NewBlindIndexer`: HMAC-SHA256 over the canonical form, with `NormalizePhoneE164` / `NormalizeEmail` supplying the E.164 / lowercased-email forms), so a module storing such an identifier must use that facility rather than reimplementing it.
- **Billing-grade metering cannot fail open.** It uses the outbox pattern in the same transaction as the business write; only analytics-grade metering may drop events.
- **Notifications are event-driven.** Business modules publish domain events; `notification` subscribes. The sole exception is synchronous verification codes. External recipients who are not users require consent verification before anything is sent.

## Where the Rest Lives

| Location | Content |
|---|---|
| `.claude/skills/**` | Coding standards handbooks — how to write the code, with templates and checklists. Load the relevant skill before writing: `backend-coding-standards`, `frontend-coding-standards`, `commit-convention`. |
| `docs/internal/**` | Design decisions and *why* they were made, including rejected alternatives. Start at `00-overview.md`, which carries the full navigation table. |
| per-module `AGENTS.md` | Module-level discipline that ships with the module to consuming projects, whatever AI tooling they use. |

## Reference App

`examples/reference-app` is an AI smile simulation platform (dental SaaS) and is the **mandatory first consumer** of every module — a module API that it does not actually use is not considered done. It was chosen because it exercises every edge of a general SaaS: multi-level organizations, pay-per-use credits, long-running AI jobs, media handling, external sharing, sensitive-data compliance, third-party integration. Validating the design against it already exposed six missing capabilities.

An end-to-end suite with recorded/replayed AI responses — real provider calls happen only during milestone acceptance and demos — is planned to run in CI with the e2e pipeline (`.github/workflows/e2e.yml`, a gated stub deferring to the roadmap M4 e2e item); no live workflow runs an end-to-end reference-app suite today (its unit suite runs on `full-ci`-labeled PRs via pr-full).
