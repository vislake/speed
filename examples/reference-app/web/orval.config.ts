/**
 * Generation configuration for the reference app's app-owned SDK: the
 * frontend-contract half of the app-owned generation flow
 * (docs/internal/21-api-contract.md's app-owned flow section). The
 * reference app is the consumer-shaped template: its three fragments
 * (examples/reference-app/internal/{notes,cases,smilesim}/api) are the
 * app's own API, never platform API, so the platform @speed/api-sdk
 * does not cover them -- they generate into this app-local SDK instead,
 * imported by the app's own web host through src/app-api.
 *
 * orval version pinned: 8.17.0, the identical pin the platform SDK
 * uses. Every runner invokes the same command -- the Taskfile
 * `api:gen:app` task and full-check.yml's reference-app job both run
 * `pnpm dlx orval@8.17.0 --config orval.config.ts` from this
 * directory -- so the two cannot drift. orval itself is never a
 * workspace dependency: pnpm dlx fetches it on demand and it stays out
 * of pnpm-lock.yaml. A version bump must land in this header, the
 * Taskfile api:gen:app task and full-check.yml together, and shows up
 * as a diff in the regenerated header (which stamps the version).
 *
 * Input is the app-owned merged document, app-openapi.yaml (this
 * directory), produced by the pinned redocly `join` of the app's three
 * fragments (Taskfile's api:gen:app task -- a fragment edit always
 * regenerates through a fresh join, never through a stale committed
 * app-openapi.yaml). Output covers types AND TanStack Query hooks
 * (client: react-query), written to src/app-api/index.ts; generated
 * files carry the DO-NOT-EDIT header below.
 *
 * HTTP transport: httpClient 'axios' selects the axios-shaped mutator
 * call -- one options object `{url, method, headers, params, data,
 * signal}` -- routed to the exported `speedRequest` adapter in
 * src/app-api/runtime.ts. That seam re-exports the platform seam
 * (@speed/api-sdk/runtime): the app host binds its single api-client
 * RequestFn once at bootstrap, and both generated surfaces -- the
 * platform SDK's and this app SDK's -- ride that one binding, so no
 * generated file and no bridge file performs HTTP itself. The app's
 * fragments ship no credential-less operation, so no per-operation
 * override exists here.
 *
 * Config paths are relative to this file's directory (the app web
 * host), the same convention as web/orval.config.ts.
 */

export default {
  'reference-app': {
    input: {
      target: './app-openapi.yaml',
    },
    output: {
      target: './src/app-api/index.ts',
      client: 'react-query',
      httpClient: 'axios',
      mode: 'single',
      override: {
        // Trailing newline is load-bearing: orval concatenates the
        // header with the file body, so the last line must end in \n.
        header: (info: { title: string }): string =>
          `// Code generated by orval 8.17.0. DO NOT EDIT.\n` +
          `// Source: ${info.title}\n` +
          `// The mutator import in this file is rewritten to an explicit\n` +
          `// .js extension by web/scripts/orval-nodenext-fixup.mjs (nodenext\n` +
          `// builds); rerun it after every regeneration.\n`,
        mutator: {
          path: './src/app-api/runtime.ts',
          name: 'speedRequest',
        },
      },
    },
  },
}
