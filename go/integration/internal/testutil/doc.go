// Package testutil holds test-only helpers shared across go/integration's
// own test files, per the backend coding standard's "shared test helpers
// live in a dedicated internal/testutil package, never scattered across
// test files" rule. It is not exported outside this module.
package testutil
