# integration

go/integration is a tenant's outward-facing API surface: API keys a tenant issues to its own scripts and third-party systems, and the three-layer rate limiting that protects the platform, the tenant and the individual key from one another. This file is the module-level discipline that ships with `go/integration` to consuming projects; the design rationale is `docs/internal/07-platform-services.md`'s integration section, and the repository-wide rules are the root `CLAUDE.md` plus `.claude/skills/backend-coding-standards`.

**Status: round 7 of at least 7.** Round 1 shipped API key issuance, listing, rotation and revocation, plus the three-layer rate-limiting composition on top of `go/ratelimit` and its HTTP 429 translation as a reusable middleware. Round 2 shipped the design doc's other half: outbound webhooks -- tenant-configured subscriptions, the internal-to-public event schema mapping mechanism, event-driven delivery via `go/jobs`, HMAC signing, SSRF-protected delivery (creation-time and dial-time), and retry with dead-lettering. Round 3 is a cross-module soft-delete-adoption backlog item (following `go/org`'s and `go/rbac`'s own rounds): `WebhookSubscription` adopts `dbkit.SoftDeletable`, `Service.RestoreWebhookSubscription` undoes a mark-delete, and this module gains its first-ever PostgreSQL integration tier -- see "Soft deletion" below for the full round. Round 4 retired round 2's own "no reference-app consumer yet" exception for the outbound-webhook surface: the reference app wires a real `EventMapping` over `org.member.joined` -- mounting, for the moment, one hand-written, permission-gated route (`examples/reference-app/cmd/server/webhooks.go`) that lets a demo tenant create a subscription, a route round 7 retired (shrinking that file back to its `EventMapping` machinery alone) once the subscription-CRUD surface was itself spec-generated HTTP -- and its own `webhook_flow_test.go` drives a REAL HMAC-signed HTTP delivery -- independently re-verified, not merely observed as "a request arrived" -- against a real receiver process it controls, plus a real, unmodified retry-then-dead-letter proof that runs this module's own `jobs`-backed retry horizon to exhaustion (7 total attempts, no 8th). That round also broke this module's still-not-frozen public API for the first time, per round 2's own explicit permission for that: the test-only, unexported `withHTTPClient`/`withWebhookURLValidator` seams (`module.go`) became the exported `WithWebhookHTTPClient`/`WithWebhookURLValidator` options, since a real host -- not just this module's own white-box tests -- turned out to need them (see "Seams" below). Round 4 left round 1's API-key surface (issuance/listing/rotation/revocation) untouched -- it still carried round 1's own "no reference-app consumer yet" exception and `Module.OpenAPISpec()` still returned `nil` for this module. Round 5 closed exactly that gap: a real OpenAPI fragment (`api/openapi.yaml`) and spec-generated HTTP surface (`api/integration-server.gen.go`, `handler.go`) for round 1's four operations, mounted in the reference app through the SAME generic route-mounting machinery every other module's fragment already uses (`reg.Routes.Mount`, `mountModuleRoutes`), gated on `integration:apikey:read`/`integration:apikey:manage` through a new router-level gate the mismatch between this module's three-segment permission names and `rbac.RequirePermissionFunc`'s own two-segment contract required (`guardIntegrationAPIKeyRoute`, `demo_subject.go` -- the same shape mismatch `wireIntegrationWebhooks` already found and worked around for this module's other permission pair), and driven end to end by `examples/reference-app/cmd/server/apikey_flow_test.go`. Round 5's own review named a real, documented gap that round could not close: no `Authenticate`/`Verify` method existed anywhere in this module, so "a request bearing a rotated-away or revoked key is refused" was asserted in prose but unverifiable in code (see round 5's own "What this proof does NOT, and cannot yet, demonstrate" paragraph, still below, now historical). **Round 6 closes exactly that gap**: `Service.Authenticate` (`authenticate.go`), a new platform-data table (`integration_api_key_hash_index`) that resolves a presented key's owning tenant from the key alone, a directly-usable HTTP middleware (`AuthMiddleware`, `middleware.go`) that extracts a bearer credential from `X-API-Key`, authenticates it and attaches the resolved tenant and identity to request context, and -- for the first time -- a real, mounted, Authenticate-gated surface in the reference app (`examples/reference-app/cmd/server/integration_authenticate.go`'s `integrationWhoamiPath`) with `LayeredLimiter`/`HTTPGuard` genuinely wired in front of it, discharging round 5's own "these rate-limit requests authenticated by an API key against a host's own inbound API surface, a surface this module still does not build" deferral. `apikey_authenticate_flow_test.go` drives the full create/authenticate/rotate/old-key-refused/new-key-succeeds/revoke/refused cycle through the real composed HTTP stack. See "In scope round 6" and "Reference app: mandatory first consumer (round 6's inbound-authentication surface)" below for the full record. **Round 7 closes the last of round 2's own documented deferrals for the outbound-webhook half**: the webhook-SUBSCRIPTION-CRUD surface round 2 shipped Service-level grows a spec-generated HTTP surface of its own -- six operations under `/api/v1/integration/webhooks` (create, list, update, delete, restore, list deliveries), growing the fragment round 5 opened with round 1's four apikey operations to ten -- implemented by round 5's own `Handler` with no Service-layer signature or business-semantics change, and consumed end to end by the reference app for the first time (`webhook_crud_flow_test.go`), which also retires the hand-mounted demo route round 4's own consumer proof created its subscriptions through. See "In scope round 7" and "Reference app: mandatory first consumer (round 7's webhook-subscription-CRUD surface)" below for the full record. Judge what exists by the code, not by this sentence.

## Scope

**In scope round 1** (unchanged by round 2). One table (`integration_api_keys`) with dual-dialect versioned SQL migrations, registered through `dbkit.MigrationRegistry` the same way every other module does. `Service` (`service.go`): `Create` (generates the raw key, returns it to the caller exactly once), `List` (never exposes the raw key or its hash), `Rotate` (create-new + revoke-old, documented below) and `Revoke`. `LayeredLimiter` (`ratelimit.go`): three independent `go/ratelimit.Allow` calls -- global, tenant, key -- composed with short-circuit-on-first-denial semantics, built entirely from `go/ratelimit`'s existing public API with no change to that package. `HTTPGuard` (`httpguard.go`): the handler-layer translation of a denied `LayeredDecision` into a 429 response with `Retry-After` and `X-RateLimit-*` headers -- a directly usable `net/http` middleware. Round 5 gives round 1's CRUD operations their own mounted HTTP surface (below), but deliberately never wires `HTTPGuard`/`LayeredLimiter` in front of it -- see round 5's own scope entry for why the two stay separate concerns. `pkgcore.Module` wiring: permissions (`integration:apikey:read`, `integration:apikey:manage`), audit actions (`integration.apikey.create`/`.revoke` -- no `.rotate`: `Service.Rotate` records a rotation as its two ordinary create/revoke events, so a rotate action would be a registered vocabulary entry nothing ever emits).

**In scope round 2.** Two new tables (`integration_webhook_subscriptions`, `integration_webhook_deliveries`, `webhook_model.go`) with dual-dialect versioned SQL migrations. `Service` grows a second surface: `CreateWebhookSubscription`/`ListWebhookSubscriptions`/`UpdateWebhookSubscription`/`DeleteWebhookSubscription`/`ListRecentWebhookDeliveries` (`webhook_service.go`). `EventMapping` and `WithEventMapping` (`eventmapping.go`, `module.go`) -- the internal-to-public event schema mapping registry a host populates at `NewModule` construction time; see "Internal-to-public event mapping" below for the design rationale. `Module.Register` now also subscribes to every declared mapping's `InternalType` on the host's `pkgcore.EventBus` and registers this module's webhook-delivery `jobs.Handler`/`jobs.FailureHook` on `reg.Jobs` (`webhook_delivery.go`, `module.go`'s `webhookDeliveryHandler`). `ValidateWebhookURL` and `newSafeHTTPClient` (`ssrf.go`) -- the two-layer SSRF defense (creation-time refusal, dial-time re-check) docs/internal/07's line naming SSRF the most common outbound-webhook security hole calls for. `signWebhookPayload` and the `HeaderWebhook*`/`WebhookReplayTolerance` exports (`webhook_signature.go`) -- the HMAC-SHA256-over-timestamp-and-body signing scheme. New permissions (`integration:webhook:read`, `integration:webhook:manage`), audit actions (`integration.webhook_subscription.create`/`.update`/`.delete`), and this round's own error codes, all bilingual.

**In scope round 3.** `WebhookSubscription` adopts `dbkit.SoftDeletable` (`webhook_model.go`), one new migration (`migrations/{sqlite,postgres}/0004_add_soft_delete.sql`) and a second, PostgreSQL-only one fixing a pre-existing column-type bug this round's own new integration tier found (`migrations/postgres/0005_fix_webhook_secret_column_type.sql`). `Service.RestoreWebhookSubscription` (`webhook_service.go`) undoes a mark-delete, always landing the subscription paused (`Active = false`) regardless of its value at deletion. A new audit action, `integration.webhook_subscription.restore`. This module's first-ever PostgreSQL integration tier (`integration_test/postgres_softdelete_test.go`). See "Soft deletion" below for the full design record.

**In scope round 4.** The reference app becomes this module's mandatory first consumer, for round 2's outbound-webhook surface only -- see "Reference app: mandatory first consumer" below for the full record. No new table, no new error code and no new permission: this round is host-side wiring plus one API-surface correction inside this module itself, `module.go`'s test-only `withHTTPClient`/`withWebhookURLValidator` seams promoted to the exported `WithWebhookHTTPClient`/`WithWebhookURLValidator` (see "Seams" below), which the first real host actually needed and which round 2's own explicit "not frozen" declaration permitted breaking.

**In scope round 5.** No new table and no new permission -- round 1's own `integration:apikey:read`/`integration:apikey:manage` are what this round's HTTP surface gates on, unchanged. What round 5 adds: `api/openapi.yaml`, this module's OpenAPI fragment for round 1's four operations (`integration_createAPIKey`, `integration_listAPIKeys`, `integration_rotateAPIKey`, `integration_revokeAPIKey`), generated by the same pinned oapi-codegen v2.8.0 every other fragment uses into a committed `api/integration-server.gen.go`; `handler.go`'s `Handler`, implementing the generated `api.ServerInterface` behind a `var _ api.ServerInterface` compile-time assertion, backed by the EXISTING `Service` methods with no new business logic of its own; `Module.OpenAPISpec()` now returns the embedded fragment instead of `nil`, and `Module.Register` mounts `Handler` on `reg.Routes` (`apiPath`, `/api/v1/integration`) the same way org's, storage's, notification's, sharing's and admin's `Register` methods already mount theirs; a new, optional `SubjectResolver` seam (`seams.go`) and `WithSubjectResolver` option, read only by `integration_createAPIKey` to resolve the caller who becomes a new key's `CreatedBy` -- the identical structurally-typed, no-import shape `org.SubjectResolver` and `notification.SubjectResolver` already establish, satisfied by the SAME resolver instance a host wiring either of those already has; two new transport-layer error codes, `integration.invalid_request_body` and `integration.subject_unresolved`, declared in `errors.go` alongside round 1's own codes (Service itself raises neither -- they are Handler's own translation-layer conditions, following org's and storage's identical split between a Service's own error index and its Handler's transport-layer additions); and one new Go dependency, `github.com/oapi-codegen/runtime` (`go.mod`), pulled in by the generated file's path-parameter binding -- the identical dependency org's, storage's, notification's, sharing's and admin's own `go.mod` already carry for the identical reason. This round deliberately did NOT wire `HTTPGuard`/`LayeredLimiter` in front of the new CRUD surface: those exist to rate-limit a request AUTHENTICATED BY one of these API keys against a HOST'S OWN inbound API surface (docs/internal/07-platform-services.md's three-layer design -- global, tenant, key), a surface this module did not yet build at the time (round 6, below, is what finally builds one). The CRUD surface this round ships is a DIFFERENT thing entirely -- a tenant administrator managing their own keys through their own ordinary session, gated by rbac exactly like every other module's management surface (org's node CRUD, storage's object CRUD, notification's preference CRUD: none of them sit behind `go/ratelimit` either) -- and applying the inbound-traffic rate limiter to it would conflate the two, the same category error the design doc's own layering is explicit about keeping apart; round 6 does not change this either -- the CRUD surface still sits behind rbac alone, never `HTTPGuard`. This round also retired round 1's own "no reference-app consumer yet" exception and, per root CLAUDE.md's public-API-is-frozen rule, froze round 1's public API for real: `examples/reference-app/cmd/server/apikey_flow_test.go` is the mandatory-first-consumer proof, driving create/list/rotate/revoke through the composed HTTP stack -- see "Reference app: mandatory first consumer (round 1's API-key surface)" below for the full record.

**In scope round 6.** No change to `APIKey`'s stored hash format, `uq_integration_api_keys_tenant_hash`, or any round 1-5 `Service` method's signature. What round 6 adds: `Service.Authenticate(ctx, rawKey string) (*AuthenticatedAPIKey, error)` (`authenticate.go`), the `Authenticate`/`Verify` method round 5's own review named as the one gap it could not close -- hashes `rawKey` with the EXISTING `hashAPIKeyToken`, resolves the raw key's owning tenant from a NEW platform-data table (`integration_api_key_hash_index`, `apiKeyHashIndex` in `model.go`, migrations `0006`), then performs the ordinary tenant-scoped read and refuses -- with the single outward-identical `ErrAuthenticationFailed` -- a hash that does not exist anywhere, one that is revoked, or one that has expired, never distinguishing which; `AuthMiddleware`/`NewAuthMiddleware` (`middleware.go`), a directly-usable `net/http` middleware extracting a bearer credential from the new `HeaderAPIKey` (`X-API-Key`) header, calling `Authenticate`, and on success attaching both the resolved tenant (`pkgcore.WithTenant`) and the full `*AuthenticatedAPIKey` (`AuthenticatedAPIKeyFromContext`) to request context for downstream handlers -- built the identical "holds a `*Module`, reads `m.service` at call time" way `Handler` is (round 5's own reasoning: `Service` is built in `Attach`, strictly after `Register`, so a middleware a host wants to construct during `Register` cannot yet hold a `*Service` directly); one new error code, `integration.authentication_failed`, declared in `errors.go`; and the reference app wiring `LayeredLimiter`/`HTTPGuard` in front of a real, mounted, Authenticate-gated demo route for the first time -- see "Reference app: mandatory first consumer (round 6's inbound-authentication surface)" below. `Service.Create` and `Service.Rotate` (both of which ultimately call `Create`) now write the new `apiKeyHashIndex` row transactionally alongside the `APIKey` row itself (`APIKeyRepository.createWithHashIndex`), a change to `Create`'s own INTERNAL persistence path that does not touch its exported signature. `Service.Authenticate` best-effort records `APIKey.LastUsedAt` on a successful authentication, closing that round-1 known limitation too (a failure to record it is logged and swallowed, never surfaced as an authentication failure). `HeaderAPIKey`, not `Authorization: Bearer`, is this round's deliberate header choice -- see `middleware.go`'s own doc comment on `HeaderAPIKey` for the real, verified-in-tree conflict with `go/authn.Middleware` that ruled the more common convention out for THIS reference app's own composition (a conflict any host reusing `authn.Middleware` as its outermost layer would hit identically, not an artifact specific to the demo).

**In scope round 7.** No new table, no migration, no new permission and no Service-layer signature change -- every one of round 7's six operations calls an EXISTING round 1-3 `Service` method unchanged. What round 7 adds: `api/openapi.yaml`'s webhook half, six operations under `/api/v1/integration/webhooks` (`integration_listWebhookSubscriptions`, `integration_createWebhookSubscription`, `integration_updateWebhookSubscription`, `integration_deleteWebhookSubscription`, `integration_restoreWebhookSubscription`, `integration_listWebhookDeliveries` -- the fragment round 5 opened with round 1's four apikey operations now carries ten), generated by the same pinned oapi-codegen v2.8.0 into the existing committed `api/integration-server.gen.go`; round 5's own `Handler` implements the six new methods behind the same `var _ api.ServerInterface` assertion, delegating to `Service.ListWebhookSubscriptions`/`CreateWebhookSubscription`/`UpdateWebhookSubscription`/`DeleteWebhookSubscription`/`RestoreWebhookSubscription`/`ListRecentWebhookDeliveries` unchanged (`errors.go`'s, `module.go`'s and `seams.go`'s own changes are doc comments keeping their round-5 claims true of the grown surface -- same two transport codes, no new one). The wire shape follows the round-5 apikey surface's own conventions with three deliberate additions. First, create and update decode through `decodeRequiredJSON` (`handler.go`), not round 5's `decodeOptionalJSON`: a GENUINELY EMPTY body is refused as `integration.invalid_request_body` (no cause) exactly like a malformed one (with cause) -- the strict-request discipline the apikey create's optional decode deliberately does not apply, an empty body being a legal scope-less issuance there but never a legal subscription here. Second, create resolves its creator through the SAME `SubjectResolver` seam round 5's own `integration_createAPIKey` already reads (`handler.go`'s shared `resolveSubject`) -- `integration_createWebhookSubscription` is the seam's second consumer, and the two subject-reading creates are the only round-7 operations that consult it at all: update preserves the existing row's `CreatedBy` untouched, delete, restore and the deliveries listing need no creator, and list reads only the tenant. Third, the deliveries listing deliberately never validates its `subscriptionId` -- an unknown id answers an empty list, never a not-found, the no-enumeration discipline this module's own outward-identical refusals already follow, pinned by the spec operation's own description -- and passes `params.Limit` straight through, nil (or any non-positive value) becoming `Service`'s own default of 50, per `handler.go`'s own doc comment on the operation. delete and restore answer 204; restore carries round 3's Service rule -- a restored subscription always comes back `Active = false` -- onto the wire unchanged; update answers 200 with the full summary body under the Service's own partial-update semantics (a named field is replaced, an absent `eventTypes` leaves the stored selection intact, an explicitly empty one is refused with `integration.event_types_required`, the Service's own existing validation); create answers 201 with the one response shape that carries the raw signing secret, mirroring `IntegrationCreatedAPIKey`'s own "shown once" contract. The mount is still round 5's single `reg.Routes.Mount(apiPath, m.handler)` line (`module.go`) -- the apikey and webhook halves share one mounted `Handler` -- and round 5's "HTTPGuard/LayeredLimiter stay unwired in front of this CRUD surface" reasoning carries over to the webhook half identically: this surface is a tenant administrator managing subscriptions through an ordinary session, gated by rbac, not inbound API-key-authenticated traffic. A new reference-app acceptance proof drives all six operations -- see "Reference app: mandatory first consumer (round 7's webhook-subscription-CRUD surface)" below.

**Deliberately not in scope round 2 either:**

| Not here | Where it belongs | Why |
|---|---|---|
| A mounted HTTP surface / OpenAPI fragment for webhook subscription CRUD | DISCHARGED by round 7 | Round 7 gave round 2's webhook-subscription surface exactly what round 5 gave round 1's API-key surface: the fragment's webhook half (`api/openapi.yaml`, six operations under `/api/v1/integration/webhooks`) and `handler.go` methods behind the same `var _ api.ServerInterface` assertion -- see "In scope round 7" above. Kept, marked discharged, rather than deleted, so a reader who remembers this deferral from round 2 or round 5 can find where it closed |
| Manual redelivery (docs/internal/07's own name for it) -- an API or method that re-enqueues a specific dead-lettered `WebhookDelivery` | A later round | The design doc frames it as delivery-log's sequential-next feature; `DeliveryStatusDeadLetter` and every field a redelivery would need (`Payload`, `SubscriptionID`, `EventType`/`EventVersion`) already exist on the row, so a later round needs no migration of its own -- exactly the "get the table shape right first" precedent round 1's own `LastUsedAt`/expiry-sweep-index rows set |
| Platform-blacklist-style writers, per-tenant delivery-volume rate limiting, or per-webhook-type opt-out | A later round | Not named anywhere in this round's instruction; the design doc's webhook paragraph does not ask for them either, unlike the API-key rate-limiting rows round 1 already covers via `LayeredLimiter` |
| Re-validating a webhook URL on a schedule independent of delivery (a background SSRF re-check job) | A later round, if ever | `newSafeHTTPClient`'s dial-time re-check already re-validates on every delivery ATTEMPT, which is the moment that actually matters (an unreachable-otherwise subscription with zero events never needs a background check); see ssrf.go's own file comment |
| An expiry-sweep job for API keys (revoking or reaping keys whose `ExpiresAt` has passed) | A later round | `idx_integration_api_keys_tenant_expires_at` still exists for the identical "get the table shape right first" reason; this module's API-key half still does not depend on `go/jobs` for its own expiry -- round 2's dependency on `go/jobs` is entirely the webhook-delivery pipeline's. Round 6 does not need this either: `Authenticate` checks `IsExpired` against the caller's own clock on every call, so an unswept, past-expiry row is refused correctly today -- a sweep job would only reclaim disk, never correctness |
| Reference-app wiring as the mandatory first consumer, for round 2's webhook-SUBSCRIPTION-CRUD surface specifically (`CreateWebhookSubscription`/`UpdateWebhookSubscription`/`DeleteWebhookSubscription` mounted as real HTTP operations) | DISCHARGED by round 7 | Round 4 discharged the mandatory-first-consumer obligation for round 2's DELIVERY pipeline (a real `EventMapping`, real signed HTTP delivery, real retry/dead-letter -- see "Reference app: mandatory first consumer (round 2's webhook surface)" below) through `webhooks.go`'s hand-mounted demo route; round 5 discharged the SEPARATE, round-1-only obligation this row used to describe (see "In scope round 5" above). Round 7 discharged the third, final one: `webhook_crud_flow_test.go` consumes the spec-generated CRUD surface the round-7 rows above describe end to end (see "Reference app: mandatory first consumer (round 7's webhook-subscription-CRUD surface)" below), and the demo route that had stood in for this obligation since round 4 was retired. Kept, marked discharged, rather than deleted, so a reader who remembers this deferral can find where it closed |
| `integration:apikey:rotate`/`:revoke` as separate permissions from `:manage`, or `integration:webhook:update`/`:delete` as separate permissions from `:manage` | Not planned | The design doc's own permission list treats each entity's lifecycle management as one capability; splitting either further is not asked for anywhere |
| Soft-deleting `APIKey` | Not planned | Already non-destructive via its own `RevokedAt` mark (round 1); see "Soft deletion" below |
| Cascading `Restore`, or re-validating a restored `WebhookSubscription`'s `URL`/`EventTypes` against `Create`'s own rules | A later round, if ever | See "Soft deletion" below's Known limitation and the `go/org`/`go/rbac` precedent it follows |
| An HTTP surface for `RestoreWebhookSubscription` | DISCHARGED by round 7 | `integration_restoreWebhookSubscription` (`POST .../webhooks/{subscriptionId}/restore`, answering 204) mounts round 3's Service method with its rule -- a restored subscription always comes back `Active = false` -- unchanged on the wire, and answers the Service's own collapsed not-found identically (an id that never existed and one that exists but is not mark-deleted are indistinguishable). Kept, marked discharged, rather than deleted, so a reader who remembers this deferral from round 3 can find where it closed |
| A production, non-demo inbound API gateway (request logging, per-route scope enforcement against `AuthenticatedAPIKey.Scopes`, a real product surface beyond one whoami route) | A later round, if ever | Round 6's own instruction explicitly scoped the reference-app leg to "one minimal, clearly-labeled demo endpoint... sufficient to prove the mechanism end to end"; `AuthenticatedAPIKey.Scopes` is returned to the caller precisely so a REAL host can build that enforcement itself, but this module ships no opinion on how a route maps to a required scope |

## Data model

One table, tenant data (`docs/internal/04-data-and-tenancy.md`): `integration_api_keys`, isolation proven by `tenancytest.AssertIsolated` (`repository_test.go`), never `AssertNotTenantScoped` -- a key belongs to exactly one tenant, never to the individual who created it (`CreatedBy` is an id reference, not an ownership tie; see model.go's own doc comment).

**The primary key is `(id)` alone**, matching `go/storage`'s `Object` precedent rather than a composite `(tenant_id, id)` key: `id` is an application-generated UUID (`uuid.NewString`), globally unique on its own, so `tenant_id` rides along as a plain, non-key column promoted by the embedded `dbkit.TenantModel`.

**The key material is never stored.** `Hash` is the hex-encoded SHA-256 of the raw key value `Service.Create` generates once and returns to the caller; it is never persisted, logged, or returned by any other call. `keygen.go`'s `newAPIKeyToken` explains why plain SHA-256 (not a deliberately-slow password hash) is the right primitive here: the input is 32 bytes of `crypto/rand` full-entropy randomness, not a human-chosen secret, so there is no dictionary an attacker could use to make a slow hash worth paying for -- the same reasoning `go/org`'s invitation token hashing already documents. `Prefix` is the plaintext, non-secret display portion (`sk_` plus eight characters of the encoded random value) a key list shows so an operator can tell two keys apart without ever seeing the rest.

**Scopes are frozen at issuance, never re-derived.** `APIKey.Scopes` is the subset of `CreatedBy`'s permissions `Service.Create` validated at the moment the key was issued, through the mandatory `PermissionLister` seam. A later change to the creator's own permissions -- promoted, demoted, or removed from the tenant entirely -- never widens or shrinks an already-issued key; the design doc is explicit that a key's scope does not change along with its creator's later permission changes. Changing what a key may do means issuing a new one (`Rotate`); nothing in this module ever rewrites `Scopes` after `Create`.

**The creator leaving does not revoke the key.** `CreatedBy` is the responsible party on record, not an ownership tie -- the design doc is explicit that it is unacceptable for an integration to break just because someone left the tenant. `APIKeySummary.CreatorLeft`, computed at `List` time through the optional `MembershipChecker` seam, is the visibility the design doc does ask for, so a tenant administrator notices a key needs a new owner of record without the key itself being touched.

**Every key has a mandatory, forced expiry.** `ExpiresAt` is required; `Service.Create` defaults an unspecified request to `MaxAPIKeyLifetime` (one year) from now and refuses a request asking for anything further out (`ErrExpiryExceedsMaximum`) or already past (`ErrExpiryInPast`), per the design doc's forced-expiry-ceiling rule -- a key that never expires is the most common credential-leak surface, so this is refused rather than silently clamped.

### Round 2's two tables

Both tenant data, isolation proven by `tenancytest.AssertIsolated` (`webhook_repository_test.go`), both primary-keyed `(id)` alone on the identical `integration_api_keys` precedent above.

**`integration_webhook_subscriptions`** (`WebhookSubscription`, `webhook_model.go`): `URL` (validated by `ValidateWebhookURL` at creation and update time -- see "Outbound webhook delivery" below), `EventTypes` (a JSON array of PUBLIC event types, never internal `pkgcore.Event.Type` strings -- see that field's own doc comment for why the two vocabularies are kept apart), `Secret` (the per-subscription HMAC key, encrypted at rest under `WebhookSecretSerializerName` -- a host registers it with `dbkit.RegisterEncryptedSerializer` before opening the database, exactly like `go/org`'s `EmailSerializerName` and `go/notification`'s `ContactAddressSerializerName`), `Active` (the pause/resume gate), `CreatedBy`.

**`Secret` is reversibly encrypted, never hashed -- unlike `APIKey.Hash`.** This is the one place round 2's storage choice looks, at a glance, like it contradicts round 1's own "the key material is never stored" rule above. It does not: an API key hash only ever needs to be COMPARED (a lookup by hash), while a webhook secret must be READ BACK IN PLAINTEXT on every delivery attempt to compute that attempt's HMAC signature (`webhook_signature.go`'s `signWebhookPayload`). A one-way digest cannot serve that purpose. See `WebhookSecretSerializerName`'s own doc comment in `webhook_model.go` for the full argument, including why the encryption key must be a different secret from any blind-index key this module might grow later (`dbkit.NewCipher`'s own doc comment).

**`integration_webhook_deliveries`** (`WebhookDelivery`, `webhook_model.go`): the delivery log docs/internal/07 requires -- `SubscriptionID`, `EventType`/`EventVersion` (the PUBLIC identity, copied from the `EventMapping` that produced the row), `IdempotencyKey` (the fan-out dedupe key, `deriveWebhookDeliveryKey` in `webhook_delivery.go`), `Payload` (the exact rendered body, computed once and never recomputed on retry -- see that field's own doc comment for why), `Status` (`DeliveryStatusPending`/`Failed`/`Delivered`/`DeadLetter`), `Attempts`, `LastStatusCode`, `LastError`, `LastAttemptAt`, `DeliveredAt`. **One row per (subscription, event), not one row per HTTP attempt** -- a retry updates the SAME row in place; see the type's own doc comment for why. `idx_integration_webhook_deliveries_subscription_created_at` serves `Service.ListRecentWebhookDeliveries`'s "recent deliveries for this subscription" read; `uq_integration_webhook_deliveries_tenant_subscription_key` is what makes fan-out idempotent under an at-least-once `EventBus`.

### Round 6's platform-data table

**`integration_api_key_hash_index`** (`apiKeyHashIndex`, `model.go`, migration `0006`): a narrow, two-column (`hash` primary key, `tenant_id`) platform-data table -- deliberately NOT `dbkit.TenantScoped`, isolation proven by `tenancytest.AssertNotTenantScoped` rather than `AssertIsolated` -- mirroring `go/sharing`'s `shareTokenIndex` almost exactly. It exists for one reason: `Service.Authenticate` must resolve a presented raw key's owning tenant BEFORE any tenant is in context at all (a genuinely inbound request authenticated by nothing but the key carries no separate tenant claim), and `integration_api_keys` cannot answer that -- it IS `dbkit.TenantScoped`, so a query against it with no tenant in context fails closed at the GORM plugin layer (`go/dbkit/tenant_scope.go`), and reaching around that with `db.Table`/`db.Model`/`db.Raw` would be exactly the bypass root `CLAUDE.md` forbids. `APIKeyRepository.createWithHashIndex` writes both this row and its `APIKey` row in one transaction (`Service.Create`, and therefore `Service.Rotate` too); `tenantForHash` reads it with no tenant predicate at all (`APIKeyRepository`'s one deliberate exception to "every query is tenant-scoped"); `byHash` performs the ordinary tenant-scoped read once a tenant is resolved. This row is NEVER updated or deleted by `Service.Revoke`, `Rotate` or anything else -- it stays in place after a key is revoked or expires, exactly like `shareTokenIndex` stays in place after a share is revoked, because `Authenticate` needs it to resolve a tenant and reach the ordinary read even for a key that will then be refused for being revoked or expired, answering with the identical `ErrAuthenticationFailed` a wrong key gets. See `model.go`'s own `apiKeyHashIndex` doc comment for the full "why a new table, not the existing `(tenant_id, hash)` index" argument -- including why `migrations/{sqlite,postgres}/0001_create_integration_api_keys.sql`'s own comment on `uq_integration_api_keys_tenant_hash` (which assumed a future lookup would already know its tenant) is left unedited as an honest historical record rather than rewritten to match round 6's actual design.

## Soft deletion

`WebhookSubscription` implements `dbkit.SoftDeletable` (`DeletedAt *time.Time` / `DeletedBy string`, the exact field shape and tags `examples/reference-app/internal/notes.Note` established as the precedent, and that `go/org`'s `OrgNode`/`Membership` round and `go/rbac`'s `RoleBinding` round both reused). That flips `dbkit.Repository[WebhookSubscription].Delete` -- called by `Service.DeleteWebhookSubscription`'s `webhookRepo.Delete(ctx, id)` -- from a physical `DELETE` onto a mark-delete `UPDATE`, and gives the model a working `dbkit.Repository[WebhookSubscription].Restore`, which `Service.RestoreWebhookSubscription` wraps.

**`WebhookSubscription` alone, of this module's models.** `APIKey` is untouched: `Service.Revoke` already sets `RevokedAt` through an ordinary `repo.Update` call and never calls `Delete` at all, so it already followed the "mark, don't destroy" philosophy natively, with its own domain-specific state field, before this round existed -- adopting `SoftDeletable` on top of that would add a second, redundant deletion mechanism to the same row. `WebhookDelivery` is untouched too: it is a transient, append-only delivery-attempt log with no `Delete` operation anywhere in this module, the identical "nothing to retrofit" reasoning `go/rbac`'s round applied to `Role`/`RolePermission`. `DeleteWebhookSubscription`'s `webhookRepo.Delete(ctx, id)` call is this module's one real delete-shaped operation against a row without its own domain-specific revocation mark, exactly why this round targeted it and only it -- the identical reasoning `go/org`'s round applied to pick `OrgNode`+`Membership` over `org_invitations`, and `go/rbac`'s round applied to pick `RoleBinding` over `Role`/`RolePermission`.

**No partial-unique-index conversion needed, unlike `go/org`'s and `go/rbac`'s identical adoptions.** Both of those rounds needed one because the table they touched carried a real unique index a soft-deleted row would keep occupying, permanently blocking reuse of the same tuple. `integration_webhook_subscriptions` carries no unique index beyond its primary key -- `idx_integration_webhook_subscriptions_tenant_active` (`migrations/{sqlite,postgres}/0002_create_integration_webhook_subscriptions.sql`) is a plain, non-unique index, so a mark-deleted row occupying it reserves nothing another row would need. `migrations/{sqlite,postgres}/0004_add_soft_delete.sql` therefore adds only the two soft-delete columns, with no index change at all -- one less moving part than either precedent round needed.

**`DeleteWebhookSubscription`'s existing error handling and doc comment needed only a wording update, not a behavior change.** `dbkit.Repository[T].Delete` reports `dbkit.ErrRecordNotFound` on zero rows affected whether the branch taken is a physical `DELETE` or, now, a mark-delete `UPDATE` (`go/dbkit/repository.go`'s `Delete`/`softDelete`) -- the promoted method's contract is unchanged from `DeleteWebhookSubscription`'s point of view, and so is `webhook_delivery.go`'s `handleDeliveryJob`: its own `webhookRepo.FindByID` lookup on a delivery's subscription is hidden from a mark-deleted row by dbkit's soft-delete auto-scope plugin exactly as it was made impossible by a physical `DELETE` before this round, so an in-flight delivery enqueued before a subscription's deletion still resolves `dbkit.ErrRecordNotFound` and settles `DeliveryStatusDeadLetter` (`TestService_handleDeliveryJob_SubscriptionMarkDeleted_DeliveryRowUnaffected` pins this explicitly, alongside the pre-existing `TestService_handleDeliveryJob_SubscriptionDeleted_TerminatesWithoutRetry`). A later review round sharpened the delivery-error classification that rests on this: `handleDeliveryJob` now settles terminal ONLY on a genuine record-not-found (marked `"webhook subscription was deleted"` -- accurate for a mark-deleted row, unlike the earlier "no longer exists"), while any other subscription-lookup failure -- a transient store error, a secret that no longer decrypts -- is returned so `jobs` retries it, never dead-lettered as if the subscription were the problem (see `handleDeliveryJob`'s own doc comment and its `RetriesNotDeadLetters` regression test). What DID need a wording fix back then: `DeleteWebhookSubscription`'s own doc comment said "permanently removes", and `handleDeliveryJob`'s said "none will reappear by retrying" -- both true of a physical `DELETE`, both no longer quite true now that `RestoreWebhookSubscription` exists. Both comments are corrected in place; see webhook_service.go and webhook_delivery.go.

**The Restore-safety decision: Restore always forces `Active = false`, a deliberate divergence from `go/org`'s and `go/rbac`'s own "changes nothing but the two soft-delete columns" precedent.** Both of those modules' `Restore` methods change nothing about the restored row beyond `deleted_at`/`deleted_by` (`go/org/AGENTS.md`'s and `go/rbac/AGENTS.md`'s own "Soft deletion" sections, and `assign.go`'s `RestoreRole` doc comment, record the reasoning in their own domains). This round does not carry that precedent over unchanged, because the two situations are not the same hazard. `go/org`'s and `go/rbac`'s `Restore` calls resume a purely INTERNAL fact -- an organization membership, an authorization grant -- evaluated fresh against the rest of the system on every read, with no further action happening on its own. Restoring a `WebhookSubscription` with `Active` still `true` is different in kind: `webhook_delivery.go`'s `handleDomainEvent` fans out to every ACTIVE subscription automatically, on every matching domain event, entirely without further human action. An unconditional restore would therefore silently resume POSTing the tenant's live event data to an external, third-party URL nobody has looked at again since the subscription was deleted, however long ago that was and however stale its URL or receiving system might now be -- a real-world side effect leaving this process, not an internal state change this codebase can reason about the safety of the way it can for an org membership or an rbac grant. `Service.RestoreWebhookSubscription` therefore always sets `Active = false` after the promoted `Restore` call, regardless of what `Active` held at the moment of deletion (`TestService_RestoreWebhookSubscription_ForcesActiveFalse_EvenIfActiveAtDelete` and its PostgreSQL re-run, `TestPostgres_RestoreWebhookSubscription_UndoesTheDelete`, both seed an ACTIVE subscription specifically to pin this). This costs the caller exactly one extra explicit step -- an `UpdateWebhookSubscription` call setting `Active` back to `true` -- to resume delivery, which is the same "no implicit side effect on a structural edit" discipline this codebase already applies elsewhere (`go/org/AGENTS.md`'s identical framing for why `TreeService.Delete` does not re-parent orphans and does not cascade `Restore`). Beyond that one field, `RestoreWebhookSubscription` follows the established precedent exactly: it does not re-validate the row against `CreateWebhookSubscription`'s own preconditions (its URL is not re-run through `ValidateWebhookURL`, for instance) and reports the identical collapsed not-found signal `dbkit.Repository[T].Restore`'s own doc comment describes -- an id with nothing to restore and an id that exists but is not currently mark-deleted are indistinguishable, matching `go/org`'s `MemberService.Restore` and `go/rbac`'s `RestoreRole`.

**No cross-replica cache-invalidation concern, unlike `go/rbac`'s round.** `go/rbac`'s `RestoreRole` had to publish `EventRoleBindingRestored` and have `Module.Attach` subscribe to it, because rbac caches authorization decisions per subject and a stale cached denial on one replica would otherwise outlive the anti-loss TTL. This module caches nothing: `webhook_delivery.go`'s `handleDomainEvent` calls `webhookRepo.ListActiveByTenant` fresh, straight against the database, on every single matching domain event -- there is no in-memory decision cache anywhere in this module for a restore (or a delete, or an update) to invalidate. `Service.RestoreWebhookSubscription` therefore only needs `AuditActionWebhookSubscriptionRestore`, the audit-trail symmetry described below; no domain event is published, matching `CreateWebhookSubscription`/`UpdateWebhookSubscription`/`DeleteWebhookSubscription`'s own identical choice (this module publishes no domain event of its own on any webhook-subscription mutation, only audit records -- `module.go`'s `Register` subscribes to OTHER modules' events for fan-out, but never publishes any of its own).

**Audit symmetry.** `AuditActionWebhookSubscriptionRestore` (`"integration.webhook_subscription.restore"`) is emitted by `RestoreWebhookSubscription` through the same `emitWebhookAudit` helper `Create`/`Update`/`DeleteWebhookSubscription` already use, completing the create/update/delete/restore quartet `module.go`'s `auditActionDecls` declares.

**Known limitation.** `RestoreWebhookSubscription` does not re-validate the restored row's `URL` against `ValidateWebhookURL` -- a caller wanting that re-checked calls `UpdateWebhookSubscription` afterward (which does re-validate on any `URL` change), the same "call the ordinary mutation for a full re-check" answer `go/rbac`'s own known limitation gives for `RestoreRole`.

**A pre-existing PostgreSQL column-type bug, found and fixed by this round's own new integration tier.** `WebhookSubscription.Secret` stores CIPHERTEXT under `WebhookSecretSerializerName`, but the PostgreSQL migration set declared it `VARCHAR(512)` since round 2 first shipped it -- AES-256-GCM ciphertext is effectively random binary data, which is essentially never valid UTF-8 text, and PostgreSQL enforces its configured encoding on every `VARCHAR`/`TEXT` value. Writing a genuine encrypted secret through this column failed with `invalid byte sequence for encoding "UTF8"` the very first time `integration_test/postgres_softdelete_test.go` -- this module's first-ever PostgreSQL integration tier of any kind -- actually exercised it. This was never caught before because this module had no PostgreSQL integration tier until this round; the unit tier's SQLite database has no fixed column encoding to violate. `migrations/postgres/0005_fix_webhook_secret_column_type.sql` brings the column to `BYTEA`, matching `go/org`'s identical `Invitation.Email`/`EmailSerializerName` precedent (`BYTEA` on PostgreSQL, `BLOB` on SQLite) exactly. This is the first migration in this module -- and, as far as this round's own review of the repository found, the first in the whole codebase -- with no `sqlite/` sibling: SQLite's dynamic typing already stores arbitrary binary through a `VARCHAR`-declared column without error, so nothing on that dialect needed to change, and `dbkit.MigrationRegistry.Apply` reads each dialect's own subdirectory independently with no cross-dialect pairing requirement. `webhook_model.go`'s `Secret` field's `gorm` tag dropped its now-meaningless `size:512` hint accordingly (this codebase never uses `AutoMigrate`, so the tag never drove the actual column type either way). Bundled into this round rather than filed as a separate fix because it directly blocked this round's own mandated PostgreSQL integration tier (item 8) against the exact table this round already touches; `TestPostgres_DeleteWebhookSubscription_MarksInsteadOfPhysicallyRemoving` and `TestPostgres_RestoreWebhookSubscription_UndoesTheDelete` are the reproducing tests this bug fix ships with, per this repository's bug-fix-needs-a-test rule -- both genuinely fail against the pre-fix column type and pass after.

## Seams

**`PermissionLister` (mandatory for any non-empty `Scopes` request).** `Service.Create` calls it exactly once per request to validate that the requested `Scopes` are a genuine subset of the creator's own permissions right now. `go/rbac`'s `Authorizer.ListPermissions(ctx, sub Subject) ([]string, error)` is the real, already-shipped method this interface mirrors structurally -- no new `rbac` method was invented for this round; a host wires it with a one-line closure, spreading `rbac.Subject`'s two fields into plain parameters so this module needs no `rbac.Subject` value to call it. This module still does not import `go/rbac` for it, for the same reason `org.FeatureGate` and `rbac.SubtreeResolver` are structurally-typed seams rather than direct imports across the module boundary: `go/rbac` carries `dbkit`, `gorm` and its own migrations along with it, and every one of those becomes a cost every consumer of this otherwise storage-and-ratelimit-only module would pay just to reference one interface's shape. An empty `Scopes` request needs no lister at all -- there is nothing to check a request for zero scopes against -- so a `Module` built with no `WithPermissionLister` option can still issue scope-less keys; a non-empty request with none wired fails closed with `ErrPermissionListerUnavailable` rather than silently skipping the check.

**Why one-at-a-time, not a bulk check.** `go/rbac`'s public API has no "is this whole set of permissions held" call, only `ListPermissions` (list everything a subject holds) and, presumably, a single-permission check elsewhere in its `Authorizer` -- this round validates each requested scope by testing set membership against `ListPermissions`'s answer, which needs exactly one `rbac` call per `Create`/`Rotate` regardless of how many scopes are requested. If `go/rbac` grows a genuine bulk "are these N permissions all held" call in a later round, `validateScopes` in `service.go` is the one place that would change to use it.

**`MembershipChecker` (optional).** Answers whether a given user is still an active member of a tenant, the one fact `Service.List` needs for `APIKeySummary.CreatorLeft`. Unlike `PermissionLister`, this is a display convenience, not a security control -- nothing about whether a key authenticates, what it may do, or whether it keeps working depends on `CreatorLeft`'s value. A host that wires none simply never sees the flag raised: `List` reports `CreatorLeft` as `false` for every row, exactly as if every creator were still active. Membership is owned by whichever module actually tracks who belongs to a tenant -- `go/org`'s roster in this codebase, but nothing requires that; the design doc's own text only ever says "creator has left", never naming `org` or `authn` as the source of truth, so this is a structurally-typed interface exactly like `org.FeatureGate` and `rbac.SubtreeResolver`.

Both seams follow the `PermissionListerFunc`/`MembershipCheckerFunc` adapter shape `http.HandlerFunc` popularized, so a host wires a closure without declaring a named type.

**`EventMapping` / `WithEventMapping` (round 2, optional -- a `Module` with none declared simply never subscribes to anything).** See `eventmapping.go`'s own doc comment for the full design rationale; summarized here because it is this round's central design decision.

The design doc requires an explicit internal-event-to-public-schema mapping layer, populated by "a business module's own `Register` call". Implementing that literally would mean adding a field to `pkgcore.Registry` -- explicitly out of this round's scope (`pkgcore` is the dependency floor of the whole monorepo; widening its contract is a decision for whoever owns it, not a side effect of one module's round) -- or `go/integration` importing every business module directly, which the root `CLAUDE.md`'s module-boundary rule forbids outright. `WithEventMapping` is the third option: a `Module` construction-time `Option`, wired by the HOST (which already imports both the business module and `go/integration`, the one place in the whole program allowed to see across every module boundary at once), following the EXACT shape round 1's own `WithPermissionLister`/`WithMembershipChecker` already established for reaching a sibling module without an import edge. A business module's own `Register` call still contributes the CATALOG half of the mapping contract for free and unprompted: `pkgcore.EventDecl`'s own doc comment already says declaring an event is "a documentation and mapping contract" `integration` is meant to read; only the TRANSFORM function -- code, not data, closing over a payload shape `go/integration` may not import -- needs the host's hand, and only the host sits above both modules to provide it.

`WithEventMapping` must run before `Module.Register`, unlike `WithPermissionLister` (whose seam is read lazily, at call time): `Register` subscribes to every declared mapping's `InternalType` on `reg.Events` synchronously, during the one legal declaration phase (`reg.Events.Subscribe` performs no I/O, so this stays inside `Register`'s "must not perform I/O; it only declares" contract), and it can only subscribe to what `NewModule`'s options have already told it about.

**`WithWebhookURLValidator` / `WithWebhookHTTPClient` (round 4, optional -- a `Module` with neither declared gets `ValidateWebhookURL`'s and `newSafeHTTPClient`'s real, strict production behavior, unchanged).** Promoted from round 2's own unexported, this-module's-tests-only `withWebhookURLValidator`/`withHTTPClient` (`module.go`) once a real host -- the reference app, wiring round 2's surface end to end for the first time in round 4 -- found it had no other way to prove a genuine signed HTTP delivery against a receiver it actually controls: every receiver process this repository's own test infrastructure can stand up offline (an `httptest.Server`, or a sibling Docker container reached the way this repository's other Docker-backed integration tiers reach theirs) resolves to either loopback or RFC 1918 private address space, both of which `ValidateWebhookURL`'s real, unmodified SSRF check correctly refuses -- there being no such address for a real receiver a test controls, this is not a gap the module's own design left open by oversight, it is the SSRF protection working exactly as intended against the one class of address every offline receiver happens to have.

Both options must be wired TOGETHER, never one alone: `WithWebhookURLValidator` overrides only the creation-time check (`Service.CreateWebhookSubscription`/`UpdateWebhookSubscription`), while `WithWebhookHTTPClient` overrides only the dial-time transport a delivery attempt actually sends through (`Service.attemptDelivery`) -- overriding one without the other would either refuse the subscription before it is ever created, or accept it at creation time only to have every delivery attempt refused at dial time instead. This mirrors `go/authn`'s own identical precedent, `WithHTTPSMSSenderClient` (`go/authn/sms.go`), which exists for the exact same reason against its own SSRF-guarded SMS gateway client. A production host must never call either: doing so does not weaken any OTHER `Module`'s or any OTHER composition's SSRF enforcement (each `Module` instance carries its own `urlValidator`/`httpClient` fields, `module.go`'s `Module` struct), but it does weaken THIS instance's, permanently, for as long as that instance runs.

**`WithAuthenticationGuard` (optional, round 8 review fixes).** Wires the rate-limit guard `AuthMiddleware` applies to authentication attempts BEFORE any `Authenticate` call -- see the Rate limiting section's own "belongs OUTSIDE authentication" paragraph for why that ordering exists and what pre-auth `Extractor` identifiers mean. Read at call time like every other `Module` field its middleware consumes; a `Module` built without it behaves exactly as it always did.

## Rate limiting

`LayeredLimiter.Allow` evaluates the global, tenant and key layers in that fixed order, short-circuiting on the first denial: a request already known to be denied at the global layer never touches the tenant or key counters at all. A zero-value `ratelimit.Limit` on any of `LayeredLimits`' three fields disables that layer entirely (`Allow` never calls the underlying `Limiter` for it), so `LayeredLimits{}`'s zero value legally disables all three -- every request is allowed. This module never modifies `go/ratelimit` itself; the three layers are entirely a composition of its existing `Limiter.Allow(ctx, key, limit)` call, one per layer, matching the design doc's own layering description.

`HTTPGuard.Middleware` wraps an `http.Handler`, deriving the tenant and key dimensions from the request through a host-supplied `Extractor` function -- this module takes no position on how a host authenticates a request or resolves its tenant. A denied `LayeredDecision` becomes a 429 with `Retry-After` (whole seconds, rounded up per RFC 9110 §10.2.3), `X-RateLimit-Remaining` (hits left), `X-RateLimit-Layer`, and `X-RateLimit-Reset` -- which carries the instant the window resets as a Unix EPOCH timestamp in seconds (GitHub's semantics for that header name; the seconds-until-reset countdown has its own home in `Retry-After`); an error from the underlying limiter itself (a `KVStore` failure) becomes a 500, never a silent allow -- `go/ratelimit.Limiter`'s own doc comment is explicit that it decides no fail-open policy on the caller's behalf, and `HTTPGuard` honors that.

**Rate-limiting belongs OUTSIDE authentication.** `WithAuthenticationGuard` (a `Module` option; see Seams) lets `AuthMiddleware` run a guard over authentication attempts themselves, BEFORE any `Authenticate` call: a request bearing a forged `X-API-Key` otherwise reaches `Authenticate`'s lookups with no bound at all -- it is refused, but refused after paying for two database lookups, and never charged against any rate-limit budget, since a guard composed behind authentication never even sees it. The guard's `Extractor` runs pre-auth, so it derives no resolved tenant/key (empty identifiers are ordinary keys to `LayeredLimiter.Allow` -- see that type's own doc comment); a host may also derive identifiers from the request itself, authn's login-limit precedent being the model (attempt limits per identifier and per IP, checked before the credential is).

## Outbound webhook delivery

**The pipeline (`webhook_delivery.go`).** `Module.Register` subscribes `Service.handleDomainEvent` (through a `Module`-level forwarding wrapper the same shape `webhookDeliveryHandler` uses for the job side, both explained in "Register-time wiring, Attach-time Service" below) to every declared `EventMapping.InternalType`. On a matching event: skip if the event carries no tenant (mirroring `org.handleUserCreated`'s identical no-tenant skip); list the tenant's ACTIVE subscriptions whose `EventTypes` includes the mapping's `PublicType` (`matchingSubscriptions` -- a Go-side filter over a JSON column, never a SQL one, per this module's dual-dialect rule); run the mapping's `Transform` and wrap it in the versioned public envelope `{event: {type, version}, data: ...}` (`buildEnvelope`); for each matching subscription, create (or, on a redelivery, find) the `WebhookDelivery` row under its derived idempotency key and enqueue one `jobs.Task` (job type `integration.webhook.deliver`) per subscription. **`handleDomainEvent` NEVER returns a non-nil error** -- the in-memory `EventBus`'s `Publish` joins a subscriber's error back into the PUBLISHING module's own call (`pkgcore.memoryEventBus.Publish`'s own doc comment), so a webhook wiring problem here must never surface as a failure of, say, `org`'s own membership-creation call. Every failure is logged at `Warn` and swallowed, the identical resilience contract `org.handleUserCreated` documents for the identical reason.

**Delivery execution (`Service.handleDeliveryJob`, the `jobs.Handler` half).** One HTTP `POST` of the row's stored `Payload` to the subscription's `URL`, signed per "HMAC signing" below. A subscription deleted or paused after its delivery was enqueued is treated as terminal (`DeliveryStatusDeadLetter`, no retry) rather than retried against a receiver the tenant no longer wants, or one that no longer has a secret to sign against. A 2xx response settles the row `DeliveryStatusDelivered`; anything else -- a non-2xx status or a transport error -- settles it `DeliveryStatusFailed` and returns the error, which is `jobs`' own signal to retry.

**Retry and dead-lettering.** Enqueued with `jobs.WithMaxRetries(webhookMaxRetries)` (6, chosen deliberately larger than `jobs.DefaultMaxRetries`'s 3 -- a webhook receiver is far more likely to be down for a transient few minutes than an in-process handler is). `jobs.StandaloneQueue` (or the distributed mode's asynq-backed queue) owns the exponential backoff and the retry-count bookkeeping entirely -- this module adds none of its own, per `go/jobs`'s own retry contract. Once retries are exhausted, `jobs` calls this module's `jobs.FailureHook.OnFailure` (`Service.onWebhookDeliveryDeadLetter`, wrapped the same way `handleDeliveryJob` is), which settles the row `DeliveryStatusDeadLetter` -- the root `CLAUDE.md`'s "the queue offers an `OnFailure` hook ... belongs to the business module" rule, applied here as simply recording the terminal state.

**HMAC signing (`webhook_signature.go`).** Every delivered request carries `HeaderWebhookID` (the delivery's own id, for a receiver's own dedupe), `HeaderWebhookTimestamp` (Unix seconds), and `HeaderWebhookSignature` (`"v1=" + hex(HMAC-SHA256(secret, "<timestamp>.<raw body>"))`). The timestamp is signed INSIDE the covered content, not merely alongside it as an unsigned header -- see `webhook_signature.go`'s own file comment for why that is what makes replay rejection possible at all (a captured genuine pair cannot be replayed under a forged, later timestamp). `WebhookReplayTolerance` (5 minutes) is exported purely as documented guidance for a receiver's own verification code; this module signs and stamps, and has no receiving side of its own to enforce the tolerance against.

**SSRF protection, at two different times (`ssrf.go`).** `ValidateWebhookURL` runs at subscription creation and update time: refuses a malformed URL, a disallowed scheme, or one resolving to any loopback/private/link-local/multicast/CGNAT address (`isBlockedIP`) -- the mandatory proof (`ssrf_test.go`'s `TestValidateWebhookURL_PrivateIPLiteral_Blocked`) is exactly the private-IP-refused test this round's own instruction called non-negotiable. `newSafeHTTPClient`'s `Transport.DialContext` re-validates the CONNECTING address on every delivery ATTEMPT, dialing the specific IP it just checked directly rather than trusting a second, independent hostname resolution -- this is what actually defeats DNS rebinding (a URL whose DNS answer changed between subscription creation and delivery), which a creation-time-only check cannot. It also automatically covers HTTP redirects, since Go's `http.Client` re-dials through this same guarded `DialContext` for a redirect's `Location`.

**Fan-out idempotency.** `deriveWebhookDeliveryKey` hashes `(subscriptionID, publicType, publicVersion, renderedBody, occurrenceAt)`; `handleDomainEvent` probes for an existing `WebhookDelivery` under that key before creating a new one, and `uq_integration_webhook_deliveries_tenant_subscription_key` is the database-level backstop for two concurrent handlers racing the same event. `jobs.Task.IdempotencyKey` is set to the identical key as a second, independent line of defense at the queue layer. `occurrenceAt` is the fan-out's occurrence marker: one reading of the service clock, stamped at the subscription boundary per event arrival. It is what stops the dedupe from silently dropping a genuinely NEW occurrence whose public body is byte-identical to an older one's (a member removed and later re-added, when the mapping's payload names only the member) -- a lost delivery a receiver can never recover. The trade: `pkgcore.Event` carries no occurrence identity of its own (no id, no timestamp), so a redelivery observed at a later clock reading is treated as a distinct occurrence and delivered again -- at-least-once behavior receivers dedupe against through `HeaderWebhookID` -- while a same-instant redelivery still merges. See `deriveWebhookDeliveryKey`'s and `enqueueDelivery`'s own doc comments.

**Register-time wiring, Attach-time `Service`.** `Module.Register` must subscribe to events and register the job handler before a `Service` exists (`Service` is built in `Attach`, which round 1 already established runs strictly after `Bootstrap` returns). Both `Module.handleDomainEvent` and `webhookDeliveryHandler` (a small `jobs.Handler`/`jobs.FailureHook` adapter type) are `Module`-level forwarding wrappers: they read `m.service` at CALL time (which, for real traffic, is always after `Attach` has run) and log-and-swallow (event side) or return a retryable error (job side) on the rare case they run before it.

## Reference app: mandatory first consumer (round 2's webhook surface)

**Round 4 retires round 2's own "no reference-app consumer yet" exception, for round 2's outbound-webhook surface only** -- per the root `CLAUDE.md`'s mandatory-first-consumer rule. `examples/reference-app/cmd/server/webhooks.go` is the host-side wiring: `orgMemberJoinedWebhookMapping` declares a real `EventMapping` over `go/org`'s real, already-shipped `org.member.joined` event -- the identical event `webhook_example_test.go`'s own `ExampleModule_webhookDelivery` already mapped offline (see that entry's own history below) -- `wireIntegrationWebhooks` mounts the module's one hand-written demo route (`POST /api/v1/demo/webhooks/subscriptions`, gated on `integration.PermissionWebhookManage`; see that route's own doc comment for why it calls `rbac.Authorizer.Can` directly rather than through `rbac.RequirePermissionFunc`'s convenience wrapper -- a genuine shape mismatch this round's first real HTTP consumer of this module's own three-segment permission names exposed, `splitPermission`'s one-colon `"<resource>:<action>"` contract in `go/rbac/middleware.go` never having been driven against one of THIS module's names before), and `cmd/server/server.go` wires `integration.NewModule` with that mapping and the app's shared `jobs.StandaloneQueue`. Round 7 retired the route half of this wiring: once the webhook-subscription-CRUD surface was itself spec-generated HTTP (see "In scope round 7" above), the hand-mounted demo route's only remaining job -- letting a demo tenant create a subscription over HTTP -- belonged to that surface, `wireIntegrationWebhooks` was deleted, and `webhooks.go` shrank back to its `EventMapping` machinery alone (its package doc records the retirement). The shape-mismatch lesson that route's own "Why this calls az.Can directly" doc comment recorded was not retired with it: round 7's gate for the spec-generated webhook surface is the identical fix, generalized to both permission pairs (see "Reference app: mandatory first consumer (round 7's webhook-subscription-CRUD surface)" below).

Round 7 also re-points the proof below's subscription-creation leg: `webhook_flow_test.go`'s shared `createWebhookSubscription` helper (and the `webhookBasePath` constant it posts to) now targets the spec-generated surface's own mount, `/api/v1/integration/webhooks` -- carrying the camelCase `eventTypes` body and this app's `X-Demo-User`/`X-Demo-User-Id` attribution headers the round-7 section below describes -- so both of the delivery legs that follow create their subscriptions through the same mounted CRUD surface any tenant administrator would use, exactly the call the demo route round 7 retired used to serve.

`examples/reference-app/cmd/server/webhook_flow_test.go` is the round's own acceptance proof, in two legs:

1. **A real signed delivery** (`TestWebhookFlow_RealSignedDelivery_EndToEnd`). A demo tenant subscribes to `org.member.joined`; a real invitation is created and accepted through `go/org`'s own composed HTTP routes -- the identical flow `org_flow_test.go` itself drives -- and the resulting `MemberJoined` event fans out through this module's real pipeline to a real receiver process the test controls. The test independently re-derives the HMAC-SHA256 signature from the subscription's own secret (never calling this module's own signing code), compares it to the delivered request's own header, decodes the versioned public envelope and compares its fields against the real membership the HTTP accept call itself returned, and confirms `invitation_id` is genuinely absent from the delivered payload.
2. **A real retry-then-dead-letter proof** (`TestWebhookFlow_ReceiverNonSuccess_RetriesThenDeadLetters`). A second subscription points at a receiver that always answers a non-2xx status. The test observes exactly `1 + webhookMaxRetries` (7) real HTTP attempts land -- every one of them independently re-verified as correctly signed, not merely counted -- and then observes no 8th attempt within a further window, proving `jobs`' own bounded retry horizon genuinely exhausts and dead-letters rather than retrying forever or silently succeeding. This leg runs for real wall-clock time (`jobs.StandaloneQueue`'s own exponential backoff, unmodified), roughly a minute, by deliberate choice: the alternative would be a mocked or shortened retry schedule that no longer proves THIS module's own real `jobs`-backed retry behavior.

Both legs need `cfg.WebhookURLValidator`/`cfg.WebhookHTTPClient` (`server.go`'s `serverConfig`, plumbed to `integration.WithWebhookURLValidator`/`WithWebhookHTTPClient` only when the test sets them) -- see "Seams" above for why this module could not have proven either leg any other way, and why this is additive to the ONE `Module` instance this test itself builds, never a weakening of this module's real, unmodified default behavior in this test or any other: every other test in the reference app's own suite, and every production boot, leaves both fields unset.

Round 1's API-key surface was, at the time round 4 shipped, a SEPARATE obligation round 4 did not discharge. Round 5 discharged it -- see "Reference app: mandatory first consumer (round 1's API-key surface)" immediately below.

## Reference app: mandatory first consumer (round 1's API-key surface)

**Round 5 retires round 1's own "no reference-app consumer yet" exception** -- per the root `CLAUDE.md`'s mandatory-first-consumer rule -- and, per that same file's public-API-is-frozen rule, freezes round 1's public API for real: `Service.Create`/`List`/`Rotate`/`Revoke`'s signatures, and every exported type this round's `Handler`/`api` package depend on, are no longer the "not frozen, may still break" surface rounds 1 through 4 left them as. Unlike round 2's webhook surface (discharged through a hand-mounted demo route, `webhooks.go`, because no OpenAPI fragment of its own existed for it at the time -- round 7 has since opened one and retired that route, see "In scope round 7" above), round 1's surface is discharged through the REAL thing: a spec-generated HTTP surface mounted through this app's own generic, no-per-module-exception route-mounting machinery.

**The HTTP surface (`api/openapi.yaml`, `api/integration-server.gen.go`, `handler.go`) -- see "In scope round 5" above for what each file adds.** `Handler` holds a reference to `*Module` itself, not to a `*Service` -- a deliberate departure from org's, storage's, notification's, sharing's and admin's own `NewHandler` shape, all of which build `Handler` directly from concrete services `NewModule` already built. This module's own `Service` is built in `Attach`, which runs strictly after `Bootstrap`'s `Register` phase completes across every module (round 1's own established shape, unchanged by this round) -- too late for a route `reg.Routes.Mount` needs mounted NOW, during `Register`. `Handler` therefore reads `m.service` AT CALL TIME, the identical forwarding-wrapper technique `handleDomainEvent` and `webhookDeliveryHandler` already use for the identical reason (see `module.go`'s "Register-time wiring, Attach-time `Service`" section and `handler.go`'s own "Round-1-only, and built differently" doc comment for the full argument) -- a Handler served before `Attach` has run answers a coded internal error rather than panicking on a nil `Service`, pinned by `TestHandler_ServedBeforeAttach_WritesInternalError`.

**The permission-gate mismatch this round's first real HTTP consumer of round 1's own permission names exposed, and how it was resolved.** `examples/reference-app/cmd/server/demo_subject.go`'s `guardModuleRoute` -- the generic gate every OTHER mounted fragment goes through -- wraps a route in `rbac.RequirePermissionFunc`, which splits a permission string on its FIRST colon (`splitPermission`, `go/rbac/middleware.go`) and refuses outright, regardless of what the caller holds, when the resulting action half contains a SECOND colon. `integration.PermissionRead`/`PermissionManage` are declared `"integration:apikey:read"`/`"integration:apikey:manage"` -- exactly that shape -- so `rbac.RequirePermissionFunc` cannot gate this fragment's routes AT ALL, not even with a custom per-request permission selector (unlike `go/pki`'s own `pkiPermissionFor`, which only needed a custom SELECTOR because `pki:read`/`pki:revoke` are ordinary one-colon strings). This is the identical shape mismatch round 4's own `wireIntegrationWebhooks` route already found and worked around, for this module's OTHER permission pair, by calling `rbac.Authorizer.Can` directly instead of going through `rbac.RequirePermissionFunc` at all (`webhooks.go`'s own "Why this calls az.Can directly" doc comment). `demo_subject.go`'s new `guardIntegrationAPIKeyRoute` applies the identical fix as a reusable router-level gate instead of an inline per-route check, since this fragment's routes are mounted through the generic `mountModuleRoutes` loop (`reg.Routes.Routes()`) rather than hand-mounted one at a time the way `wireIntegrationWebhooks`'s own demo route is: it derives resource and action by cutting `integrationAPIKeyPermissionFor`'s answer at its LAST colon (`"integration:apikey"`, `"read"`/`"manage"`) and calls `az.Can` with them directly, never through `rbac.Permission`/`splitPermission`'s one-colon round trip. `TestBuildServer_APIKeyPermissionGate_EnforcesTheAPIKeyPermissions` is this round's own proof that the gate genuinely evaluates a real permission rather than silently passing every request through: the read-only demo user (holding `notes:read` and nothing else) is refused both directions, the owner (holding every permission any module declared) passes both, and a request naming no identity at all is refused too.

**`integration.SubjectResolver` and `WithSubjectResolver` (`seams.go`, `module.go`) -- the identical structurally-typed, no-import shape `org.SubjectResolver` and `notification.SubjectResolver` already establish.** Read only by `integration_createAPIKey`, to resolve the caller who becomes a new key's `CreatedBy` -- `list`, `rotate` and `revoke` need no caller identity at all: `List` reads only the tenant, and `Rotate`/`Revoke` resolve their own `CreatedBy` from the EXISTING key row (`Service.Rotate` carries the predecessor's own `CreatedBy` forward), never from whoever is calling the HTTP endpoint. `examples/reference-app/cmd/server/server.go` wires the identical `demoOrgSubjectResolver{}` instance org's and notification's own wiring already share -- a third compile-time check (`var _ integration.SubjectResolver = demoOrgSubjectResolver{}`) confirms it satisfies this seam too with no adapter code of its own.

**`examples/reference-app/cmd/server/apikey_flow_test.go` is this round's own acceptance proof**: `TestBuildServer_APIKeyFlow_CreateListRotateRevoke_EndToEnd` drives create (capturing the plaintext key, decoded through a raw JSON map for the list assertion below so a struct that simply never declared a field could not hide a real regression) → list (exactly the one key just created, with neither `key` nor `hash` present anywhere on the wire) → rotate (a brand-new id and key; the predecessor lists as revoked, the replacement as live; rotating the now-revoked predecessor again reports the already-revoked conflict) → revoke (204, then a second revoke reports the already-revoked conflict, and revoking an id that never existed reports not-found -- the two refusals are distinct codes, never conflated), entirely through the composed HTTP stack -- real `authn` registration and sign-in, real `tenancy`/`rbac` middleware, this module's real `Service` underneath. `TestBuildServer_APIKeyPermissionGate_EnforcesTheAPIKeyPermissions` is the gate proof described above.

**What this proof did NOT, and could not yet, demonstrate -- historical, discharged by round 6.** "Rotate" and "Revoke" above were proven only as row-state transitions -- the predecessor's `Revoked`/`RevokedAt` reading back true through the very same session-authenticated `list` call a tenant administrator makes to manage their own keys. No test in this codebase used a captured raw key value as a bearer credential against any endpoint, because no such endpoint existed: `go/integration` shipped no `Authenticate`/`Verify` method, so there was no lookup path anywhere in this codebase that could ever be fed a presented key to refuse. Concretely, the property "a request bearing a rotated-away or revoked key is refused" was unverifiable at the time -- a real, then-undischarged testing gap relative to that specific claim, though a deliberate, documented scope boundary rather than a regression: round 1's own design explicitly deferred request authentication to a later round. **Round 6 is that later round**: `go/integration/apikey_authenticate_flow_test.go`'s own `TestService_Authenticate_RotatedAwayKey_ReportsAuthenticationFailed` proves it at the `Service` level, and `examples/reference-app/cmd/server/apikey_authenticate_flow_test.go`'s `TestBuildServer_APIKeyAuthenticateFlow_RotateAndRevokeRefuseTheOldKey` proves the identical property against the real composed HTTP stack -- see "Reference app: mandatory first consumer (round 6's inbound-authentication surface)" immediately below.

**Why `HTTPGuard`/`LayeredLimiter` stayed unwired in front of this surface through round 5** -- see "In scope round 5" above for the full reasoning: they rate-limit a request authenticated BY one of these keys against a host's own inbound API surface, a materially different thing from a tenant administrator managing their own keys through their own ordinary session. Wiring them in front of THIS CRUD surface would still conflate the two, and round 6 does not do that -- what round 6 wires the two in front of is a DIFFERENT, new surface entirely (the round-6 section below).

## Reference app: mandatory first consumer (round 6's inbound-authentication surface)

**Round 6 discharges the specific, documented gap round 5's own review left open** -- see that section's own "What this proof did NOT, and could not yet, demonstrate" paragraph just above, and `keygen.go`'s `hashAPIKeyToken` doc comment, both now updated to record round 6 as the round that closed it.

**`Service.Authenticate` (`authenticate.go`) and `AuthMiddleware` (`middleware.go`) -- see "In scope round 6" above for what each adds.** The one design choice worth restating here: unlike Create/List/Rotate/Revoke, `Authenticate` takes NO tenant from `ctx` and requires none -- it resolves the tenant itself, from the presented key's hash alone, through the new `apiKeyHashIndex` platform-data table (see "Round 6's platform-data table" above), mirroring `go/sharing`'s `Service.AccessPublic`/`tenantForTokenHash` mechanism for the identical reason: a genuinely inbound, API-key-authenticated request carries no separate tenant claim of its own, the same situation an anonymous share-link visitor is in.

**The header convention: `X-API-Key`, not `Authorization: Bearer` -- a considered choice forced by a real, verified-in-tree conflict, not an arbitrary preference.** `middleware.go`'s own `HeaderAPIKey` doc comment has the full argument; summarized here because it is this round's other central design decision: `go/authn.Middleware` is mounted as the OUTERMOST layer of every route the reference app serves, and its own doc comment states its contract for a present-but-unverifiable bearer credential plainly -- "A token that does not verify: 401 immediately." An `sk_...` API key is not a JWT `go/authn`'s `Verifier` could ever accept, so reusing `Authorization` for it would 401 every API-key-authenticated request at `go/authn.Middleware`, before `AuthMiddleware` -- mounted below it in the chain -- ever ran at all. `X-API-Key` sidesteps this entirely: `go/authn.Middleware` only inspects `Authorization`, so a request carrying `X-API-Key` and no `Authorization` header proceeds through it with no `Principal` (its own documented "no Authorization header" case) and reaches this module's own `AuthMiddleware` exactly like a genuinely public, tenant-less request needs to.

**The tenancy-allowlist requirement, mirroring `sharing.PathAccess` exactly.** Because `AuthMiddleware`, not `tenancy.Middleware`, is what resolves this route's tenant, the route must be added to `tenancy.Middleware`'s own `WithAllowlist` set (`server.go`'s middleware-chain composition) -- without it, `tenancy.Middleware` would refuse every request here with `tenancy.tenant_unresolved` before `AuthMiddleware` ever got the chance to resolve a tenant from the presented key, the identical reasoning `sharing.PathAccess`'s own allowlist entry already documents there.

**The demo route (`examples/reference-app/cmd/server/integration_authenticate.go`).** `integrationWhoamiPath` (`GET /api/v1/demo/integration/whoami`) is a minimal, deliberately-labeled demo endpoint -- explicitly NOT a real product feature, per round 6's own instruction -- gated by the composed chain `integration.AuthMiddleware -> integration.HTTPGuard.Middleware -> whoami handler`: `AuthMiddleware` runs first so `HTTPGuard`'s own `Extractor` can read the tenant and key `Authenticate` already resolved out of request context, rather than re-deriving either itself. This is the concrete realization of the design intent round 5's own "In scope" entry documented but could not yet build against ("these rate-limit requests authenticated BY an API key against a host's own inbound API surface, a surface this module still does not build") -- `integrationWhoamiLimits` (`LayeredLimits{Global, Tenant, Key}`) are real, non-zero limits backed by `reg.KVStore()`, this app's own resolved `KVStore` seam. The route carries no `rbac.Authorizer` dependency at all and needs none: unlike `wireIntegrationWebhooks`'s or the spec-generated CRUD surface's own session-authenticated, permission-checked management actions, this route IS the authentication layer for a different kind of caller entirely (a script or third-party system holding nothing but the API key itself) -- `Authenticate`'s own refusal is the whole of this route's access control.

**`examples/reference-app/cmd/server/apikey_authenticate_flow_test.go` is this round's own acceptance proof**: `TestBuildServer_APIKeyAuthenticateFlow_RotateAndRevokeRefuseTheOldKey` drives create (through the ordinary session-authenticated CRUD surface) → authenticate against `integrationWhoamiPath` with the fresh raw key (succeeds, resolving the right tenant and key id) → authenticate with no credential at all (refused) → authenticate with a never-issued key (refused, identically) → rotate (through the ordinary CRUD surface) → authenticate with the OLD raw key (refused -- THE property round 5's own review could not verify) → authenticate with the NEW key (succeeds) → revoke (through the ordinary CRUD surface) → authenticate with the now-revoked key (refused, identically to every other refusal reason) -- entirely through the composed HTTP stack, sharing `apikey_flow_test.go`'s own `apikeyRequest`/`decodeCreatedAPIKey` helpers for the CRUD half and never importing `go/integration` itself, matching that file's own wire-shape discipline.

## Reference app: mandatory first consumer (round 7's webhook-subscription-CRUD surface)

**`examples/reference-app/cmd/server/webhook_crud_flow_test.go` is round 7's own mandatory-first-consumer proof**, driving all six operations of the fragment's webhook half through the composed HTTP stack -- the compensation "In scope round 7" above names for the round-2 deferral the Known limitations section below records as discharged. Three tests, each proving what its name says:

- **`TestBuildServer_WebhookPermissionGate_EnforcesTheWebhookPermissions`** is this surface's mirror of round 1's own apikey permission-gate test (`apikey_flow_test.go`): the read-only demo user -- holding `notes:read` and deliberately no `integration:webhook:*` permission at all -- is refused 403 `rbac.permission_denied` across BOTH directions of `guardIntegrationRoute`'s gate on every operation class the fragment ships: list and the deliveries listing (which `integrationPermissionFor` gates on `integration:webhook:read`) and create, update, delete and restore (gated on `integration:webhook:manage`). A request with no identity at all -- a bearer token naming a tenant, but no demo header, and no principal `demoSubjectResolver` can map to a role -- is refused identically: there being no built-in role for a request the gate cannot attribute to anyone, this always denies rather than defaulting to any grant. This is also this round's own proof that the router-level gate genuinely evaluates a real permission per sub-path rather than method alone: a router bug that gated every `/webhooks` request on the API-key pair -- or passed everything through -- would make this the test that fails.

- **`TestBuildServer_WebhookCRUD_CreateListUpdateDeleteRestore_EndToEnd`** walks the full lifecycle the round-2 deferral names: create (201, the raw signing secret shown exactly once, `createdBy` attributed through the `integration.SubjectResolver` seam -- this app's `demoOrgSubjectResolver` reading the `X-Demo-User-Id` header -- never a request field), list (200, secret-free, verified at the raw-JSON level so a decode struct that simply omitted the field could not hide a regression), update (a partial PATCH: a present-but-empty `eventTypes` refused 400 `integration.event_types_required`, a `url`-only PATCH leaving `eventTypes` and `active` untouched, pause with `active=false`), the collapsed not-found refusals (an id that never existed, restore against a subscription that is not currently deleted, and -- after delete -- update and a second delete against the now-hidden id all answering the one 404 `integration.webhook_subscription_not_found`), delete (204; the mark-delete vanishing from every read, the post-delete list answering zero rows), restore (204; the row returns but ALWAYS `Active = false` whatever `active` held at deletion, round 3's Service rule carried onto the wire) and the explicit re-activation PATCH that resumes delivery.

- **`TestBuildServer_WebhookCRUD_DeliveriesList_RecentDeliveredRow`** proves the recent-deliveries listing against a REAL delivery through round 2's pipeline -- the same receiver rig `webhook_flow_test.go`'s own delivery proofs drive: exactly one row in the delivered-row vocabulary (status `delivered`, attempts and `lastStatusCode` from the receiver's 200, the delivery and last-attempt timestamps set, and no payload bytes anywhere), while a `subscriptionId` that names nothing answers an empty list rather than a 404 -- the no-enumeration signal the spec's own description of the operation pins, a caller able to tell "no such subscription" from "zero deliveries" being able to probe another tenant's subscription ids.

**The gate generalization the host side needed** (`examples/reference-app/cmd/server/demo_subject.go`): round 4's hand-mounted demo route is retired -- `webhooks.go` shrank back to its `EventMapping` machinery alone, its package doc recording the retirement -- and the guard that route used to carry grew into the reusable middleware the spec-mounted fragments need, since those routes arrive through the generic `mountModuleRoutes` loop rather than hand-mounted one at a time. Round 5's `guardIntegrationAPIKeyRoute` became `guardIntegrationRoute`, still reproducing `rbac.RequirePermissionFunc`'s fail-closed shape by hand rather than calling it, for the reason `integrationRouteSentinel`'s own doc comment gives in full: this module's permission strings do not fit `RequirePermissionFunc`'s `<resource>:<action>` contract at all. `integrationPermissionFor` picks the permission a request must hold, and the gate cuts its answer at the LAST colon into `resource` and `action` ("integration:apikey" and "read"/"manage", or "integration:webhook" and the webhook pair's own two verbs) -- the identical split round 4's demo route hand-derived. Round 7's change is confined to `integrationPermissionFor`'s sub-path dispatch between the module's two permission pairs, which both halves of the mounted fragment share the one mount path `integrationRoutePath` over: a request whose path continues `/webhooks` is round 7's webhook-subscription surface, its GET/HEAD reads gated on `integration:webhook:read` and its four mutations (create, update, delete, restore) on `integration:webhook:manage`; anything else under the mount is round 5's API-key surface, gated on `integration:apikey:read`/`integration:apikey:manage` unchanged. `writeIntegrationError` and `integrationErrInternal`, which round 4's `webhooks.go` had declared next to its hand-mounted subscription route, moved into `demo_subject.go` with the rest of the gate glue (the same writer round 6's `integrationWhoamiPath` handler already shares).

**The gate and the handler's own attribution stay on deliberately different headers.** The gate reads `X-Demo-User` through `demoSubjectResolver` -- a verified authn `Principal` second, otherwise denial -- to decide WHO may act; the handler-side `CreatedBy` attribution goes through the module's own `integration.SubjectResolver` seam (this app's `demoOrgSubjectResolver`), which reads `X-Demo-User-Id` alone. A create that passes the gate but carries no `X-Demo-User-Id` is refused 401 `integration.subject_unresolved` by `integration_createWebhookSubscription`, never attributed to a default user -- the round's flow tests always send both demo headers, so they never exercise that leg themselves; `webhook_flow_test.go`'s `createWebhookSubscription` doc comment records the shape. Every request helper this round drives -- `webhookCRUDRequest` (this file) and `createWebhookSubscription` (`webhook_flow_test.go`, now POSTing to `webhookBasePath`, `/api/v1/integration/webhooks`, where it used to drive round 4's retired demo route) -- sends exactly that pair, and every response decodes through structs that mirror the JSON on the wire, camelCase exactly as the fragment declares it, never into `go/integration/api`'s generated types -- the same wire-shape discipline `apikey_flow_test.go`'s own test structs follow, and this file never imports `go/integration` itself.

## Known limitations

- **No inbound request authentication -- DISCHARGED by round 6.** Rounds 1 through 5 issued, listed, rotated and revoked keys, but nothing verified an incoming request's presented key against the stored hash, and `LastUsedAt` was never written. Round 6's `Service.Authenticate` (`authenticate.go`) and `AuthMiddleware` (`middleware.go`) are exactly that lookup path -- see "In scope round 6" and "Reference app: mandatory first consumer (round 6's inbound-authentication surface)" above for the full record, including the reference app's own end-to-end proof that a rotated-away or revoked key is genuinely refused as a bearer credential. `Authenticate` also now writes `LastUsedAt` best-effort on every successful call. This bullet is kept, marked discharged, rather than deleted, so a reader who remembers this limitation from an earlier round can find where it was closed rather than wondering whether it silently regressed.
- **`Rotate` is not atomic.** The new key's `Create` and the predecessor's `Revoke` are two separate database writes, not one transaction -- `dbkit.Repository[T]` exposes no cross-call transaction seam a business module can reach. A failure between the two leaves two live keys rather than zero, a safe-direction surplus (never a lockout) that `Rotate` reports to its caller rather than silently swallowing. See `service.go`'s `Rotate` doc comment for the full argument.
- **No expiry-sweep job.** `idx_integration_api_keys_tenant_expires_at` exists now; nothing in this round enqueues or runs a sweep against it. This module does not depend on `go/jobs` yet.
- **`MaxAPIKeyLifetime` is a package-level constant, not a dynamic configuration item.** Reading one from `go/config` would add a dependency edge this module's position in `docs/internal/01-architecture.md`'s graph does not have, paid for by every consumer that boots this module without config. A host that genuinely needs a different ceiling is a later round's `WithMaxAPIKeyLifetime` option.
- **Round 2's webhook-SUBSCRIPTION-CRUD surface had no mounted HTTP surface and no reference-app consumer of its own -- DISCHARGED by round 7.** Round 7 mounted the CRUD surface as the fragment's webhook half (`api/openapi.yaml`, six operations under `/api/v1/integration/webhooks`, see "In scope round 7" above) and `examples/reference-app/cmd/server/webhook_crud_flow_test.go` is the reference-app consumer proof driving all six through the composed HTTP stack (see "Reference app: mandatory first consumer (round 7's webhook-subscription-CRUD surface)" above). Round 1's API-key surface no longer belongs in this bullet either -- round 5 gave it both an HTTP surface and a reference-app consumer, and froze its public API. Round 2's own delivery PIPELINE was never affected by any of these statements -- see "Reference app: mandatory first consumer (round 2's webhook surface)" above for what that half already has (round 7 retired the hand-mounted demo route that half's own tests used to subscribe through, re-pointing their helper at the new spec surface). This bullet is kept, marked discharged, rather than deleted, so a reader who remembers this deferral from round 2's own record can find where it closed.
- **`RestoreWebhookSubscription` does not re-validate the restored row.** See "Soft deletion" above's own Known limitation.
- **No manual redelivery.** `DeliveryStatusDeadLetter` is a real, observable terminal state (`Service.ListRecentWebhookDeliveries`), but nothing in this round re-enqueues a specific dead-lettered delivery. See the scope table above for why the row shape already anticipates it.
- **A dead-lettered delivery is not distinguished from a "the subscription was deleted/paused" early refusal in `Status` alone.** Both settle as `DeliveryStatusDeadLetter` (see `handleDeliveryJob`'s own doc comment for why reusing the one terminal-failure status was chosen over inventing a third) -- an operator tells them apart by `LastError`'s text, which is not a stable, matchable code. A later round that needs to distinguish them programmatically will want a real code here instead of free text.
- **A delivery's `Payload` is computed once, at fan-out time, from whatever the `EventMapping.Transform` produced then.** If a later release changes what a `Transform` returns for the SAME `PublicVersion` (a bug fix within a version, not a genuine schema break), an already-enqueued-but-not-yet-delivered row still sends the OLD rendering on retry. This is intentional -- an HMAC signature must cover a byte-for-byte stable body across every retry of one delivery -- but is worth knowing before "just retry it" is assumed to pick up a mapping fix.
- **Delivery is not exactly-once on the send side.** A crash between the receiver accepting the request and this module's own `Update` call recording `DeliveryStatusDelivered` leaves the row `DeliveryStatusPending`/`Failed`, and the next retry re-sends -- a real, if narrow, double-delivery window, the same class of gap `go/notification`'s own delivery pipeline documents for its identical settle-after-send shape.
- **No per-tenant or per-subscription delivery-volume rate limiting.** Unlike round 1's own three-layer `LayeredLimiter` for inbound API traffic, nothing in this round bounds how many webhook deliveries one tenant's event volume can generate. Not named in this round's own instruction; a later round's concern if a runaway event source ever makes it one.

## Testing

- **Unit tests**: SQLite only, no Docker required. `go/integration/internal/testutil` provides `NewSQLite`/`NewPostgres`/`Migrate`, mirroring `go/pki/internal/testutil`'s exact shape, so every test in this module applies the real, versioned migration files from zero rather than an `AutoMigrate` or a hand-written `CREATE TABLE`.
- **Integration tests (round 3, new; re-verified round 6)**: `integration_test/postgres_softdelete_test.go`, tagged `integration` (`go test -tags=integration ./integration_test/...`), Docker required. Applies this module's migration set -- 0001 through round 6's own `0006` -- against a real `postgres:16-alpine` container from zero, re-proving `TestPostgres_DeleteWebhookSubscription_MarksInsteadOfPhysicallyRemoving` and `TestPostgres_RestoreWebhookSubscription_UndoesTheDelete`/`...LiveSubscription_ReturnsNotFound` on the dialect whose strict encoding is what round 3's own `0005_fix_webhook_secret_column_type.sql` bug fix needed a real server to surface at all -- the SQLite unit tier could not have caught it. Round 6's own `0006_create_integration_api_key_hash_index.sql` was re-run against this same tier to confirm it applies cleanly on both dialects; it needed no dialect-specific column type (two plain `VARCHAR` columns), so it introduced no new bug of the kind round 3's own migration did.
- **Reference app (round 4, new)**: `examples/reference-app/cmd/server/webhook_flow_test.go` is round 2's own outbound-webhook surface's mandatory-first-consumer proof -- see "Reference app: mandatory first consumer (round 2's webhook surface)" above. It is not part of this module's own `go test ./...`/CI leg (it lives in, and runs with, the reference app's own test suite), but it is the one place this module's real signed-delivery and real retry/dead-letter behavior is proven end to end through an actual composed HTTP application rather than this module's own white-box unit tests.
- **Reference app (round 5, new)**: `examples/reference-app/cmd/server/apikey_flow_test.go` is round 1's own API-key surface's mandatory-first-consumer proof -- see "Reference app: mandatory first consumer (round 1's API-key surface)" above. Same "not part of this module's own CI leg, lives with the reference app's own test suite" shape as `webhook_flow_test.go`. Its rotate/revoke assertions are row-state proofs only (the predecessor reads back `Revoked` through `list`) -- historically this section noted that a rotated-away key being refused as a bearer credential was untested; round 6 (below) is what closed that.
- **Reference app (round 6, new)**: `examples/reference-app/cmd/server/apikey_authenticate_flow_test.go`'s `TestBuildServer_APIKeyAuthenticateFlow_RotateAndRevokeRefuseTheOldKey` is round 6's own mandatory-first-consumer proof -- see "Reference app: mandatory first consumer (round 6's inbound-authentication surface)" above for the full record. Same "not part of this module's own CI leg" shape as the other reference-app flow tests.
- **Reference app (round 7, new)**: `examples/reference-app/cmd/server/webhook_crud_flow_test.go` is round 7's own mandatory-first-consumer proof, its three tests -- `TestBuildServer_WebhookPermissionGate_EnforcesTheWebhookPermissions`, `TestBuildServer_WebhookCRUD_CreateListUpdateDeleteRestore_EndToEnd` and `TestBuildServer_WebhookCRUD_DeliveriesList_RecentDeliveredRow` -- driving the whole CRUD + recent-deliveries surface through the composed HTTP stack -- see "Reference app: mandatory first consumer (round 7's webhook-subscription-CRUD surface)" above for the full record. Honestly recorded: round 7 adds no new module-level `_test.go` file of its own -- the round's module-side change (the fragment's webhook half, its generated additions, and `Handler` methods over unchanged Service calls, `errors.go`/`module.go`/`seams.go` carrying doc comments alone) is covered by these flow tests plus the round-2/round-3/round-5/round-6 module suites the operations delegate into, the deliberate "coverage by the first consumer" shape of a surface-only round. Same "not part of this module's own CI leg" shape as the other reference-app flow tests.
- `authenticate_test.go` (round 6, new) covers `Service.Authenticate`'s full surface, white-box, over `testService`: a valid key (resolving the right tenant, key id, creator and scopes with NO tenant in `ctx` at all), a wrong/never-issued key, an empty credential, a revoked key, a ROTATED-AWAY key specifically (`TestService_Authenticate_RotatedAwayKey_ReportsAuthenticationFailed` -- the exact property this round exists to make testable), an expired key (via a mutated `svc.now`), and `LastUsedAt` actually being written on success -- every refusal reason asserted against the single `ErrAuthenticationFailed` sentinel, never a reason-specific code.
- `authenticate_example_test.go` (round 6, new) is the compiled godoc `Example` root `CLAUDE.md`'s Documentation rule requires for this round's new public API (`Service.Authenticate`, `AuthMiddleware`, `NewAuthMiddleware`, `HeaderAPIKey`, `AuthenticatedAPIKeyFromContext`): `ExampleAuthMiddleware` drives create → authenticate → no-credential-refused → rotate → old-key-refused → new-key-succeeds through a real, composed `net/http.Handler`, printing only deterministic comparisons (never a raw uuid) so the `// Output:` block stays stable.
- `middleware_test.go` (round 6, new) covers `AuthMiddleware.Middleware`'s full HTTP-transport surface: missing header, an unrecognized key, a valid key (asserting BOTH `pkgcore.TenantFromContext` and `AuthenticatedAPIKeyFromContext` are what `next` actually sees), and `TestAuthMiddleware_ServedBeforeAttach_WritesInternalError`, mirroring `TestHandler_ServedBeforeAttach_WritesInternalError`'s identical Register-time/Attach-time race proof.
- `repository_test.go` (round 6 additions) covers `apiKeyHashIndex`'s `tenancytest.AssertNotTenantScoped` proof, `createWithHashIndex`'s transactional write (both rows land, agreeing on the same tenant), `tenantForHash`'s unknown-hash refusal and its "needs no tenant in context" property (the direct proof of its whole reason to exist, mirroring `go/sharing`'s identical test), and `byHash`'s cross-tenant refusal.
- `handler_test.go` (round 5, new) covers `Handler`'s full HTTP-transport surface, white-box, over a real `Module` built through `Register`+`Attach` (never a mock of `Service`): create (empty-body scopeless issuance, scope validation reaching a real `PermissionLister`, no-`SubjectResolver`/resolver-reports-not-ok both answering `ErrSubjectUnresolved`, malformed-body answering `ErrInvalidRequestBody`), list (never exposes `key` or `hash`, checked through a raw map decode so a struct that simply never declared the field could not hide a real regression), rotate (not-found, success with a differing id and key, the revoked-predecessor conflict), revoke (success, already-revoked conflict, not-found), and `TestHandler_ServedBeforeAttach_WritesInternalError`, which reaches `NewHandler` directly (bypassing the normal Register-then-Attach sequence) to prove the Register-time-Handler/Attach-time-Service race answers a coded internal error rather than panicking.
- `handler_example_test.go` (round 5, new) is the compiled godoc `Example` root `CLAUDE.md`'s Documentation rule requires for this round's new public API (`Handler`, `NewHandler`, `SubjectResolver`, `WithSubjectResolver`): `ExampleHandler` drives create → list → rotate → revoke through a real, composed `net/http.Handler` built from `reg.Routes.Routes()`, the same route a real host's `mountModuleRoutes` would copy onto its own mux.
- `repository_test.go` runs the mandatory `tenancytest.AssertIsolated` suite against `APIKeyRepository`.
- `model_test.go` covers `APIKey`'s `TenantScoped` implementation, `IsRevoked`/`IsExpired`, and the `scopesJSON`/`parseScopes` round trip including the nil-becomes-empty-array contract.
- `keygen_test.go` covers `newAPIKeyToken`'s prefix/hash shape, uniqueness across calls, and `hashAPIKeyToken`'s determinism.
- `seams_test.go` covers the `PermissionListerFunc`/`MembershipCheckerFunc` adapters.
- `ratelimit_test.go` covers `LayeredLimiter`'s short-circuit ordering (global denies without touching tenant/key, tenant denies without touching key), per-layer independence, disabled-layer skipping, and invalid-limit propagation.
- `httpguard_test.go` covers `HTTPGuard.Middleware`'s allow/deny/limiter-error paths and the header/body shape of a 429 response.
- `service_test.go` covers `Create`/`List`/`Rotate`/`Revoke`'s full validation surface: scope validation (including the no-lister-needed empty-scopes case and the lister-unavailable refusal), expiry default/ceiling/past-refusal, cross-tenant not-found indistinguishability, `CreatorLeft` with and without a `MembershipChecker`, and `Rotate`'s create-new-then-revoke-old behavior including its scope-revalidation-on-rotation property.
- `module_test.go` covers `pkgcore.Module` identity, `Register`'s declared permissions and audit actions, `Attach`'s single-call contract, and the injected-clock seam (`withClock`) reaching a real `Service` -- plus, round 2, `Register`'s job-handler registration, its event-mapping subscription actually reaching the host's bus, and its duplicate-mapping refusal.
- `errors_test.go` covers the error index's `apperr` shape (every code prefixed `integration.`, `ErrRateLimited`'s 429 status) -- both rounds' codes.
- `webhook_model_test.go` covers `WebhookSubscription`/`WebhookDelivery`'s `TenantScoped` implementation and the `eventTypesJSON`/`parseEventTypes` round trip, mirroring `model_test.go`'s round-1 shape -- plus, round 3, `WebhookSubscription`'s `GetDeletedAt` implementation, mirroring `examples/reference-app/internal/notes`'s and `go/rbac`'s identical no-database sanity check.
- `webhook_repository_test.go` runs `tenancytest.AssertIsolated` against both new repositories, plus `ListActiveByTenant`, `ByIdempotencyKey` and `ListRecentBySubscription`'s own query shapes.
- `eventmapping_test.go` covers `EventMapping.validate`, `buildEventMappingIndex`'s duplicate-`InternalType` refusal and its "distinct internal types may share one public type" legality, and `buildEnvelope`'s wrapping.
- `ssrf_test.go` covers `ValidateWebhookURL`'s full refusal surface -- **including the mandatory private-IP-blocked proof** -- `isBlockedIP`'s per-range table, and `newSafeHTTPClient`'s dial-time refusal against a real `httptest.Server` on loopback.
- `webhook_signature_test.go` covers `signWebhookPayload`'s determinism and that the secret, timestamp and body each independently change the signature.
- `webhook_service_test.go` covers `CreateWebhookSubscription`/`UpdateWebhookSubscription`/`DeleteWebhookSubscription`/`ListRecentWebhookDeliveries`'s validation surface, including the creation-time SSRF gate wired into the real service path (not only unit-tested against `ValidateWebhookURL` in isolation) -- plus, round 3, `DeleteWebhookSubscription`'s mark-instead-of-physically-remove proof and `RestoreWebhookSubscription`'s full round-trip surface: undoes-the-delete with fields intact, the forced-`Active=false` proof (seeded specifically as `Active: true` at delete time), and the not-found family (unknown id, never-deleted id, twice, cross-tenant) mirroring `go/org`'s and `go/rbac`'s own equivalent test names.
- `webhook_delivery_test.go` covers `handleDomainEvent`'s fan-out (unmapped types ignored, no-tenant skip, inactive-subscription exclusion, idempotent redelivery), `handleDeliveryJob`'s full outcome surface (signed success against a real `httptest.Server`, receiver-error retry, subscription-deleted/paused early termination, already-delivered no-op) with the delivered request's signature independently recomputed and compared, and `onWebhookDeliveryDeadLetter`'s terminal-state write -- plus, round 3, `TestService_handleDeliveryJob_SubscriptionMarkDeleted_DeliveryRowUnaffected`, which reaches under `Service` to confirm the subscription row is mark-deleted (not gone) and the delivery row is completely untouched by it, on top of re-proving the pre-existing terminal-without-retry outcome.
- `keygen_test.go` additionally covers `newWebhookSecret`'s literal prefix and cross-call distinctness.

## Error index

| Code | `apperr` kind | Raised by |
|---|---|---|
| `integration.key_not_found` | `NotFound` | `Service.Rotate`/`Revoke` for an id that does not exist, or belongs to another tenant (the two are deliberately indistinguishable) |
| `integration.key_already_revoked` | `Conflict` | `Service.Rotate`/`Revoke` against a key whose `RevokedAt` is already set |
| `integration.created_by_required` | `Invalid` | `Service.Create` with an empty creator user id |
| `integration.expiry_exceeds_maximum` | `Invalid` | `Service.Create` whose requested `ExpiresAt` is further out than `MaxAPIKeyLifetime` |
| `integration.expiry_in_past` | `Invalid` | `Service.Create` whose requested `ExpiresAt` is not strictly after now |
| `integration.scope_not_held_by_creator` | `Forbidden` | `Service.Create`/`Rotate` requesting a scope the creator does not currently hold; `WithParam("scope", ...)` names it |
| `integration.permission_lister_unavailable` | `Internal` | `Service.Create`/`Rotate` requesting a non-empty `Scopes` with no `PermissionLister` wired |
| `integration.rate_limited` | 429 (not an `apperr` builder kind -- a struct literal, matching `go/authn`'s identical `ErrRateLimited` shape) | `HTTPGuard.Middleware` (via `WithRateLimitParams`) for a denied `LayeredDecision` |
| `integration.already_attached` | `Internal` | `Module.Attach` called a second time on the same `Module` |
| `integration.internal_error` | `Internal` | A repository failure, a `PermissionLister`/`MembershipChecker` call that itself failed, or a `crypto/rand` failure |
| `integration.webhook_subscription_not_found` | `NotFound` | `Service.UpdateWebhookSubscription`/`DeleteWebhookSubscription`/`RestoreWebhookSubscription` for an id that does not exist, or belongs to another tenant (deliberately indistinguishable, the identical reasoning `integration.key_not_found` documents). Round 7's mounted update/delete/restore operations return the Service's own error unchanged -- `handler.go`'s webhook operations call `writeError` on it directly -- so the same 404 answers over HTTP |
| `integration.webhook_url_required` | `Invalid` | `Service.CreateWebhookSubscription`/`UpdateWebhookSubscription` with an empty URL |
| `integration.webhook_url_invalid` | `Invalid` | A malformed URL, one with no host, or a disallowed scheme; `WithParam("reason", ...)` names which (`ssrf.go`'s `ValidateWebhookURL`) |
| `integration.webhook_url_unresolvable` | `Invalid` | A URL whose host could not be resolved to any address at all; `WithParam("host", ...)` |
| `integration.webhook_url_blocked` | `Invalid` | A URL resolving to a private/loopback/link-local/multicast/CGNAT address -- the mandatory SSRF refusal; `WithParam("ip", ...)` |
| `integration.event_types_required` | `Invalid` | `Service.CreateWebhookSubscription`/`UpdateWebhookSubscription` with an empty `EventTypes` selection |
| `integration.webhook_event_type_unknown` | `Invalid` | A requested public event type no `EventMapping` declares; `WithParam("event_type", ...)` |
| `integration.invalid_event_mapping` | `Invalid` | A `WithEventMapping` declaration missing a required field; `WithParam("field", ...)` (`eventmapping.go`'s `EventMapping.validate`) |
| `integration.duplicate_event_mapping` | `Invalid` | Two `EventMapping` declarations sharing one `InternalType`; `WithParam("internal_type", ...)` |
| `integration.invalid_request_body` | `Invalid` | `Handler.IntegrationCreateAPIKey` (round 5) for a non-empty request body that does not decode as `api.IntegrationCreateAPIKeyRequest`. A transport-layer condition `Service` itself never raises -- it has no `*http.Request` to malform -- following org's and storage's identical split between a Service's own error index and its Handler's transport-layer additions. Round 7 extends it to `Handler.IntegrationCreateWebhookSubscription`/`IntegrationUpdateWebhookSubscription`, which decode through `decodeRequiredJSON`: there a GENUINELY EMPTY body is refused too (no cause), exactly like a malformed one (with cause), the strict-request discipline round 5's apikey create deliberately does not apply |
| `integration.subject_unresolved` | `Unauthorized` | `Handler.IntegrationCreateAPIKey` (round 5) with no `SubjectResolver` wired, or one that could not identify the caller -- and, round 7, `Handler.IntegrationCreateWebhookSubscription` under the same conditions, the seam's second consumer reached through the same shared `resolveSubject`. See `SubjectResolver`'s own doc comment (`seams.go`): this module fails closed here rather than inventing a default creator, the identical rule `org.ErrSubjectUnresolved` and `notification.ErrSubjectUnresolved` already enforce for their own structurally-identical seam |
| `integration.authentication_failed` | `Unauthorized` | `Service.Authenticate` (round 6) for ANY of: no such hash exists anywhere, the key is revoked, or the key has expired -- deliberately never distinguished, the same no-enumeration discipline `authn.invalid_credentials` and `sharing.not_accessible` already apply to their own bearer-credential refusals. Also `AuthMiddleware`'s own missing-or-empty-credential case, so a request presenting none is refused identically to one presenting a wrong one |

Every code above has a matching description entry in `locales/{zh-CN,en-US}.toml` under the identical id, per this codebase's i18n convention.

## Adjudications a reviewer should not "correct"

**The primary key is `(id)` alone, not `(tenant_id, id)`.** See "Data model" above -- `id` is a globally-unique application-generated UUID, matching `go/storage`'s `Object` precedent rather than the composite-key shape a module-local id would need.

**Rate limiting is never hard-wired to any specific storage key naming beyond this module's own `layerStorageKey` prefix.** `LayeredLimiter.Allow` treats `globalKey`, `tenantKey` and `apiKeyID` as opaque caller-supplied strings, exactly as `go/ratelimit.Limiter.Allow` itself treats its own `key` parameter -- this module adds no interpretation of what those strings mean.

**`Rotate` carries `Scopes` forward from the predecessor, but re-validates them against the creator's CURRENT permissions rather than trusting them as already-proven.** This is not a bypass of `Create`'s own scope check: routing the predecessor's scopes back through `Create` means a creator whose permissions shrank since the predecessor was issued can have a `Rotate` fail with `integration.scope_not_held_by_creator` on a scope it no longer holds. This is a deliberate, safe-direction consequence of "scope validation happens at issuance" applying to every issuance, including a rotation's, not a special carve-out.

**`ExpiresAt` is NOT carried forward on `Rotate`.** A rotation gets a fresh full-lifetime expiry (`Create`'s own default), not a continuation of the predecessor's absolute timestamp -- copying that timestamp would defeat the point of rotating a key that is itself close to (or, for a caller rotating a stale-but-still-live key, already past) its own expiry.

**`WebhookSubscription.Secret` is reversibly encrypted, not hashed like `APIKey.Hash`.** See "Round 2's two tables" above -- this is a deliberate divergence from round 1's own "the key material is never stored" pattern, not an inconsistency: a webhook secret must be read back in plaintext to sign every delivery attempt, while an API key hash only ever needs to be compared.

**`EventMapping` is wired through a `Module` construction-time `Option` (`WithEventMapping`), never a `pkgcore.Registry` field.** See "Seams" above's `EventMapping` entry for the full rationale -- this round was explicitly scoped to leave `pkgcore` untouched, and the `Option` shape reuses round 1's own established seam-wiring convention rather than inventing a new one.

**A dead-lettered delivery reuses `DeliveryStatusDeadLetter` for three distinct causes** (retries genuinely exhausted, the subscription was deleted after enqueue, the subscription was paused after enqueue) **rather than three separate statuses.** This was a deliberate choice to keep the state machine to the two terminal states `webhook_model.go`'s own doc comment documents (`Delivered`, `DeadLetter`) rather than growing a third and fourth just to distinguish "why" from "what" -- an operator reads `LastError`'s text for the why. See "Known limitations" above for the follow-up this leaves for a later round.

**`handleDomainEvent` and the job-side `webhookDeliveryHandler` are both `Module`-level methods/types, not `Service`-level, even though they forward almost everything to `Service`.** This is not incidental placement: `Module.Register` must call `reg.Events.Subscribe` and `reg.Jobs.Handle` before a `Service` exists at all (`Service` is built in `Attach`, which runs strictly after `Bootstrap`'s `Register` phase completes across every module) -- see "Register-time wiring, Attach-time `Service`" above.

**`RestoreWebhookSubscription` forces `Active = false`, unlike `go/org`'s and `go/rbac`'s own Restore methods, which change nothing beyond the two soft-delete columns.** Do not "fix" this into matching their precedent -- it is a deliberate, reasoned divergence, not an inconsistency this codebase forgot to bring into line. See "Soft deletion" above for the full argument: resuming automatic outbound HTTP delivery to an external, third-party URL is a materially different kind of side effect from resuming an internal permission or membership grant, and this module treats it accordingly.

**`migrations/postgres/0005_fix_webhook_secret_column_type.sql` has no `sqlite/` sibling.** Do not add one -- see "Soft deletion" above's own paragraph on this migration for why SQLite genuinely needs no equivalent change, and `go/dbkit/migrations.go`'s `migrationFiles` for why `dbkit.MigrationRegistry.Apply` has no cross-dialect pairing requirement that would make an asymmetric migration set invalid.

**Round 5's HTTP-mounted API-key CRUD surface (`api/openapi.yaml`, `handler.go`) does NOT sit behind `HTTPGuard`/`LayeredLimiter`, and this is not an oversight to "finish."** See "In scope round 5" above for the full argument: those exist to rate-limit a request AUTHENTICATED BY an already-issued key against a host's own inbound API surface (a surface this module still does not build -- "Deliberately not in scope" above), never the ordinary, session-authenticated administrative act of managing one's own keys -- gated by rbac exactly like every other module's own management surface, none of which sit behind `go/ratelimit` either. Wiring the two together would conflate a request a key AUTHENTICATES with a request that merely MANAGES keys.

**`Handler` (round 5, `handler.go`) holds a `*Module`, not a `*Service`, unlike every other module's own `Handler`.** Do not "simplify" this to match org's/storage's/notification's/sharing's/admin's shape -- it cannot: this module's own `Service` is built in `Attach`, strictly after `Register` (round 1's own established shape), so a `Handler` built during `Register` (as `reg.Routes.Mount` requires) has no `Service` to hold yet. Reading `m.service` at call time is the fix, mirroring `handleDomainEvent`'s and `webhookDeliveryHandler`'s own identical technique -- see "Reference app: mandatory first consumer (round 1's API-key surface)" above.

**`Service.Authenticate` (round 6) resolves its tenant from the presented key alone and requires none in `ctx` -- unlike every other `Service` method, all of which call `pkgcore.MustTenantFromContext`.** Do not "fix" this into requiring a tenant in `ctx` too, and do not assume `migrations/{sqlite,postgres}/0001_create_integration_api_keys.sql`'s own comment on `uq_integration_api_keys_tenant_hash` ("every lookup this module ever performs already knows its tenant from request context") still describes this method -- it does not, and that comment is deliberately left unedited as a historical record of round 1's assumption rather than rewritten to match round 6's actual design. See "Round 6's platform-data table" above and `authenticate.go`'s own "Tenant resolution" doc comment section for the full argument: a genuinely inbound API-key-authenticated request carries no separate tenant claim of its own, the identical situation `go/sharing`'s `AccessPublic` answers for an anonymous share-link visitor, and `integration_api_keys` cannot answer it directly because it is `dbkit.TenantScoped` -- a query against it with no tenant in context fails closed, not open.

**`AuthMiddleware` (round 6, `middleware.go`) reads its bearer credential from `HeaderAPIKey` ("X-API-Key"), never `Authorization: Bearer`.** Do not "fix" this to match `go/authn.Middleware`'s more common convention -- it cannot, in any host whose outermost middleware layer is `go/authn.Middleware` (this reference app included): that middleware already claims the `Authorization` header globally and unconditionally, and its own doc comment states a present-but-unverifiable bearer credential is refused with an immediate 401 -- reusing `Authorization` for an API key would 401 every API-key-authenticated request at `go/authn.Middleware`, before `AuthMiddleware` (mounted below it in the chain) ever ran at all. See `middleware.go`'s own `HeaderAPIKey` doc comment for the full argument, including why this is a real, verified-in-tree conflict rather than a hypothetical one.

**Round 6's demo route (`examples/reference-app/cmd/server/integration_authenticate.go`'s `integrationWhoamiPath`) is deliberately NOT gated by `rbac.Authorizer`, unlike `wireIntegrationWebhooks`'s own demo route.** Do not add a permission check here -- this route's whole purpose is to demonstrate authenticating a caller that holds nothing but the API key itself (a script or third-party system, never an authn session), so `Service.Authenticate`'s own refusal already is this route's access control; layering rbac on top would conflate two different callers' authentication mechanisms into one route.
