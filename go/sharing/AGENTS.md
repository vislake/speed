# sharing

go/sharing implements public share links: a controlled entry point that lets an unauthenticated external visitor view one internal resource -- a patient viewing their own simulation result, a customer viewing a report, an anonymous one-time result page. This file is the module-level discipline that ships with `go/sharing` to consuming projects; the design rationale is `docs/internal/07-platform-services.md`'s sharing section, and the repository-wide rules are the root `CLAUDE.md` plus `.claude/skills/backend-coding-standards`.

**Status: rounds 1 and 2 landed in this history.** Round 1 shipped the Share domain model and its access log, the Service that creates, accesses and revokes share links, the jobs-driven expiry sweep, and the module's `pkgcore.Module` wiring (permissions, one audit action, an event catalog, one configuration item). Round 2 closed every gap round 1 recorded as deliberately open: it resolved the tenant-resolution gap for a genuinely unauthenticated viewer (`Service.AccessPublic`, see "Tenant resolution for an unauthenticated viewer" below), shipped the module's one public HTTP route (`api/openapi.yaml`, `handler.go`), added the `ResourceResolver` seam that turns a Share's `ResourceRef` into actual bytes, applied `go/ratelimit` to both `Create` and `AccessPublic` (`ratelimit.go`), and wired the reference app as a real, end-to-end HTTP consumer (`examples/reference-app/cmd/server/sharing_flow_test.go`) -- retiring round 1's "No real consumer yet" section. A reader checking a later main tree should still not assume this line is current without checking; the code is always the authority over this sentence.

## Scope

**In scope round 1.** Two tables (`sharing_shares`, `sharing_access_log`) with dual-dialect versioned SQL migrations. `Service`: `Create` mints a share (forcing a default expiry, refusing an explicit request for one that never expires, hashing an optional password, drawing a fresh high-entropy token), `Access` resolves a bearer token into the share it names or refuses with one outward-identical answer, `Revoke` withdraws a share with immediate effect, `Get`/`ListAccessLog` serve an owner's own view. `Service.Sweep` plus the `sharing.expiry_sweep` jobs task mark expired or view-exhausted shares. `pkgcore.Module` wiring: permissions (`sharing:read`, `sharing:create`, `sharing:revoke`), one audit action (`sharing.share.create_sensitive`), three declared domain events (`sharing.share.created`/`.accessed`/`.revoked`), and one configuration item (`sharing.default_expiry`).

**In scope round 2.** `Service.AccessPublic` resolves the tenant from a bearer token alone (no tenant anywhere in `ctx`) and re-enters the unchanged `Access` -- the resolved answer to "Tenant resolution for an unauthenticated viewer" below, backed by the new, deliberately narrow `shareTokenIndex` platform-data table and `ShareRepository.tenantForTokenHash`/`createWithTokenIndex` (`repository.go`, `model.go`). One HTTP route (`api/openapi.yaml`'s `sharing_accessShare`, `GET /api/v1/sharing/access`, exported as `PathAccess`), implemented by `handler.go`'s `Handler` behind a generated `api.ServerInterface`, genuinely public and unauthenticated -- a host must allowlist it with `tenancy.WithAllowlist`, the same mechanism `go/config`'s two pre-auth endpoints already use. The `ResourceResolver` seam (`resolver.go`) turns a granted share's `ResourceRef` into actual bytes, structurally typed (no import edge), satisfied in the reference app by an adapter over `*storage.ObjectService`. `go/ratelimit` applied to `Create` (per tenant) and `AccessPublic` (per IP and per token hash, `ratelimit.go`), built lazily over the host's `KVStore` through the same `hostSeams` seam `EventBus()` already used (extended with `KVStore()`). Every response the public route writes carries `Cache-Control: no-store`. The reference app wires the module end to end -- `sharing_resolver.go`'s adapter, `sharing_flow_test.go`'s real-HTTP create/access/revoke/refused cycle -- retiring "No real consumer yet" below.

**Deliberately not in scope, still** (see "Known limitations" below for the full reasoning behind each):

| Not here | Where it belongs | Why |
|---|---|---|
| Compliance's own consumption of `sharing.share.accessed` | `go/compliance` (M4) | `compliance` does not exist yet; this module only publishes, the identical publish-now-subscriber-later shape `go/dbkit/audit`'s write-capture events used before `go/dbkit/audit` itself existed to consume them |
| A live per-tenant reader of `ConfigDefaultExpiry` | A later round, or whichever host wires `WithTenantConfigReader` | See "Tenant-configured default expiry" below |
| Owner-facing HTTP routes (`Create`/`Revoke`/`Get`/`ListAccessLog` over HTTP, not just `Service`) | A later round | This round's HTTP scope was specifically the public, unauthenticated surface (round-2 instructions); an owner-facing surface sits behind the ordinary authenticated chain like every other module's fragment and is a separable piece of work the reference app currently discharges by calling `Service` directly (`sharing_flow_test.go`) |
| A browser-facing password-entry page for a protected share | A later round, or a host's own frontend | `handler.go`'s route accepts the password over `HeaderSharePassword`; rendering an actual HTML prompt that collects and resends it is presentation-layer work no module in this codebase ships (see `docs/internal/12-frontend.md`'s scope) |

## Data model

Two tables, one tenant-scoped, both round 1's own design:

| Table | Domain | Repository shape | Isolation proof |
|---|---|---|---|
| `sharing_shares` | Tenant | `ShareRepository`, embeds `dbkit.Repository[Share]` | `tenancytest.AssertIsolated` |
| `sharing_access_log` | Tenant | `AccessLogRepository`, embeds `dbkit.Repository[AccessLogEntry]` | `tenancytest.AssertIsolated` |

Both are tenant data: a share belongs to the tenant whose resource it exposes, and an access log entry belongs to the same tenant as the share it was recorded against. Neither table carries a SQL foreign key to the other, or to any cross-module row -- `AccessLogEntry.ShareID` is a plain, unenforced string column, following this codebase's blanket no-FK convention even for a same-module reference.

**`Share.TokenHash`, never `Share.Token`.** The design sketch in `docs/internal/07-platform-services.md` shows a `Token` field on the model; this module deliberately stores only the hex-encoded SHA-256 of the token (`token.go`'s `hashShareToken`), mirroring `go/org`'s `Invitation.TokenHash` precedent exactly. The raw token is returned to the caller of `Service.Create` exactly once, inside `CreateResult.Token`, and is never persisted anywhere. A leaked database backup therefore yields no usable share link, and `Service.Access`'s lookup is a hash comparison rather than a comparison against a stored secret.

**`Share.ExpiresAt` is a `*time.Time` that is never actually nil once a row exists.** The Go type matches the design sketch's optional shape (`Service.Create`'s own `CreateParams.ExpiresAt` really is optional), but `Service.Create` resolves a nil request into a concrete time before the row is ever written, and refuses outright (`ErrExpiryRequired`) a caller that explicitly asks for one that never expires (`CreateParams.Forever`). The column is declared `NOT NULL` as a second, database-level line of defense.

**`Share.PasswordHash` is an argon2id PHC digest, never plaintext.** `password.go` implements a small, self-contained argon2id hasher at fixed, un-configurable cost parameters (OWASP's first recommended configuration, matching `authn.DefaultPasswordParams`'s own floor) rather than importing `go/authn` for its `HashPassword`/`PasswordParams` -- `go/authn` is a heavy module several tiers away in the dependency graph, and pulling in its full SMS/OIDC/session surface for one hashing function would violate this codebase's "measure what a dependency costs" discipline. The two implementations are deliberately not unified; if a policy-driven, host-configurable share password ever matters, that is this module's own future round, not a `go/authn` dependency.

**`ShareRepository.tryRecordView` is a compare-and-swap guard, not a raw SQL increment.** It matches the exact row state (`view_count = ?`) a caller last observed and refuses the write if the row has moved on -- the identical shape `org.InvitationRepository.acceptIfPending` uses for its own single-use guard -- rather than `view_count = view_count + 1` reached through `.Model(...)`, which this codebase's `raw-gorm-bypass` semgrep rule flags on any receiver. `Service.recordView`'s retry loop (bounded by `maxRecordViewAttempts`) re-reads a lost race and retries against the row's latest state, so ordinary concurrency between two viewers of one share is never misreported as "not accessible" -- only a share that has genuinely stopped being live short-circuits the loop as a real refusal. `TestService_Access_ConcurrentAccessesRespectMaxViews` races many goroutines against a `MaxViews`-limited share under `-race` and asserts exactly `MaxViews` of them are granted.

## The five mandatory rules

Every rule `docs/internal/07-platform-services.md`'s own "必须遵守的规则" (mandatory rules) list states is enforced, each with a passing test:

1. **Tokens are cryptographically random, at least 128 bits, never derived from a predictable value.** `token.go`'s `newShareToken` draws 32 bytes (256 bits) from `crypto/rand` and nothing else. `TestNewShareToken_MeetsTheEntropyFloor`, `TestNewShareToken_IsUnpredictable`, `TestNewShareToken_LeakingOneTokenDoesNotRevealAnother` (`token_test.go`).
2. **A share created with no explicit expiry gets the tenant's configured default, and a request for a never-expiring link is refused outright.** `Service.Create`'s `resolveExpiry` (nil `ExpiresAt`) and its `CreateParams.Forever` check (`ErrExpiryRequired`). `TestService_Create_NoExpiryFallsBackToDefault`, `TestService_Create_NoExpiryUsesTenantConfiguredDefault`, `TestService_Create_ForeverRefused` (`service_test.go`).
3. **Revocation takes effect on the very next access check, with no caching involved.** `Service.Access` re-reads the row from the database on every call; there is no process-local or otherwise cached share state anywhere in this module. `TestService_Access_RevokedShare_ImmediatelyDenied` (`service_test.go`) is the exact create/access-succeeds/revoke/access-immediately-fails sequence the design doc calls for. See "Revocation and caching" below for the obligation this places on a future HTTP layer.
4. **Every access is logged, and a resource owner can read back who viewed a share and how many times.** `AccessLogEntry` (timestamp, IP, user agent, referrer, outcome) is written on every `Service.Access` call that resolves to a known share of the caller's tenant, granted or denied alike; `Service.ListAccessLog` is the owner-facing read. `TestService_Access_LogsEveryAttempt`, `TestService_Access_LogsDeniedAttemptsAgainstAKnownShare` (`service_test.go`).
5. **The share surface leaks nothing about the tenant: every refusal reason looks identical from the outside.** `Service.Access` answers an unrecognized token, a revoked share, an expired share, a view-exhausted share, and a missing or wrong password all with the exact same `ErrNotAccessible` -- same `Code`, same `Status`, no distinguishing `Params` -- mirroring the existence-disclosure suppression `go/authn` already applies to account enumeration. `TestService_Access_EveryRefusalReasonIsOutwardlyIdentical` (`service_test.go`) asserts this across all five reasons in one table-driven test.

## Sensitive-resource confirmation

Rule 4 of the doc's "sensitive resource sharing" note (not to be confused with the five mandatory rules' own numbering above): creating a share for a resource carrying sensitive personal information is itself an audit event. `CreateParams.Sensitive` is an explicit, caller-supplied boolean -- this module deliberately builds no generic sensitivity-classification system, per this round's own scope boundary; a create call simply says "this resource is sensitive" or does not.

When `Sensitive` is true, `Service.Create`'s `emitSensitiveAudit` fires the `sharing.share.create_sensitive` audit action through `go/dbkit/audit`'s **declarative `Emit` path**, not `dbkit.Options.AuditBus`'s automatic write-capture plugin. This is a deliberate choice, not an oversight: `go/dbkit/AGENTS.md`'s "Audit trail collection" section records that `AuditBus` must never point a persister at the same database a `dbkit.Repository[T]` write's own open transaction is still writing to, or the write-capture plugin's synchronous, same-goroutine publish deadlocks into `SQLITE_BUSY`. `emitSensitiveAudit` is called *after* `s.shares.Create` has already returned -- by which point `Create`'s own transaction has committed -- exactly mirroring `examples/reference-app/internal/notes/handler.go`'s `recordNoteCreatedAudit`, the established precedent for this exact hazard. A failure to emit the audit event is logged at Error level and never turns an otherwise successful share creation into an error for the caller, per `docs/internal/10-compliance-and-audit.md`'s "must alert, must not be silently dropped" rule. `TestService_Create_SensitiveEmitsAuditEvent` / `TestService_Create_NotSensitiveEmitsNoAuditEvent` (`service_test.go`).

## Tenant-configured default expiry

`ConfigDefaultExpiry` (`sharing.default_expiry`) is declared on the registry by `Register`, with its `Default` set to `defaultShareExpiry` (30 days) -- visible today, and eventually editable, through `go/config`'s own admin-console machinery once a host wires that path. Declaring the schema is as far as this round goes, mirroring `go/pki`'s identical round-1 choice for its own validity-period config items: no module in this codebase's history yet reads a live per-tenant `go/config` value from business code (the closest precedent, `org.FeatureGate`, is a structurally-typed *feature-flag* seam, a narrower shape), so there is no established pattern to extend, and adding a full `go/config` dependency for one round's single read would be a meaningfully larger dependency footprint than this module's own scope warrants.

Instead, `Service`'s `TenantConfigReader` interface (`service.go`) is a structurally-typed, no-import seam -- `ShareDefaultExpiry(ctx, tenant) (time.Duration, bool, error)` -- the same shape `org.FeatureGate` and `org.Scope` use to reach `go/config`-shaped or `go/org`-shaped behavior without an import edge in either direction. `Module.WithTenantConfigReader` wires one in; without it (the default), `Service.Create` always falls back to `defaultShareExpiry`, which is exactly what rule 2's own "30 days if the tenant has not configured one" fallback means when nothing has been configured at all. The seam is fully implemented and tested (`TestService_Create_NoExpiryUsesTenantConfiguredDefault`, `TestService_Create_TenantConfigReaderReportingUnconfigured_FallsBackToDefault`) against a fake reader -- **no host wires a real adapter over `*config.Service` yet**, which is this round's honest limitation, not a missing feature: writing that adapter is a `go/config`-side or reference-app-side concern for whichever round actually needs per-tenant-tunable share expiry.

## Revocation and caching

Rule 3 (`docs/internal/07-platform-services.md`'s "revocation takes effect immediately" rule) is explicit that a share page and the resource it serves **must declare `Cache-Control: no-store` and must never sit behind a CDN** -- CDN caching is named as the single most common way revocation silently fails to take effect. **This obligation is discharged as of round 2.** `Service.Access` itself has no caching problem (it re-reads the database on every call, per rule 3's own test above), and `handler.go`'s `Handler.SharingAccessShare` sets `Cache-Control: no-store` as its very first action -- before any other work, so every response the route can possibly produce carries it, success or refusal alike. This covers more than `SharingAccessShare`'s own body, too: `NewHandler` wires the mux with `api.HandlerWithOptions`, not the bare `api.HandlerFromMux`, precisely so a request the spec-generated parameter binder rejects before `SharingAccessShare` is ever called -- a missing or malformed `token`, a duplicated `X-Sharing-Password` header -- is answered by `handler.go`'s own `bindingErrorHandler` (`sharing.invalid_request`) rather than oapi-codegen's default `http.Error`, and that handler sets the same header first too. `TestHandler_SharingAccessShare_SetsNoStoreCacheControl` (`handler_test.go`) pins the success/refusal cases at the unit level, `TestHandler_SharingAccessShare_MissingTokenParam_Answers400` and `TestHandler_SharingAccessShare_DuplicatePasswordHeader_Answers400` pin the two binding-error cases the same way, and `TestBuildServer_SharingFlow_CreateAccessRevoke_EndToEnd` (the reference app's `sharing_flow_test.go`) re-proves the success/refusal path over a real HTTP response from the composed stack. This module still cannot enforce "must never sit behind a CDN" itself -- that is a deployment-topology decision outside any module's reach -- but the header is the one lever a module can pull, and it is pulled unconditionally.

## Tenant resolution for an unauthenticated viewer

**Resolved in round 2, choosing the first of the two directions round 1 left open (see below): the tenant is resolved *from the token itself*, before any tenant-scoped query ever runs.**

`Service.AccessPublic` (`service.go`) is the genuinely unauthenticated entry point round 1's `Access` never was: it takes a `ctx` that is not expected to carry a tenant at all. It resolves the owning tenant via `ShareRepository.tenantForTokenHash` (`repository.go`) -- a deliberately narrow, tenant-less lookup against `shareTokenIndex` (`model.go`), a table that maps a token's hash to its owning tenant and nothing else -- then attaches that tenant to `ctx` with `pkgcore.WithTenant` and re-enters the ordinary, **completely unchanged** `Service.Access`. Every one of `Access`'s own guarantees (rule 3's immediate revocation, rule 4's access logging, rule 5's outward-identical answers, the constant-time password check) therefore holds for an anonymous caller exactly as it already held for an authenticated one, because `AccessPublic` does not reimplement any of them -- it only supplies the one thing an anonymous caller cannot: a tenant.

**Why this direction, and not the second one round 1 named** (a system-context-shaped escape hatch searching across tenants): root `CLAUDE.md`'s own architecture-discipline rules restrict `pkgcore.WithSystemContext` to `admin`, `compliance`, `jobs` and `authn`, and its own "Multi-tenant isolation" section is explicit that the *only* legitimate cross-tenant path is that one audited primitive -- a second, ad hoc escape hatch invented inside `sharing` for this one need would be exactly the uncontrolled proliferation that rule exists to prevent, and `sharing` is not on the whitelist regardless. Resolving the tenant from the token first needed no new primitive at all: `token_hash` is already effectively globally unique (`newShareToken` draws 256 bits of entropy from `crypto/rand`), so a lookup keyed on the hash alone, with no tenant predicate, can safely return the owning tenant before any tenant-scoped query runs -- and re-entering the ordinary `Access` afterward means every mandatory rule's enforcement point stays exactly where it already was, audited by round 1's own test suite, rather than duplicated into a second code path a reviewer would have to keep in step by hand.

**Why a new table, not a raw-SQL or `WithSystemContext` bypass on `sharing_shares` itself:** `dbkit`'s tenant-scope GORM plugin (`go/dbkit/tenant_scope.go`) fails every tenant-scoped query closed when `ctx` carries no tenant, by design and correctly -- it has no way to tell "this caller is allowed to look this up with no tenant" apart from an ordinary forgotten-tenant-filter bug. Escaping that plugin for `Share` (a genuinely tenant-scoped table) would mean either raw SQL -- forbidden by root `CLAUDE.md`'s "Do not reach for raw SQL to escape tenant filtering" rule outright -- or `pkgcore.WithSystemContext`, forbidden for the reason above. `shareTokenIndex` sidesteps the conflict entirely by never being tenant-scoped in the first place: it is **platform data** (`docs/internal/04-data-and-tenancy.md`'s data-domain table), the identical treatment `go/authn`'s `users` table, `go/jobs`'s `jobRecord` and `go/config`'s `row` already get for the same structural reason -- something that must be resolvable before a tenant is known cannot itself be tenant-scoped. It is reached only through `dbkit.Open()`'s plain `*gorm.DB` (never `dbkit.Repository[T]`, whose generic constraint requires `TenantScoped`, which this type must **not** implement), and its isolation suite is `tenancytest.AssertNotTenantScoped`, not `AssertIsolated` (`TestShareTokenIndex_AssertNotTenantScoped`, `repository_test.go`).

**Why this is not a general cross-tenant query capability:** the table carries exactly two columns -- a token hash and the tenant it belongs to -- and `tenantForTokenHash` returns a tenant id and nothing else: no `ResourceRef`, no `ShareID`, no `Share` row at all. A caller of this method learns only "which tenant does this hash belong to", never anything about the share itself; every other question is still answered exclusively by the ordinary tenant-scoped `Share` row `Access` reads after this lookup hands back a tenant. `ShareRepository.createWithTokenIndex` writes the `Share` row and its `shareTokenIndex` row in one transaction (`dbkit.WithTenantSession`), so a share is never left reachable by its owner while permanently unreachable by an anonymous visitor holding the same token.

**Timing property this collapsing does not hide** (recorded honestly, not swept in with rule 5's own outward-identical-answer proof): an unrecognized token costs one repository read (`tenantForTokenHash`) plus one burned password check before `AccessPublic` answers `ErrNotAccessible`, while a recognized-but-refused token costs that same read plus `Access`'s own `byTokenHash` read on top of it. Rule 5 (`AGENTS.md`'s "The five mandatory rules") was written to protect "which of Access's five refusal reasons applied", never "does this token exist at all" -- a property a valid token's own successful use already discloses to whoever holds it, so this small, structural timing difference is not a new leak.

## Reference app: mandatory first consumer

**The reference app is a real, end-to-end HTTP consumer of `go/sharing` as of round 2**, retiring round 1's "No real consumer yet" exception (the compensating godoc `Example` round 1 shipped, `example_test.go`, remains and still passes -- it is simply no longer the *only* proof).

`cmd/server/server.go` wires `sharing.NewModule(db, sharing.WithResourceResolver(...))`, its resolver a small adapter (`sharing_resolver.go`'s `storageSharingResolver`) over the app's own `storageModule.ObjectService()` -- `sharing` never imports `go/storage` itself (`resolver.go`'s own doc comment explains why: the structurally-typed, no-import-edge seam shape this codebase uses throughout), so this composition is entirely the reference app's own, the same way `orgFeatureGate` and `demoOrgSubjectResolver` compose their own seams. The module's one public route is allowlisted in `tenancy.Middleware` (`sharing.PathAccess`, `GET` only) and marked `routePublic` in `demo_subject.go`'s exhaustive route-guard table, for the identical structural reason `config`'s, `org`'s, `authn`'s and `notification`'s own `routePublic` entries are: an anonymous visitor holding a bearer token has no `Subject` an `rbac` permission check could evaluate at all.

`cmd/server/sharing_flow_test.go` drives the round's acceptance shape end to end: create a share for a real, completed `go/storage` object (through a second `sharing.Module` built over a second connection to the running server's own SQLite file -- `Create`/`Revoke` stay `Service`-level this round, see the scope table above, so this is the same "second connection is the only reach a test has into storage" pattern `server_test.go`'s `TestBuildServer_NoteCreate_PersistsAuditEvent` already established for `go/dbkit/audit`'s own table), access it as a genuinely unauthenticated visitor would through the real composed HTTP stack (no `Authorization` header, no demo user header), assert the served bytes match the object's own finalized checksum and that the response carries `Cache-Control: no-store`, revoke it, and observe the very next access answer 404 `sharing.not_accessible`. A second test, `TestBuildServer_SharingFlow_UnknownToken_Answers404`, proves the route is genuinely reachable with no `Principal` and no tenant at all -- a request `tenancy.Middleware` would otherwise 403 with `tenancy.tenant_unresolved` on any non-allowlisted path.

This proof also caught a real bug during this round's own development, worth recording: `Service.AccessPublic` resolves the tenant only inside its own call into `Access`, and never mutates the `*http.Request`'s context that reached the handler -- a first implementation of `Handler.SharingAccessShare` forwarded that same, still-tenant-less context straight into `ResourceResolver.OpenResource`, which made every granted access fail with `ErrResourceUnavailable` the moment the resolver itself needed a tenant (`storage.ObjectService.OpenContent` does). The fix rebuilds the tenant from the granted `Share`'s own row (`share.GetTenantID()`, the `tenant_id` column the tenant-scope plugin already populated when the row was written) before calling the resolver. `TestHandler_SharingAccessShare_ResolverSeesTheShareOwningTenant` (`handler_test.go`) is the unit-level regression test for this exact defect, and the reference app's own flow test is what surfaced it in the first place -- the clearest illustration in this module's own history of why a real HTTP consumer, not only a godoc `Example`, is the standard this repository holds every module to.

## Known limitations

- **No owner-facing HTTP routes.** `Create`, `Revoke`, `Get` and `ListAccessLog` remain `Service`-level only -- this round's HTTP scope was specifically the public, unauthenticated surface. See the "Deliberately not in scope, still" table above.
- **No live consumer of the `sharing.share.accessed` event.** `docs/internal/07-platform-services.md` names `go/compliance` (M4) as the eventual subscriber; this module only publishes, per this repository's established publish-now-subscriber-later pattern.
- **No live `TenantConfigReader` adapter over `go/config`.** See "Tenant-configured default expiry" above.
- **`clientIP` (`handler.go`) trusts only the direct connection's `r.RemoteAddr`, never `X-Forwarded-For` or any other caller-supplied header.** This feeds both the access log and `ratelimit.go`'s per-IP rate-limit key, and a spoofable header for either would let an attacker rotate around their own rate limit for free. A host that terminates TLS behind a trusted reverse proxy and wants the proxy's forwarded address instead is expected to normalize `r.RemoteAddr` itself, ahead of this handler -- no per-deployment trusted-proxy configuration exists in this module.
- **No browser-facing password-entry page.** `handler.go`'s route accepts a share's password over `HeaderSharePassword`, but rendering an actual HTML prompt that collects and resends it is presentation-layer work no module in this codebase ships; a real deployment's frontend builds that page itself.
- **`Sweep`'s reaping is row hygiene, not a correctness dependency.** `Share.isLive` is evaluated fresh on every `Service.Access` call regardless of whether a sweep has ever run; `Sweep` only converges an owner-facing listing's `RevokedAt` column onto "yes, this is gone" for rows nothing has looked at since they expired. `shareTokenIndex` rows are never swept or cleaned up either -- see model.go's own doc comment for why that is harmless, not an oversight.
- **No PostgreSQL integration tier.** `go/sharing/internal/testutil.NewPostgres` exists (mirroring `NewSQLite`) so a later round's `integration_test/` package needs no `db.go` of its own, but no such package is shipped yet. This round's SQLite-backed unit suite runs every isolation and CAS-guard assertion against SQLite only, `shareTokenIndex`'s new `AssertNotTenantScoped` suite included.
- **`Service.Access`'s outward-identical-answer property is proven for the reasons this round's code paths can produce (unknown token, revoked, expired, view-exhausted, missing/wrong password) and no others.** A future round adding a new refusal reason to `Access` must extend `TestService_Access_EveryRefusalReasonIsOutwardlyIdentical`'s table alongside it, or the new reason's outward shape goes unverified. That test asserts `Code`/`Status`/`Params` equality only; it does not measure or bound response latency, so `burnSharePasswordCheck`'s constant-time property (`password.go`) is enforced by code review and by every refusal path being required to call it, not by an automated timing assertion. The same limitation applies to `handler_test.go`'s HTTP-layer table.
- **This module's HTTP fragment is not part of the merged `build/openapi/speed.yaml` document.** It serves raw bytes, not a typed JSON operation a frontend SDK would meaningfully wrap around, so joining the orval-driven merge (as notes, authn and notification do) has no payoff -- the same reasoning `go/org`'s and `go/storage`'s own fragments record for staying out of the merge, for their own separate reasons.

## Testing

- **Unit tests**: SQLite only, no Docker required. `go/sharing/internal/testutil` provides `NewSQLite`/`NewPostgres`/`Migrate`, mirroring `go/pki/internal/testutil`'s exact shape.
- `model_test.go` covers `Share.isLive`'s full state matrix (revoked, expired, exactly-at-expiry, view-ceiling reached/exceeded, nil `ExpiresAt`) and both models' `TableName`.
- `token_test.go` covers the entropy floor, unpredictability across a sample, and that leaking one token's hash reveals nothing about another's.
- `password_test.go` covers hash/verify round-tripping, that two hashes of the same password differ (fresh salt), and a malformed stored hash reporting an error rather than a false match.
- `repository_test.go` covers both repositories' `tenancytest.AssertIsolated` suites, the token-hash lookup's tenant scoping, `tryRecordView`'s compare-and-swap guard (including a deliberately staged lost race), the expiry-sweep listing, and (round 2) `shareTokenIndex`'s `AssertNotTenantScoped` suite, `createWithTokenIndex`'s atomic dual-write, and `tenantForTokenHash`'s tenant-less resolution (including the direct proof that it needs no tenant in `ctx` at all).
- `service_test.go` is this module's largest suite: every one of the five mandatory rules (see above), `Create`'s full validation and defaulting matrix, the sensitive-resource audit emission, the three domain events' publication, `Revoke`/`Get`/`ListAccessLog`'s owner-facing behavior including a `-race`-covered concurrent-access test, and (round 2) `AccessPublic`'s own suite -- resolving a tenant from a bare `context.Background()`, the unknown-token/revoked/password-protected refusal paths, and cross-tenant token isolation.
- `ratelimit_test.go` (round 2) covers `rateLimiter`'s injected-override-over-host-KVStore precedence and its fail-closed behavior with no host attached, and `checkCreateRateLimit`/`checkAccessRateLimit`'s `ErrRateLimited` decoration and underlying-store-error wrapping, against a scripted `ratelimit.Limiter` double.
- `handler_test.go` (round 2) covers the HTTP layer directly: `Cache-Control: no-store` on every response shape, the outward-identical-answer property re-proven at the wire (status and body byte-for-byte across six refusal reasons), the password-protected variant's correct-password grant, `ErrResourceUnavailable` on both a nil and a failing `ResourceResolver`, the missing-required-parameter 400, and the tenant-propagation regression the reference app's own flow test first caught (`TestHandler_SharingAccessShare_ResolverSeesTheShareOwningTenant`).
- `cleanup_test.go` covers `Service.Sweep`'s marking behavior and idempotence, `expirySweepHandler`'s `Handle`/`Type`, and `Module.EnqueueExpirySweep`'s queue-required refusal and task-shape.
- `module_test.go` covers `pkgcore.Module` identity, the dual-dialect migration layout, locale-file parity, `Register`'s full declared surface (bootstrapped through a real `pkgcore.Kernel`, which also proves the locale bundle survives `i18n.Builder.AddModule`'s parity check) including (round 2) the one mounted route and its non-nil `OpenAPISpec`, coexistence with another module, the no-I/O contract of `Register`, and that `Register` builds `Handler` from the module's final post-`Option` state (`WithResourceResolver` included).
- `errors_test.go` covers the `hasCode` helper and that every declared error uses the `sharing.<reason>` code convention.
- **Reference app**: `examples/reference-app/cmd/server/sharing_flow_test.go` is this module's mandatory-first-consumer proof -- see "Reference app: mandatory first consumer" above.

## Error index

| Code | `apperr` kind | Raised by |
|---|---|---|
| `sharing.resource_ref_required` | `Invalid` | `Service.Create` with an empty `ResourceRef` |
| `sharing.expiry_required` | `Invalid` | `Service.Create` with `CreateParams.Forever` true |
| `sharing.invalid_max_views` | `Invalid` | `Service.Create` with a non-positive `MaxViews` |
| `sharing.invalid_request` | `Invalid` | `Handler`'s `bindingErrorHandler` (`handler.go`), when the spec-generated parameter binder itself rejects a request -- a missing or malformed `token` query parameter, a duplicated `X-Sharing-Password` header |
| `sharing.not_accessible` | `NotFound` | `Service.Access`/`AccessPublic`, for every refusal reason -- see rule 5 above |
| `sharing.share_not_found` | `NotFound` | `Service.Revoke`/`Get`/`ListAccessLog` for an unknown share id in the caller's tenant |
| `sharing.internal_error` | `Internal` | A storage error this module cannot classify |
| `sharing.rate_limited` | `429` (struct literal, not a builder shape) | `Service.Create`/`AccessPublic` when the caller's `go/ratelimit` budget is exhausted (`ratelimit.go`) -- carries `dimension` and `retry_after_seconds` params |
| `sharing.resource_unavailable` | `502` (struct literal, not a builder shape) | `Handler.SharingAccessShare` when access was granted but the `ResourceResolver` (nil, or a failing call) could not open the resource -- deliberately distinct from `sharing.not_accessible`, see that error's own doc comment |

Every code above has a matching description entry in `locales/{zh-CN,en-US}.toml`, under the identical id.
