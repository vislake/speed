//go:build go1.22

// Package api provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.8.0 DO NOT EDIT.
package api

import (
	"fmt"
	"net/http"

	"github.com/oapi-codegen/runtime"
)

// AiGatewayCredential A resolved or just-written credential's metadata. Never carries the api key.
type AiGatewayCredential struct {
	// BaseURL The credential's configured base URL; empty when the provider's own constructor supplies a default and none was set here.
	BaseURL *string `json:"baseUrl,omitempty"`

	// Provider The ChatProviderRegistry or ImageProviderRegistry name.
	Provider string `json:"provider"`

	// Scope "system" or "tenant" -- which scope this metadata describes. On aiGateway_getCredential this is whichever scope actually answered (CredentialService.Resolve's own fallback order); on the two write operations it is always the scope that operation just wrote.
	Scope string `json:"scope"`
}

// AiGatewayError The structured {code, params} error envelope every speed API returns instead of localized text: code is a stable machine-readable identifier naming exactly one failure, and params carries the structured per-code details when the failure has any. The server never renders a message -- a client maps each code to its own locale's text.
type AiGatewayError struct {
	// Code Example: aigateway.credential_not_found
	Code   *string                 `json:"code,omitempty"`
	Params *map[string]interface{} `json:"params,omitempty"`
}

// AiGatewaySetCredentialRequest defines model for AiGatewaySetCredentialRequest.
type AiGatewaySetCredentialRequest struct {
	// APIKey The vendor API key. Never echoed back on any response.
	APIKey string `json:"apiKey"`

	// BaseURL The OpenAI-compatible endpoint's base URL for this credential. Omit to leave the provider's own default in effect.
	BaseURL *string `json:"baseUrl,omitempty"`
}

// Provider defines model for Provider.
type Provider = string

// AiGatewaySetPlatformCredentialJSONRequestBody defines body for AiGatewaySetPlatformCredential for application/json ContentType.
type AiGatewaySetPlatformCredentialJSONRequestBody = AiGatewaySetCredentialRequest

// AiGatewaySetTenantCredentialJSONRequestBody defines body for AiGatewaySetTenantCredential for application/json ContentType.
type AiGatewaySetTenantCredentialJSONRequestBody = AiGatewaySetCredentialRequest

// ServerInterface represents all server handlers.
type ServerInterface interface {
	// AiGatewayGetCredential Report which scope currently answers for a provider.
	// (GET /api/v1/ai-gateway/credentials/{provider})
	AiGatewayGetCredential(w http.ResponseWriter, r *http.Request, provider Provider)
	// AiGatewaySetPlatformCredential Set the platform-wide default credential.
	// (PUT /api/v1/ai-gateway/credentials/{provider}/platform)
	AiGatewaySetPlatformCredential(w http.ResponseWriter, r *http.Request, provider Provider)
	// AiGatewaySetTenantCredential Set the caller's own tenant's BYOK credential.
	// (PUT /api/v1/ai-gateway/credentials/{provider}/tenant)
	AiGatewaySetTenantCredential(w http.ResponseWriter, r *http.Request, provider Provider)
}

// ServerInterfaceWrapper converts contexts to parameters.
type ServerInterfaceWrapper struct {
	Handler            ServerInterface
	HandlerMiddlewares []MiddlewareFunc
	ErrorHandlerFunc   func(w http.ResponseWriter, r *http.Request, err error)
}

type MiddlewareFunc func(http.Handler) http.Handler

// AiGatewayGetCredential operation middleware
func (siw *ServerInterfaceWrapper) AiGatewayGetCredential(w http.ResponseWriter, r *http.Request) {

	var err error
	_ = err

	// ------------- Path parameter "provider" -------------
	var provider Provider

	err = runtime.BindStyledParameterWithOptions("simple", "provider", r.PathValue("provider"), &provider, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationPath, Explode: false, Required: true, Type: "string", Format: "", ValueIsUnescaped: true})
	if err != nil {
		siw.ErrorHandlerFunc(w, r, &InvalidParamFormatError{ParamName: "provider", Err: err})
		return
	}

	handler := http.Handler(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		siw.Handler.AiGatewayGetCredential(w, r, provider)
	}))

	for _, middleware := range siw.HandlerMiddlewares {
		handler = middleware(handler)
	}

	handler.ServeHTTP(w, r)
}

// AiGatewaySetPlatformCredential operation middleware
func (siw *ServerInterfaceWrapper) AiGatewaySetPlatformCredential(w http.ResponseWriter, r *http.Request) {

	var err error
	_ = err

	// ------------- Path parameter "provider" -------------
	var provider Provider

	err = runtime.BindStyledParameterWithOptions("simple", "provider", r.PathValue("provider"), &provider, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationPath, Explode: false, Required: true, Type: "string", Format: "", ValueIsUnescaped: true})
	if err != nil {
		siw.ErrorHandlerFunc(w, r, &InvalidParamFormatError{ParamName: "provider", Err: err})
		return
	}

	handler := http.Handler(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		siw.Handler.AiGatewaySetPlatformCredential(w, r, provider)
	}))

	for _, middleware := range siw.HandlerMiddlewares {
		handler = middleware(handler)
	}

	handler.ServeHTTP(w, r)
}

// AiGatewaySetTenantCredential operation middleware
func (siw *ServerInterfaceWrapper) AiGatewaySetTenantCredential(w http.ResponseWriter, r *http.Request) {

	var err error
	_ = err

	// ------------- Path parameter "provider" -------------
	var provider Provider

	err = runtime.BindStyledParameterWithOptions("simple", "provider", r.PathValue("provider"), &provider, runtime.BindStyledParameterOptions{ParamLocation: runtime.ParamLocationPath, Explode: false, Required: true, Type: "string", Format: "", ValueIsUnescaped: true})
	if err != nil {
		siw.ErrorHandlerFunc(w, r, &InvalidParamFormatError{ParamName: "provider", Err: err})
		return
	}

	handler := http.Handler(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		siw.Handler.AiGatewaySetTenantCredential(w, r, provider)
	}))

	for _, middleware := range siw.HandlerMiddlewares {
		handler = middleware(handler)
	}

	handler.ServeHTTP(w, r)
}

type UnescapedCookieParamError struct {
	ParamName string
	Err       error
}

func (e *UnescapedCookieParamError) Error() string {
	return fmt.Sprintf("error unescaping cookie parameter '%s'", e.ParamName)
}

func (e *UnescapedCookieParamError) Unwrap() error {
	return e.Err
}

type UnmarshalingParamError struct {
	ParamName string
	Err       error
}

func (e *UnmarshalingParamError) Error() string {
	return fmt.Sprintf("Error unmarshaling parameter %s as JSON: %s", e.ParamName, e.Err.Error())
}

func (e *UnmarshalingParamError) Unwrap() error {
	return e.Err
}

type RequiredParamError struct {
	ParamName string
}

func (e *RequiredParamError) Error() string {
	return fmt.Sprintf("Query argument %s is required, but not found", e.ParamName)
}

type RequiredHeaderError struct {
	ParamName string
	Err       error
}

func (e *RequiredHeaderError) Error() string {
	return fmt.Sprintf("Header parameter %s is required, but not found", e.ParamName)
}

func (e *RequiredHeaderError) Unwrap() error {
	return e.Err
}

type InvalidParamFormatError struct {
	ParamName string
	Err       error
}

func (e *InvalidParamFormatError) Error() string {
	return fmt.Sprintf("Invalid format for parameter %s: %s", e.ParamName, e.Err.Error())
}

func (e *InvalidParamFormatError) Unwrap() error {
	return e.Err
}

type TooManyValuesForParamError struct {
	ParamName string
	Count     int
}

func (e *TooManyValuesForParamError) Error() string {
	return fmt.Sprintf("Expected one value for %s, got %d", e.ParamName, e.Count)
}

// Handler creates http.Handler with routing matching OpenAPI spec.
func Handler(si ServerInterface) http.Handler {
	return HandlerWithOptions(si, StdHTTPServerOptions{})
}

// ServeMux is an abstraction of [http.ServeMux].
type ServeMux interface {
	HandleFunc(pattern string, handler func(http.ResponseWriter, *http.Request))
	http.Handler
}

type StdHTTPServerOptions struct {
	BaseURL          string
	BaseRouter       ServeMux
	Middlewares      []MiddlewareFunc
	ErrorHandlerFunc func(w http.ResponseWriter, r *http.Request, err error)
}

// HandlerFromMux creates http.Handler with routing matching OpenAPI spec based on the provided mux.
func HandlerFromMux(si ServerInterface, m ServeMux) http.Handler {
	return HandlerWithOptions(si, StdHTTPServerOptions{
		BaseRouter: m,
	})
}

func HandlerFromMuxWithBaseURL(si ServerInterface, m ServeMux, baseURL string) http.Handler {
	return HandlerWithOptions(si, StdHTTPServerOptions{
		BaseURL:    baseURL,
		BaseRouter: m,
	})
}

// HandlerWithOptions creates http.Handler with additional options
func HandlerWithOptions(si ServerInterface, options StdHTTPServerOptions) http.Handler {
	m := options.BaseRouter

	if m == nil {
		m = http.NewServeMux()
	}
	if options.ErrorHandlerFunc == nil {
		options.ErrorHandlerFunc = func(w http.ResponseWriter, r *http.Request, err error) {
			http.Error(w, err.Error(), http.StatusBadRequest)
		}
	}

	wrapper := ServerInterfaceWrapper{
		Handler:            si,
		HandlerMiddlewares: options.Middlewares,
		ErrorHandlerFunc:   options.ErrorHandlerFunc,
	}

	m.HandleFunc(http.MethodGet+" "+options.BaseURL+"/api/v1/ai-gateway/credentials/{provider}", wrapper.AiGatewayGetCredential)
	m.HandleFunc(http.MethodPut+" "+options.BaseURL+"/api/v1/ai-gateway/credentials/{provider}/tenant", wrapper.AiGatewaySetTenantCredential)
	m.HandleFunc(http.MethodPut+" "+options.BaseURL+"/api/v1/ai-gateway/credentials/{provider}/platform", wrapper.AiGatewaySetPlatformCredential)

	return m
}
